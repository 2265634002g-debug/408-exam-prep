import type { KnowledgePoint } from '@/types/knowledge';

export const cnKnowledgePoints: KnowledgePoint[] = [

  // ===========================================================
  // 第1章 计算机网络体系结构
  // ===========================================================

  // --- 1.1 概述 ---
  {
    id: 'cn-1-1-1', chapterId: 'cn-1', title: '计算机网络概述',
    keyConcepts: ['计算机网络', '组成', '功能', '分类', 'LAN', 'WAN', 'MAN'], relatedPoints: [],
    content: `## 计算机网络的定义
> 将分散的、具有独立功能的计算机系统，通过通信设备与线路连接起来，由网络软件实现资源共享和信息传递。

---

## 组成
- **硬件**：计算机、通信设备、传输介质
- **软件**：协议、网络操作系统
- **协议**：通信双方共同遵守的规则集合

---

## 分类
| 分类方式 | 类型 |
|----------|------|
| 范围 | LAN(局域网)、WAN(广域网)、MAN(城域网) |
| 传输技术 | 广播式、点对点 |
| 拓扑 | 星形、总线、环形、网状 |

---

## 考试辨析
- 408 计网 25 分，以选择题为主
- 协议三要素：语法、语义、同步`,
  },

  // --- 1.2 参考模型 ---
  {
    id: 'cn-1-2-1', chapterId: 'cn-1', title: '计算机网络体系结构与参考模型',
    keyConcepts: ['OSI', 'TCP/IP', '五层协议', '分层', 'PDU', 'SDU'], relatedPoints: [],
    content: `## OSI 七层参考模型

\`\`\`
应用层        ← 应用进程间的通信
表示层        ← 数据格式转换/加密
会话层        ← 对话管理
传输层        ← 端到端可靠传输（TCP/UDP）
网络层        ← 路由选择（IP）
数据链路层    ← 帧传输/差错控制
物理层        ← 比特流传输
\`\`\`

---

## TCP/IP 四层模型

| TCP/IP | 对应 OSI | 典型协议 |
|--------|---------|----------|
| 应用层 | 应用+表示+会话 | HTTP/DNS/FTP/SMTP |
| 传输层 | 传输层 | TCP/UDP |
| 网际层 | 网络层 | **IP** |
| 网络接口层 | 数据链路+物理 | Ethernet/PPP |

---

## 五层教学模型（408 采用）

物理层 → 数据链路层 → 网络层 → 传输层 → 应用层

---

## 考试辨析
- 408 考查的是**五层模型**
- 传输层是**端到端**，网络层以下是**点到点**
- OSI 上层=端到端，下层=点到点`,
  },

  // ===========================================================
  // 第2章 物理层
  // ===========================================================

  // --- 2.1 通信基础 ---
  {
    id: 'cn-2-1-1', chapterId: 'cn-2', title: '通信基础',
    keyConcepts: ['信道', '带宽', '奈奎斯特', '香农定理', '编码', '调制'], relatedPoints: [],
    content: `## 基本概念
| 术语 | 含义 |
|------|------|
| 信道 | 信号传输的通道 |
| 带宽 | 信道能通过的频率范围（Hz） |
| 数据率 | 单位时间传输的比特数（bps） |
| 码元 | 一个数字脉冲（信号单元） |

---

## 奈奎斯特定理（无噪声）
$$C_{max} = 2W \\log_2 V$$
$W$ = 带宽(Hz)，$V$ = 离散电平数

## 香农定理（有噪声）
$$C_{max} = W \\log_2(1 + S/N)$$
$S/N$ = 信噪比，$dB = 10 \\log_{10}(S/N)$

---

## 编码与调制
| 方式 | 用途 | 举例 |
|------|------|------|
| 数字→数字（编码） | 数字信号传输 | NRZ、曼彻斯特、差分曼彻斯特 |
| 数字→模拟（调制） | 模拟信道传数字 | ASK/FSK/PSK/QAM |
| 模拟→数字（编码） | PCM | 采样→量化→编码 |

---

## 考试辨析
- 奈奎斯特：无噪声时的**上限**
- 香农定理：有噪声时的**上限**——更接近实际`,
  },

  // --- 2.2 传输介质 ---
  {
    id: 'cn-2-2-1', chapterId: 'cn-2', title: '传输介质',
    keyConcepts: ['双绞线', '同轴电缆', '光纤', '无线电'], relatedPoints: [],
    content: `## 常见传输介质
| 介质 | 特点 | 适用 |
|------|------|------|
| **双绞线** | 便宜、易安装 | LAN（以太网） |
| **同轴电缆** | 抗干扰好 | 有线电视 |
| **光纤** | 带宽大、距离远、抗干扰 | 骨干网、WAN |
| 无线电 | 无需线缆 | WiFi、蜂窝网络 |

---

## 考试辨析
- 光纤使用**全反射**原理——多模/单模
- 双绞线通过**绞合**减少串扰`,
  },

  // --- 2.3 物理层设备 ---
  {
    id: 'cn-2-3-1', chapterId: 'cn-2', title: '物理层设备',
    keyConcepts: ['中继器', '集线器', '放大器'], relatedPoints: [],
    content: `## 物理层设备
| 设备 | 功能 |
|------|------|
| **中继器** | 信号再生放大，延长传输距离 |
| **集线器 (Hub)** | 多端口中继器，**半双工，共享带宽** |

---

## 考试辨析
- 中继器和集线器工作在**物理层**——只转发信号
- 不能连接不同协议的网段`,
  },

  // ===========================================================
  // 第3章 数据链路层
  // ===========================================================

  // --- 3.1 概述 ---
  {
    id: 'cn-3-1-1', chapterId: 'cn-3', title: '数据链路层概述',
    keyConcepts: ['数据链路层', '帧', '成帧', '差错控制', '流量控制'], relatedPoints: [],
    content: `## 数据链路层的功能
1. 为网络层提供服务接口
2. **成帧**：将比特流组装成帧
3. **差错控制**：检测/纠正误码
4. **流量控制**：协调发送方和接收方速率

---

## 成帧方法
| 方法 | 原理 |
|------|------|
| 字符计数 | 帧头标明长度 |
| 字符填充 | 特殊字符界定帧边界 |
| 零比特填充 | 5个连续1后插0（HDLC） |

---

## 考试辨析
- 数据链路层的传输单位是**帧**
- 零比特填充法：发送 0111110 → 01111100（5个1后插0）`,
  },

  // --- 3.2 组帧 ---
  {
    id: 'cn-3-2-1', chapterId: 'cn-3', title: '组帧',
    keyConcepts: ['组帧', '字符计数', '字符填充', '零比特填充', 'HDLC'], relatedPoints: ['cn-3-1-1'],
    content: `## 组帧 (Framing)

> 将比特流划分为帧——数据链路层的核心功能。

---

## 四种组帧方法

| 方法 | 原理 | 优缺点 |
|------|------|--------|
| **字符计数** | 帧头部标明帧长度 | 简单，但计数出错则后续全乱 |
| **字符填充** | 用特殊字符（DLE STX/ETX）定界 | 需字符转义 |
| **零比特填充** | 5个连续1后插入一个0 | **HDLC/PPP 采用**，最常用 |
| **物理层编码违例** | 利用无效编码作为帧边界 | 如曼彻斯特编码的违例码 |

---

## 零比特填充法（高频考点）
发送：数据中出现 5 个连续 1 → 自动插入 1 个 0
接收：发现 5 个连续 1 → 检查下一位 → 0 则删除，1 则可能是帧尾

例：原始数据 01111110 → 传输 011111010（第5个1后插0）

---

## 考试辨析
- 零比特填充是 HDLC 协议的标准组帧方式
- 408 选择题常考"5个1后插0"的具体过程
`,
  },

  // --- 3.3 差错控制 ---
  {
    id: 'cn-3-3-1', chapterId: 'cn-3', title: '差错控制',
    keyConcepts: ['差错控制', 'CRC', '奇偶校验', '海明码', '纠错', '检错'], relatedPoints: [],
    content: `## 差错控制技术

| 方法 | 类型 | 说明 |
|------|------|------|
| **奇偶校验** | 检错 | 增加一位使1的个数为奇/偶 |
| **CRC（循环冗余校验）** | 检错 | 除法求余，最常用 |
| **海明码** | **纠错** | 可定位并纠正单比特错误 |

---

## CRC 原理
1. 发送方：$M(x) \\cdot 2^k \\div G(x)$ → 余数 = 校验码
2. 接收方：收到的帧 $\\div G(x)$ → 余数为0则无误

---

## 考试辨析
- CRC 只能**检错**，不能纠错
- 海明码可以**纠错**（单比特）
- $n$ 位校验位最多检测 $n$ 位以内的错误`,
  },

  // --- 3.4 流量控制 ---
  {
    id: 'cn-3-4-1', chapterId: 'cn-3', title: '流量控制与可靠传输',
    keyConcepts: ['停止等待', 'GBN', 'SR', '滑动窗口', 'ARQ', 'ACK'], relatedPoints: [],
    content: `## 滑动窗口协议

| 协议 | 窗口大小(发送) | 窗口大小(接收) | 重传 |
|------|--------------|--------------|------|
| **停止-等待** | 1 | 1 | 超时 |
| **GBN (回退N)** | $2^n-1$ | 1 | **批量重传** |
| **SR (选择重传)** | $2^{n-1}$ | $2^{n-1}$ | **只重传错帧** |

---

## 三种协议对比
| | 停止-等待 | GBN | SR |
|------|----------|-----|-----|
| 效率 | 低 | 中 | 高 |
| 实现 | 最简单 | 简单 | 复杂 |
| 缓冲区 | 小 | 小 | 大 |

---

## 考试辨析
- GBN 接收方只接受**按序到达的帧**
- SR 接收方可缓存**乱序帧**
- 序号空间大小 $\\ge 窗口大小 \\times 2$（避免新旧帧混淆）`,
  },

  // --- 3.5 介质访问控制 ---
  {
    id: 'cn-3-5-1', chapterId: 'cn-3', title: '介质访问控制',
    keyConcepts: ['CSMA/CD', 'CSMA/CA', 'MAC', 'ALOHA', '碰撞', '冲突域'], relatedPoints: [],
    content: `## 介质访问控制的分类

### 信道划分
- FDMA、TDMA、CDMA

### 随机访问
| 协议 | 说明 | 适用 |
|------|------|------|
| ALOHA | 想发就发 | — |
| CSMA | 先听后发 | — |
| **CSMA/CD** | 先听后发+边发边听+冲突停止 | **以太网** |
| **CSMA/CA** | 碰撞避免 | WiFi |

---

## CSMA/CD 核心
1. 先听后发
2. 边发边听
3. 冲突停止 → 发冲突加强信号
4. 二进制指数退避（随机等待后重试）

最小帧长 = 传播时延 × 数据率 × 2

---

## CSMA/CD vs CSMA/CA
| | CD | CA |
|------|-----|-----|
| 检测方式 | 碰撞检测 | 碰撞**避免** |
| 适用 | 有线（能检测） | 无线（无法同时收发） |

---

## 考试辨析
- 以太网使用 CSMA/CD（经典考点）
- 最小帧长的公式必须掌握
- 冲突域：CSMA/CD 环境中可能发生碰撞的计算机集合`,
  },

  // --- 3.6 局域网 ---
  {
    id: 'cn-3-6-1', chapterId: 'cn-3', title: '局域网 (LAN)',
    keyConcepts: ['以太网', 'MAC地址', 'VLAN', '交换机', 'STP'], relatedPoints: [],
    content: `## 以太网 (Ethernet)

> 最经典的局域网技术——IEEE 802.3 标准。

- MAC 帧格式：目的地址 | 源地址 | 类型/长度 | 数据 | FCS
- MAC 地址 48 位（6 字节）
- 工作在**数据链路层**

---

## 交换机 (Switch)

| 特性 | 说明 |
|------|------|
| 工作层 | 数据链路层（转发帧） |
| 自学习 | 通过源 MAC 地址学习端口映射 |
| 转发 | 目的 MAC 不在表 → 洪泛 |
| 冲突域 | 每个端口独立冲突域 |

---

## VLAN

> 虚拟局域网——在交换机上将端口逻辑分组，隔离广播域。

---

## 考试辨析
- 交换机隔离**冲突域**——每端口一个冲突域
- 路由器隔离**广播域**——每端口一个广播域`,
  },

  // --- 3.7 广域网 ---
  {
    id: 'cn-3-7-1', chapterId: 'cn-3', title: '广域网 (WAN)',
    keyConcepts: ['广域网', 'PPP', 'HDLC', 'WAN'], relatedPoints: ['cn-3-6-1'],
    content: `## 广域网 vs 局域网

| | LAN | WAN |
|------|-----|-----|
| 范围 | 小 | 大（跨城市/国家） |
| 协议 | 以太网/IEEE 802 | PPP/HDLC/帧中继/ATM |
| 拓扑 | 广播 | 点对点 |

---

## PPP 协议
- Point-to-Point Protocol：点对点链路层协议
- 功能：成帧 + 链路控制(LCP) + 网络控制(NCP)
- 不提供可靠传输（序号/确认机制）
- 用于拨号上网、PPPoE

---

## 考试辨析
- PPP 是面向**字节**的协议，HDLC 是面向**比特**的
- 408 对 WAN 要求较低，了解 PPP 和 HDLC 的基本概念即可
`,
  },

  // --- 3.8 设备 ---
  {
    id: 'cn-3-8-1', chapterId: 'cn-3', title: '数据链路层设备',
    keyConcepts: ['网桥', '交换机', '冲突域', '广播域'], relatedPoints: [],
    content: `## 网桥 vs 交换机
| | 网桥 | 交换机 |
|------|------|--------|
| 端口数 | 少 | 多 |
| 转发 | 基于软件/存储转发 | 基于硬件 |
| 本质 | 交换机 = **多端口网桥** |

---

## 关键概念
| 概念 | 含义 |
|------|------|
| **冲突域** | 同一时刻只能一个设备发送数据的范围 |
| **广播域** | 能收到同一广播的范围 |

---

## 考试辨析
- 交换机/网桥隔离**冲突域**（不隔离广播域）
- 路由器隔离**冲突域 + 广播域**
- 中继器/集线器不隔离任何域`,
  },

  // ===========================================================
  // 第4章 网络层
  // ===========================================================

  // --- 4.1 概述 ---
  {
    id: 'cn-4-1-1', chapterId: 'cn-4', title: '网络层功能与路由概述',
    keyConcepts: ['网络层', '路由', '转发', '虚电路', '数据报'], relatedPoints: [],
    content: `## 网络层两大功能

1. **路由选择**：确定分组从源到目的地的路径
2. **分组转发**：路由器将分组从输入端口移到输出端口

---

## 虚电路 vs 数据报

| | 虚电路 | 数据报 |
|------|--------|--------|
| 连接建立 | 需要 | 不需要 |
| 路由确定 | 一次确定 | 每个分组独立决策 |
| 顺序 | 按序到达 | 可能乱序 |
| 典型 | ATM | **IP（互联网）** |

---

## 考试辨析
- 互联网采用**数据报**方式——无连接
- 路由算法是大题常考内容（RIP/OSPF/BGP）`,
  },

  // --- 4.2 IPv4 ---
  {
    id: 'cn-4-2-1', chapterId: 'cn-4', title: 'IPv4',
    keyConcepts: ['IPv4', 'IP地址', '子网划分', 'CIDR', 'NAT', 'ARP', 'DHCP'], relatedPoints: [],
    content: `## IPv4 地址
- 32 位，点分十进制
- 分类：A(0~127)、B(128~191)、C(192~223)、D(组播)、E(保留)

---

## 子网划分与 CIDR

### 子网划分
- IP = 网络号 + 子网号 + 主机号
- 子网掩码：网络位全1，主机位全0

### CIDR（无类别域间路由）
- IP/前缀长度（如 192.168.1.0/24）
- 路由聚合：合并多个网络为一个超网

---

## 核心协议
| 协议 | 功能 |
|------|------|
| **ARP** | IP → MAC 地址映射 |
| **DHCP** | 自动分配 IP 地址 |
| **NAT** | 私有 IP ↔ 公网 IP 转换 |

---

## 考试辨析
- 子网划分计算是408必考题型
- ARP 在局域网内广播——找同一网段的 MAC
- NAT 使内网多设备共享一个公网 IP`,
  },

  // --- 4.3 IPv6 ---
  {
    id: 'cn-4-3-1', chapterId: 'cn-4', title: 'IPv6',
    keyConcepts: ['IPv6', '地址格式', '双冒号表示', 'IPv4兼容', '隧道'], relatedPoints: ['cn-4-2-1'],
    content: `## IPv6 地址

- **128 位**（IPv4 仅 32 位）
- 冒号十六进制：如 \`2001:0db8:0000:0000:0000:ff00:0042:8329\`
- 简化：省略前导 0 + 双冒号压缩（一次）

---

## IPv6 vs IPv4

| | IPv4 | IPv6 |
|------|------|------|
| 地址长度 | 32 位 | **128 位** |
| 首部 | 变长(20~60B) | 固定 40B（扩展首部） |
| 校验和 | 有 | **无**（交给上层） |
| 分片 | 路由器可分片 | **只允许源结点分片** |
| NAT | 广泛使用 | 不需要 |
| 安全性 | 可选(IPSec) | **内置 IPSec** |

---

## IPv4 → IPv6 过渡
- **双协议栈**：同时支持 IPv4 和 IPv6
- **隧道技术**：IPv6 数据报封装在 IPv4 中穿过 IPv4 网络

---

## 考试辨析
- IPv6 地址 128 位——地址空间大到"每一粒沙子都能分配"
- IPv6 首部固定 40 字节——去掉了很多 IPv4 的字段（校验和/分片）
`,
  },

  // --- 4.4 路由 ---
  {
    id: 'cn-4-4-1', chapterId: 'cn-4', title: '路由算法与路由协议',
    keyConcepts: ['RIP', 'OSPF', 'BGP', '距离向量', '链路状态', '路径向量'], relatedPoints: [],
    content: `## 内部网关协议 (IGP) — 同一 AS 内

### RIP (Routing Information Protocol)
| 属性 | 说明 |
|------|------|
| 算法 | **距离向量 (Distance Vector)** —— Bellman-Ford |
| 度量 | **跳数**（最大 15 跳 = 不可达，16 = 无穷） |
| 更新 | 每 30 秒广播一次完整路由表 |
| 传输 | UDP 520 端口 |
| 收敛 | 慢——"好消息传得快，坏消息传得慢" |
| 问题 | **慢收敛** → 路由环路 → 计数到无穷 |
| 解决 | 水平分割、毒性反转、触发更新 |
| 适用 | 小型网络（< 16 跳） |

### OSPF (Open Shortest Path First)
| 属性 | 说明 |
|------|------|
| 算法 | **链路状态 (Link State)** —— Dijkstra SPF |
| 更新 | 链路状态发生变化时才发送 LSA（增量更新） |
| 传输 | **直接用 IP**（协议号 89），不需要 UDP/TCP |
| 设计 | 分 Area（Area 0 为骨干区），层次化 |
| 收敛 | **快**——基于全网拓扑，计算最短路径树 |
| 适用 | 大型企业网络 |

---
## 外部网关协议 (EGP) — 跨 AS

### BGP (Border Gateway Protocol)
| 属性 | 说明 |
|------|------|
| 算法 | **路径向量 (Path Vector)** |
| 传输 | TCP 179 端口（可靠传输） |
| 策略 | **基于策略路由**——不只看最短路径 |
| 更新 | 只发送变化的路由（增量） |
| 适用 | AS 之间——互联网核心路由协议 |

---
## 三种协议对比（必考）
| | RIP | OSPF | BGP |
|------|-----|------|-----|
| 算法类型 | DV | LS | PV |
| 范围 | AS 内 | AS 内 | AS 间 |
| 度量 | 跳数 | 开销(带宽) | 路径属性 |
| 传输层 | UDP 520 | IP 协议 89 | TCP 179 |
| 交换内容 | 路由表 | 链路状态 | 路径向量 |
| 更新方式 | 周期性广播 | 触发式洪泛 | 增量更新 |

---
## 考试辨析
- RIP/OSPF/BGP 是 408 计网的核心考点——几乎每年都有选择题
- RIP 16 跳不可达 → 限制了网络直径
- OSPF 用 Dijkstra 算法计算最短路径树（Sink Tree）
- BGP 是互联网的"邮政系统"——跨运营商路由必须用 BGP
- 区分"域内路由(IGP)"与"域间路由(EGP)"——选择题常见陷阱`,
  },

  // --- 4.6 移动 IP ---
  {
    id: 'cn-4-6-1', chapterId: 'cn-4', title: '移动 IP',
    keyConcepts: ['移动IP', '家乡代理', '外地代理', '转交地址', '隧道'], relatedPoints: [],
    content: `## 移动 IP

> 移动主机在不同网络间移动时保持通信不中断的机制。

---

## 三个关键实体
| 实体 | 角色 |
|------|------|
| **家乡代理** | 主机在"家乡网络"的代理——截获发往移动主机的分组 |
| **外地代理** | 主机当前所在网络的代理 |
| **转交地址** | 移动主机在外地网络中的临时地址 |

---

## 基本流程
1. 移动主机到达外地网络 → 获取转交地址
2. 向外地代理和家乡代理注册
3. 通信对端发送分组 → 家乡代理截获 → 隧道封装 → 外地代理 → 移动主机
4. 移动主机发出的分组直接发送给通信对端

---

## 考试辨析
- 408 对移动 IP 要求较低
- 理解"三角路由"概念即可——家乡代理→外地网络多走了一段路
`,
  },

  // --- 4.5 IP 多播 ---
  {
    id: 'cn-4-5-1', chapterId: 'cn-4', title: 'IP 多播',
    keyConcepts: ['多播', '组播', 'IGMP', 'D类地址'], relatedPoints: [],
    content: `## IP 多播

> 一个源点发送，**一组**指定接收者接收——介于单播和广播之间。

---

## 多播地址
- IPv4：D 类地址 224.0.0.0 ~ 239.255.255.255
- 多播 MAC：01-00-5E-xx-xx-xx（IP 多播 → MAC 多播的映射）

---

## IGMP (Internet Group Management Protocol)
- 主机-路由器之间的多播组成员管理协议
- 主机通过 IGMP 告知路由器"我要加入/离开某多播组"

---

## 考试辨析
- 408 对多播要求较低，了解 D 类地址和 IGMP 基本概念即可
- 多播 MAC 地址的低 23 位 = 多播 IP 的低 23 位
`,
  },

  // --- 4.7 设备 ---
  {
    id: 'cn-4-7-1', chapterId: 'cn-4', title: '网络层设备',
    keyConcepts: ['路由器', '路由表', '转发', '默认网关'], relatedPoints: [],
    content: `## 路由器
> 工作在网络层的互联设备——根据 IP 地址转发分组。

- 每端口一个 IP 地址（在不同网络）
- 隔离冲突域，也隔离广播域
- 存储 IP 分组，查路由表转发

---

## 交换机 vs 路由器
| | 交换机 | 路由器 |
|------|--------|--------|
| 工作层 | 数据链路层 | **网络层** |
| 转发依据 | MAC 地址 | **IP 地址** |
| 隔离冲突域 | ✅ | ✅ |
| 隔离广播域 | ❌ | ✅ |

---

## 考试辨析
- 路由器是**网络层**设备——408 常考各设备对应层次`,
  },

  // ===========================================================
  // 第5章 传输层
  // ===========================================================

  // --- 5.1 概述 ---
  {
    id: 'cn-5-1-1', chapterId: 'cn-5', title: '传输层概述',
    keyConcepts: ['传输层', '端口', '复用', '分用', 'TCP', 'UDP'], relatedPoints: [],
    content: `## 传输层的功能
1. 端到端通信（进程到进程）
2. 复用和分用（端口机制）
3. 差错控制（TCP）

---

## 端口号
| 类型 | 范围 | 举例 |
|------|------|------|
| 熟知端口 | 0~1023 | HTTP(80), DNS(53), FTP(21) |
| 注册端口 | 1024~49151 | — |
| 临时端口 | 49152~65535 | 客户端使用 |

---

## 考试辨析
- **传输层 = 端到端**——只看源和目的进程
- IP 定位到主机，端口定位到进程
- Socket (套接字) = IP + 端口`,
  },

  // --- 5.2 UDP ---
  {
    id: 'cn-5-2-1', chapterId: 'cn-5', title: 'UDP协议',
    keyConcepts: ['UDP', '无连接', '无确认', '无流量控制', '首部8字节'], relatedPoints: ['cn-5-1-1'],
    content: `## UDP 的特点

> User Datagram Protocol —— 无连接、尽最大努力交付。

| 特性 | 说明 |
|------|------|
| 无连接 | 不建立连接 |
| 无拥塞控制 | 想发就发 |
| 不可靠 | 不保证到达 |
| 首部开销小 | 仅 8 字节 |
| 支持广播/组播 | TCP 不支持 |

---

## UDP 首部格式
\`\`\`
源端口 | 目的端口 | 长度 | 校验和
   2    |    2     |  2   |   2  (字节)
\`\`\`

---

## UDP 适用场景
- DNS、DHCP、RIP（实时通信，丢包可重试）
- 视频直播、VoIP（少量丢包可接受，低延迟更重要）

---

## 考试辨析
- UDP 首部 8 字节，TCP 首部最少 20 字节
- UDP 没有拥塞控制——在高拥塞时可能导致网络崩溃`,
  },

  // --- 5.3 TCP ---
  {
    id: 'cn-5-3-1', chapterId: 'cn-5', title: 'TCP协议',
    keyConcepts: ['TCP', '三次握手', '四次挥手', '滑动窗口', '拥塞控制', '慢开始', '拥塞避免', '快重传', '快恢复', 'TIME_WAIT'], relatedPoints: ['cn-5-1-1'],
    content: `## TCP 的特点
> Transmission Control Protocol —— **面向连接、可靠、字节流**传输。
| 特性 | 说明 |
|------|------|
| 面向连接 | 通信前须建立连接（三次握手） |
| 可靠传输 | 确认 + 超时重传 + 滑动窗口 |
| 全双工 | 双方可同时收发数据 |
| 面向字节流 | 不保留报文边界（与 UDP 相反） |
| 流量控制 | 滑动窗口（接收方控制发送速率） |
| 拥塞控制 | 检测网络状态，主动调整发送速率 |

---
## TCP 首部格式（最少 20 字节）
| 字段 | 位数 | 说明 |
|------|------|------|
| 源端口 / 目的端口 | 各 16 位 | 标识进程 |
| 序号 (Seq) | 32 位 | 本报文段第一个字节的序号 |
| 确认号 (Ack) | 32 位 | 期望收到对方下一个字节的序号 |
| 数据偏移 | 4 位 | 首部长度（单位 4 字节） |
| 控制位 | URG/ACK/PSH/RST/SYN/FIN | 各 1 位 |
| 窗口 | 16 位 | 接收窗口大小（流量控制） |
| 校验和 | 16 位 | 加伪首部计算 |
| 紧急指针 | 16 位 | URG=1 时有效 |

---
## 三次握手（建立连接）
\`\`\`
客户端                    服务器
|                         |
|--- SYN=1, seq=x ------>|  ① 客户端请求连接
|                         |
|<-- SYN=1, ACK=1,       |  ② 服务器确认 + 请求
|    seq=y, ack=x+1 -----|
|                         |
|--- ACK=1, seq=x+1,     |  ③ 客户端确认
|    ack=y+1 ----------->|
|                         |
|      【连接建立】       |
\`\`\`
> **为什么是三次？** —— 防止已失效的连接请求到达服务器，导致服务器白白等待。

---
## 四次挥手（释放连接）
\`\`\`
客户端                    服务器
|--- FIN=1, seq=u ------>|  ① 客户端请求关闭
|<-- ACK=1, ack=u+1 -----|  ② 服务器确认
|                         |
|<-- FIN=1, seq=v --------|  ③ 服务器请求关闭
|--- ACK=1, ack=v+1 ---->|  ④ 客户端确认
|                         |
|    TIME_WAIT (2MSL)     |
\`\`\`
> **为什么是四次？** —— TCP 全双工，双方需独立关闭。
> **TIME_WAIT = 2MSL**：确保最后的 ACK 能到达服务器，让旧连接的报文在网络中消失。

---
## TCP 可靠传输机制
1. **确认 (ACK)**：接收方确认收到的字节
2. **超时重传 (RTO)**：自适应超时时间（基于 RTT 的加权平均）
3. **滑动窗口**：允许连续发送多个报文而不必等确认
4. **快重传**：收到 3 个重复 ACK → 立即重传（不等超时）

---
## TCP 拥塞控制（四大算法）
> 拥塞窗口 cwnd = 发送方可以发送的数据量。
| 算法 | 阶段 | cwnd 变化 | 触发条件 |
|------|------|-----------|----------|
| **慢开始** | 建连/超时后 | 指数增长（×2 每 RTT） | cwnd < ssthresh |
| **拥塞避免** | cwnd $\\ge$ ssthresh | 线性增长（+1 每 RTT） | 持续 |
| **快重传** | 收到 3 个重复 ACK | 不等超时，立即重传 | 3 dup ACK |
| **快恢复** | 快重传后 | cwnd = cwnd/2，ssthresh = cwnd/2 | 不降到 1 |

---
## 考试辨析
- TCP 是 **408 计网考大题的主要出题范围**
- **三次握手 vs 四次挥手**是永恒的选择题考点
- 拥塞控制算法的 cwnd 变化曲线图需要能手画
- SYN 洪泛攻击：伪造大量 SYN 不回复 ACK——用 SYN Cookie 防御
- TCP 首部 ACK 位和 ack 确认号是两个不同概念——前者是标志位，后者是序号`,
  },

  // ===========================================================
  // 第6章 应用层
  // ===========================================================

  // --- 6.1 应用模型 ---
  {
    id: 'cn-6-1-1', chapterId: 'cn-6', title: '网络应用模型',
    keyConcepts: ['C/S', 'P2P'], relatedPoints: [],
    content: `## C/S 模型（客户/服务器）
- 服务器：永久在线，被动等待请求
- 客户端：主动发起请求
- 典型：Web、FTP、Email

---

## P2P 模型（对等）
- 每个结点既是客户端也是服务器
- 优点：可扩展性强、成本低
- 典型：BitTorrent、区块链

---

## 考试辨析
- C/S 模型服务器是瓶颈——P2P 无中心，更可扩展`,
  },

  // --- 6.2 DNS ---
  {
    id: 'cn-6-2-1', chapterId: 'cn-6', title: 'DNS系统',
    keyConcepts: ['DNS', '域名', '域名解析', '递归查询', '迭代查询', '根域名服务器'], relatedPoints: [],
    content: `## DNS (域名系统)
> 将域名转换为 IP 地址的分布式数据库系统。用 **UDP 53 端口**。

---

## 域名层次结构

根 → 顶级域名(.com/.cn/.org) → 二级域名 → 三级域名...

---

## 两种查询方式
| 方式 | 原理 | 负担 |
|------|------|------|
| **递归查询** | "你帮我去查" | 被查询端 |
| **迭代查询** | "我告诉你去找谁" | 查询端 |

主机→本地DNS：递归
本地DNS→根/顶级/权威：迭代

---

## 考试辨析
- DNS 用 UDP（因为数据量小，超时可重试）
- 域名服务器层次是常考结构`,
  },

  // --- 6.4 电子邮件 ---
  {
    id: 'cn-6-4-1', chapterId: 'cn-6', title: '电子邮件',
    keyConcepts: ['SMTP', 'POP3', 'IMAP', 'MIME', '电子邮件'], relatedPoints: ['cn-6-1-1', 'cn-6-2-1'],
    content: `## 电子邮件系统

---

## 核心协议
| 协议 | 用途 | 传输 | 端口 |
|------|------|------|------|
| **SMTP** | 发送/中继邮件 | TCP | 25 |
| **POP3** | 从服务器下载邮件到本地 | TCP | 110 |
| **IMAP** | 在服务器上管理邮件（不下载） | TCP | 143 |
| **MIME** | 扩展 SMTP 支持多媒体（非文本） | — | — |

---

## 工作流程
1. 发送方用 SMTP 将邮件发送到发送方邮件服务器
2. 发送方服务器用 SMTP 将邮件中继到接收方服务器
3. 接收方用 POP3/IMAP 从服务器读取邮件

---

## POP3 vs IMAP
| | POP3 | IMAP |
|------|------|------|
| 邮件位置 | 下载到本地 | 保存在服务器 |
| 多设备 | 不同步 | **同步** |
| 离线 | ✅ | ❌ |

---

## 考试辨析
- SMTP 只能发 7 位 ASCII——MIME 弥补了此限制
- SMTP 用"推"（发送），POP3/IMAP 用"拉"（接收）
- 408 常考邮件收发涉及的协议组合
`,
  },

  // --- 6.3 FTP ---
  {
    id: 'cn-6-3-1', chapterId: 'cn-6', title: 'FTP',
    keyConcepts: ['FTP', '文件传输', '控制连接', '数据连接', '21', '20'], relatedPoints: ['cn-6-1-1'],
    content: `## FTP (File Transfer Protocol)

> 在网络上进行文件传输的协议。用 **TCP**。

---

## 双连接机制（经典考点）
| 连接 | 端口 | 方向 | 说明 |
|------|------|------|------|
| **控制连接** | 21 | 客户端 → 服务器 | 传输命令和响应（始终保持） |
| **数据连接** | 20 | 服务器 → 客户端 | 传输文件数据（需要时建立） |

---

## 主动模式 vs 被动模式
| 模式 | 数据连接建立方 | 数据端口 | 防火墙友好 |
|------|--------------|----------|-----------|
| PORT (主动) | 服务器发起 | 20 | ❌ 差 |
| PASV (被动) | 客户端发起 | >1024 随机 | ✅ 好 |

---

## 考试辨析
- FTP 用两个 TCP 连接——这是其最鲜明的特征
- 控制连接全程保持，数据连接用完即关闭
- 端口 21 控制 + 20 数据 = FTP 标准模式
`,
  },

  // --- 6.5 WWW ---
  {
    id: 'cn-6-5-1', chapterId: 'cn-6', title: '万维网与HTTP',
    keyConcepts: ['HTTP', 'HTTPS', 'URL', 'HTML', 'Cookie', 'GET', 'POST', '状态码'], relatedPoints: [],
    content: `## HTTP

> 超文本传输协议——万维网的核心。用 **TCP 80 端口**。

---

## HTTP 报文

请求报文：方法 URL 版本 + 首部 + (实体)
响应报文：版本 状态码 短语 + 首部 + (实体)

---

## 常见状态码
| 状态码 | 含义 |
|--------|------|
| 200 | OK |
| 301 | 永久重定向 |
| 304 | 未修改（缓存有效） |
| 404 | 未找到 |
| 500 | 服务器内部错误 |

---

## HTTP 版本演进
| 版本 | 特点 |
|------|------|
| HTTP/1.0 | 每次请求新建TCP连接 |
| HTTP/1.1 | 持久连接、管道化 |
| HTTP/2 | 多路复用、头部压缩 |
| HTTP/3 | 基于QUIC(UDP) |

---

## HTTPS
- HTTP + TLS/SSL → 加密传输
- **TCP 443 端口**

---

## 考试辨析
- HTTP 是无状态协议——用 Cookie 弥补
- GET 是幂等的（重复请求不影响），POST 不是`,
  },
];
