import type { KnowledgePoint } from '@/types/knowledge';

export const osKnowledgePoints: KnowledgePoint[] = [

  // ===========================================================
  // 第1章 操作系统概述
  // ===========================================================

  // --- 1.1 基本概念 ---
  {
    id: 'os-1-1-1', chapterId: 'os-1', title: '操作系统的概念与特征',
    keyConcepts: ['操作系统', '并发', '共享', '虚拟', '异步', '基本特征'], relatedPoints: ['os-1-3-1'],
    content: `## 操作系统的定义
> 操作系统是控制和管理整个计算机硬件与软件资源的**系统软件**，
是用户与计算机硬件之间的接口。

---
## 操作系统的四大特征
| 特征 | 含义 | 说明 |
|------|------|------|
| **并发** | 多个程序同一时间间隔内运行 | 宏观并行、微观串行（单核） |
| **共享** | 多道程序共用系统资源 | 互斥共享 / 同时访问 |
| **虚拟** | 物理实体映射为逻辑对应物 | 虚拟CPU（进程）、虚拟内存 |
| **异步** | 程序走走停停，不可预知 | 并发执行的必然结果 |

> **并发和共享是操作系统最基本的两个特征**——互为存在条件。

---
## 考试辨析
- 并发 ≠ 并行：并发是逻辑上的同时，并行是物理上的同时
- 没有并发和共享，就谈不上虚拟和异步`,
  },

  // --- 1.2 发展与分类 ---
  {
    id: 'os-1-2-1', chapterId: 'os-1', title: '操作系统的发展与分类',
    keyConcepts: ['批处理', '分时', '实时', '网络', '分布式', '个人计算机'], relatedPoints: [],
    content: `## 操作系统的发展阶段

| 阶段 | 特点 | 标志 |
|------|------|------|
| 手工操作 | 人工插拔 | — |
| **单道批处理** | 监督程序控制 | 联机→脱机 |
| **多道批处理** | 多道程序交替运行 | 资源利用率高、吞吐量大 |
| **分时系统** | 时间片轮转 | 交互性强 |
| **实时系统** | 及时响应 | 可靠性高 |
| 网络/分布式 | 资源共享 | 现代主流 |

---

## 考试辨析
- 分时系统的关键是**时间片轮转**——每个用户轮流获得CPU
- 多道批处理的主要目的：**提高资源利用率**（不是交互性）`,
  },

  // --- 1.3 运行环境 ---
  {
    id: 'os-1-3-1', chapterId: 'os-1', title: '操作系统的运行环境',
    keyConcepts: ['内核态', '用户态', '特权指令', '系统调用', '中断', '访管指令'], relatedPoints: ['os-1-1-1', 'co-5-4-1'],
    content: `## CPU 的两种状态

| 状态 | 特征 | 可执行指令 |
|------|------|-----------|
| **内核态（管态）** | OS 运行时 | 全部指令（包括特权指令） |
| **用户态（目态）** | 用户程序运行时 | 只能执行非特权指令 |

---

## 系统调用

> 用户程序请求 OS 服务（如 I/O、创建进程）的方式。

- 通过**访管指令**（陷入指令）进入内核态
- 本质是**受控的中断**（软中断）

\`\`\`
用户程序 → 系统调用 → 陷入 → 内核态 → OS服务 → 返回用户态
\`\`\`

---

## 考试辨析
- 系统调用运行在**内核态**——用户只能通过调用接口使用
- 访管指令 ≠ 特权指令——访管指令是用户态下唯一能"进入内核"的手段
- 中断处理在内核态执行`,
  },

  // --- 1.4 体系结构 ---
  {
    id: 'os-1-4-1', chapterId: 'os-1', title: '操作系统体系结构',
    keyConcepts: ['整体式', '模块化', '层次式', '微内核', '宏内核'], relatedPoints: [],
    content: `## 主要体系结构

| 结构 | 特点 | 代表 |
|------|------|------|
| **整体式** | 模块自由调用，无层次 | 早期 Unix |
| **层次式** | 分层设计，上层依赖下层 | THE |
| **微内核** | 内核最小化，服务移入用户态 | Minix、Mach |
| **宏内核** | 所有功能在内核态 | Linux、Windows |

---

## 微内核 vs 宏内核
| | 微内核 | 宏内核 |
|------|--------|--------|
| 内核大小 | 小 | 大 |
| 可靠性 | 高（服务隔离） | 低（一损俱损） |
| 效率 | 低（频繁IPC） | 高 |
| 典型 | QNX、Fuchsia | Linux、Windows |

---

## 考试辨析
- 微内核只保留最核心功能（进程调度、IPC、地址空间管理）
- 微内核的缺点：**效率低**（用户态服务间切换开销大）`,
  },

  // ===========================================================
  // 第2章 进程管理
  // ===========================================================

  // --- 2.1 进程与线程 ---
  {
    id: 'os-2-1-1', chapterId: 'os-2', title: '进程与线程',
    keyConcepts: ['进程', 'PCB', '线程', 'TCB', '五状态', '上下文切换'], relatedPoints: ['os-2-2-1'],
    content: `## 进程

> 进程 = 程序的一次执行过程，是**资源分配**的基本单位。

进程映像 = PCB + 程序段 + 数据段

**PCB (进程控制块)**：OS 管理进程的唯一标识，包含：
- PID、状态、PC、寄存器、内存信息、打开文件...

---

## 进程的五个状态
\`\`\`
新建 → 就绪 ⇄ 运行 → 终止
         ↕
        阻塞
\`\`\`
| 转换 | 原因 |
|------|------|
| 就绪→运行 | 被调度选中 |
| 运行→就绪 | 时间片用完 |
| 运行→阻塞 | 等待 I/O |
| 阻塞→就绪 | I/O 完成 |

---

## 线程

> 线程是**CPU 调度**的基本单位——同一进程内的线程共享进程资源。

---

## 进程 vs 线程
| | 进程 | 线程 |
|------|------|------|
| 资源 | 独立| **共享**（同进程内） |
| 调度 | 切换开销大 | 切换开销小 |
| 通信 | IPC | 直接读写 |
| 独立性 | 强 | 弱（一个线程崩溃 = 整个进程崩溃） |

---

## 考试辨析
- PCB 是进程存在的**唯一标志**
- 同一进程的线程**共享**堆、全局变量、文件
- 同一进程的线程**独立**栈、寄存器`,
  },

  // --- 2.2 CPU调度 ---
  {
    id: 'os-2-2-1', chapterId: 'os-2', title: 'CPU 调度',
    keyConcepts: ['调度', 'FCFS', 'SJF', '优先级', 'RR', '多级队列', '周转时间', '等待时间', '响应时间'], relatedPoints: ['os-2-1-1'],
    content: `## 调度的三个层次

| 层次 | 频率 | 功能 |
|------|------|------|
| **高级调度** | 很低 | 将作业从辅存调入内存（作业调度） |
| **中级调度** | 中等 | 挂起/激活进程（内存调度） |
| **低级调度** | 很高 | 从就绪队列选进程上 CPU（进程调度） |

---

## 调度算法

| 算法 | 策略 | 特点 |
|------|------|------|
| **FCFS** | 先来先服务 | 简单，对短作业不利 |
| **SJF** | 最短作业优先 | 平均等待时间最优，但需预知运行时间 |
| **优先级** | 优先级高先运行 | 可能导致饥饿 |
| **RR（时间片轮转）** | 轮流获得时间片 | **分时系统标配** |
| **多级反馈队列** | 多队列动态调整 | 现代 OS 实际使用 |

---

## 性能指标
| 指标 | 含义 |
|------|------|
| 周转时间 | 完成时间 - 到达时间 |
| 带权周转时间 | 周转时间 / 运行时间 |
| 等待时间 | 在就绪队列等待的总时间 |
| 响应时间 | 从提交到首次响应的时间 |

---

## 考试辨析
- SJF 平均等待时间最小——但必须预知运行时间
- RR 的时间片太大 → 退化为 FCFS；太小 → 频繁切换开销大
- 408 选择题常需要手工计算各算法的等待时间`,
  },

  // --- 2.3 同步与互斥 ---
  {
    id: 'os-2-3-1', chapterId: 'os-2', title: '同步与互斥',
    keyConcepts: ['互斥', '同步', '临界区', '信号量', 'PV操作', '生产者消费者', '读者写者', '哲学家就餐'], relatedPoints: ['os-2-1-1'],
    content: `## 基本概念
| 概念 | 含义 | 举例 |
|------|------|------|
| **互斥 (Mutual Exclusion)** | 同一时刻只能有一个进程访问临界资源 | 打印机只能一人用 |
| **同步 (Synchronization)** | 进程间按一定顺序协作执行 | A 做完 B 才能做 |
| **临界区 (Critical Section)** | 进程中访问临界资源的那段代码 | 修改共享变量的代码 |
| **临界资源** | 一次只能被一个进程独占的资源 | 共享变量、打印机 |

---
## 临界区使用原则
1. **空闲让进**：无进程在临界区时，申请者可立即进入
2. **忙则等待**：有进程在临界区时，其他进程必须等待
3. **有限等待**：进程在有限时间内一定能进入临界区（不能饥饿）
4. **让权等待**：不能进入临界区时应释放 CPU（不能忙等）

---
## 信号量与 PV 操作（408 核心考点）
> Dijkstra 于 1965 年提出。**P = Proberen (荷兰语: 测试)**，**V = Verhogen (增加)**。

\`\`\`c
// 信号量定义
typedef struct {
    int value;       // 信号量值
    PCB *queue;      // 等待该信号量的进程队列
} Semaphore;

// P 操作 (Wait / Down / 申请资源)
void P(Semaphore s) {
    s.value--;
    if (s.value < 0) {
        block(s.queue);   // 将当前进程放入等待队列
    }
}

// V 操作 (Signal / Up / 释放资源)
void V(Semaphore s) {
    s.value++;
    if (s.value <= 0) {
        wakeup(s.queue);  // 从等待队列唤醒一个进程
    }
}
\`\`\`

> **s.value 的含义**：
> - s.value > 0：表示**还有 s.value 个可用资源**
> - s.value = 0：表示无可用资源，也无等待进程
> - s.value < 0：表示有 |s.value| 个进程在等待

---
## PV 操作的应用模式

### 1. 互斥锁（初值 = 1）
\`\`\`c
Semaphore mutex = {1, NULL};
// 进入临界区前
P(mutex);
// ... 临界区 ...
// 退出临界区后
V(mutex);
\`\`\`

### 2. 同步信号量（初值 = 0）
\`\`\`c
Semaphore sync = {0, NULL};
// 前驱进程：做完事后
V(sync);           // 通知后继
// 后继进程：
P(sync);           // 等待前驱完成
\`\`\`

---
## 经典同步问题

### 生产者-消费者（单缓冲 → 多缓冲）
\`\`\`c
Semaphore empty = {N, NULL};  // 空缓冲区数量
Semaphore full  = {0, NULL};  // 满缓冲区数量
Semaphore mutex = {1, NULL};  // 缓冲区互斥访问

// 生产者
while(1) {
    produce_item();
    P(empty);        // ① 先申请空位
    P(mutex);        // ② 互斥访问缓冲池
    put_item();
    V(mutex);        // ③ 释放互斥
    V(full);         // ④ 增加产品
}

// 消费者
while(1) {
    P(full);         // ① 先申请产品
    P(mutex);        // ② 互斥访问
    get_item();
    V(mutex);        // ③ 释放互斥
    V(empty);        // ④ 增加空位
    consume_item();
}
\`\`\`
> **P操作的顺序不能颠倒！** 先 P(empty) 再 P(mutex) —— 否则可能死锁。

### 读者-写者问题
	**读者优先**：只要有读者在读，新读者可以立即进入。写者必须等所有读者离开。
	**写者优先**：有写者等待时，新读者不能再进入（防止写者饥饿）。
	核心：rw_mutex（读写互斥）+ mutex（count互斥）+ count（读者计数）

	---

	## 经典同步问题三：哲学家进餐问题（Dijkstra, 1965）

	### 问题描述
	> 五位哲学家围坐圆桌，每人面前一盘意大利面。每两位之间有一根筷子。
	> 哲学家交替进行“思考”和“进餐”。进餐需同时拿起左右两根筷子。

	### 简单解法（会导致死锁）
	\`\`\`c
	Semaphore chopstick[5] = {1,1,1,1,1};
	void philosopher(int i) {
	    while(1) {
	        think();
	        P(chopstick[i]);                // 拿左边
	        P(chopstick[(i+1) % 5]);         // 拿右边
	        eat();
	        V(chopstick[i]);                // 放左边
	        V(chopstick[(i+1) % 5]);         // 放右边
	    }
	}
	\`\`\`
	> 五位同时拿左边筷子 → **死锁**（每人拿一根，都在等另一根）。

	### 防止死锁的四种方法（408高频考点）

	**方法一：最多允许四人同时拿筷子（限制并发数）**

	\`\`\`c
	Semaphore limit = {4, NULL};  // 最多4人拿筷子
	void philosopher(int i) {
	    while(1) {
	        think();
	        P(limit);           // 先申请“拿筷子权”
	        P(chopstick[i]);
	        P(chopstick[(i+1)%5]);
	        eat();
	        V(chopstick[i]); V(chopstick[(i+1)%5]);
	        V(limit);
	    }
	}
	\`\`\`

	**方法二：奇数先左后右，偶数先右后左（破坏循环等待）**

	\`\`\`c
	void philosopher(int i) {
	    while(1) {
	        think();
	        if (i % 2 == 0) {           // 偶数号
	            P(chopstick[(i+1)%5]);   // 先右后左
	            P(chopstick[i]);
	        } else {                     // 奇数号
	            P(chopstick[i]);         // 先左后右
	            P(chopstick[(i+1)%5]);
	        }
	        eat();
	        V(chopstick[i]); V(chopstick[(i+1)%5]);
	    }
	}
	\`\`\`

	**方法三：左右两根必须同时拿到（一次性分配，用互斥信号量保护拿筷子过程）**

	**方法四：引入 AND 型信号量——同时 P 多根筷子，要么全拿要么全不拿**

	---

	## PV 题型的答题模板## PV 题型的答题模板
1. 确定**进程数量**和**资源类型**
2. 为每种资源/约束设信号量并定初值
3. 写出每个进程的操作流程，标注 P/V 的位置
4. 检查：P 成对 V（不在同一进程），无死锁

---
## 考试辨析
- **PV 操作是 408 操作系统大题的必考内容**（分值 7-10 分）
- P/V 必然成对出现，但**不在同一进程中**（互斥除外）
- 同步信号量初值通常为 0，互斥信号量初值通常为 1
- 判断信号量初值 = 资源可用数量
- P 操作的次序非常关键——次序错可能导致死锁`,
  },

  // --- 2.4 死锁 ---
  {
    id: 'os-2-4-1', chapterId: 'os-2', title: '死锁',
    keyConcepts: ['死锁', '必要条件', '预防', '避免', '银行家算法', '安全状态', '检测', '解除'], relatedPoints: ['os-2-3-1'],
    content: `## 死锁的定义
> 多个进程因竞争资源而陷入**无限等待**的状态——若无外力，这些进程将永远无法继续。

---
## 死锁的四个必要条件（必须同时满足）
1. **互斥条件**：资源只能独占使用
2. **不可剥夺条件**：已分配的资源不能被强制抢走
3. **请求并保持条件**：进程已持有资源，再申请新资源时被阻塞但不释放已有资源
4. **循环等待条件**：存在进程 $\\{P_0, P_1, ..., P_n\\}$，$P_0$ 等 $P_1$，$P_1$ 等 $P_2$，...，$P_n$ 等 $P_0$

---
## 处理死锁的四种策略
| 策略 | 时机 | 做法 |
|------|------|------|
| **预防 (Prevention)** | 运行前 | 破坏四个必要条件之一 |
| **避免 (Avoidance)** | 运行时 | 判断分配是否会导致不安全状态 |
| **检测 (Detection)** | 运行时 | 定期检查是否存在死锁 |
| **解除 (Recovery)** | 死锁后 | 终止进程或剥夺资源 |

---
## 死锁预防：破坏四个条件
| 破坏条件 | 方法 | 代价 |
|----------|------|------|
| 互斥 | 无法破坏（资源本质属性） | — |
| 不可剥夺 | 允许强制剥夺 | 实现复杂，可能丢失工作 |
| 请求并保持 | **一次性分配全部资源** | 资源利用率低，可能饥饿 |
| 循环等待 | **资源有序分配**（编号递增） | 资源利用率提高 |

---
## 银行家算法（死锁避免）
> Dijkstra 1965 年提出。核心思想：分配前先判断是否仍处于**安全状态**。

### 数据结构
- **Available[m]**：每种资源的可用数量
- **Max[n][m]**：每个进程对每种资源的最大需求
- **Allocation[n][m]**：已分配给每个进程的数量
- **Need[n][m]**：每个进程还需要的数量 = Max - Allocation

### 安全性算法步骤
1. Work = Available, Finish[i] = false (i=1..n)
2. 找满足 Finish[i]=false 且 Need[i] $\\le$ Work 的进程 i
3. 若找到：Work += Allocation[i], Finish[i] = true, 回到步骤 2
4. 若全部 Finish[i] = true → **安全状态**

### 资源请求算法
当进程 i 请求资源 Request[i]：
1. 若 Request[i] $\\le$ Need[i] → 继续；否则出错
2. 若 Request[i] $\\le$ Available → 继续；否则等待
3. 试探分配：Available -= Request[i], Allocation[i] += Request[i], Need[i] -= Request[i]
4. 执行安全性算法 → 安全则分配，不安全则恢复并让进程等待

---
## 考试辨析
- **安全状态 $\\neq$ 无死锁**，不安全状态 $\\neq$ 死锁（只是可能）
- 常考"至少需要多少资源才能确保不发生死锁"
- 408 多次考过银行家算法的大题（手工模拟安全性检查）
- 死锁预防 vs 死锁避免：预防在运行前，避免在运行时`,
  },

  // ===========================================================
  // 第3章 内存管理
  // ===========================================================

  // --- 3.1 内存管理概念 ---
  {
    id: 'os-3-1-1', chapterId: 'os-3', title: '内存管理概念',
    keyConcepts: ['内存管理', '地址转换', '逻辑地址', '物理地址', '链接', '装入', '覆盖', '交换'], relatedPoints: ['os-3-2-1', 'ds-1-1-2'],
    content: `## 内存管理的任务
1. 内存分配与回收
2. 地址转换（逻辑地址 → 物理地址）
3. 内存保护（越界检查）
4. 内存共享

---

## 程序的链接与装入
| 阶段 | 说明 |
|------|------|
| **静态链接** | 程序运行前链接好各模块 |
| **装入时动态链接** | 装入内存时边装入边链接 |
| **运行时动态链接** | 用到哪个模块才链接（DLL） |
| **绝对装入** | 地址固定 |
| **可重定位装入** | 装入时修改地址 |
| **动态运行时装入** | 运行时地址才确定 |

---

## 覆盖与交换
| 技术 | 原理 | 说明 |
|------|------|------|
| **覆盖** | 同一内存区存放不同时使用的模块 | 程序员手动划分 |
| **交换** | 进程在内存与外存间换入换出 | OS 自动管理 |

---

## 考试辨析
- 覆盖是**同一进程内**不同模块共享内存
- 交换是**不同进程间**在主存和辅存间移动`,
  },

  // --- 3.2 连续分配 ---
  {
    id: 'os-3-2-1', chapterId: 'os-3', title: '连续分配管理方式',
    keyConcepts: ['单一连续', '固定分区', '动态分区', '首次适应', '最佳适应', '外部碎片', '紧凑'], relatedPoints: ['os-3-1-1'],
    content: `## 连续分配方式演进

### 1. 单一连续分配
- 内存只分系统区和用户区
- 只能运行单道程序

### 2. 固定分区分配
- 预先将内存划分成固定大小分区
- 有**内部碎片**

### 3. 动态分区分配
- 按进程需要动态划分
- 有**外部碎片**（需紧凑解决）

---

## 动态分区的分配算法

| 算法 | 策略 | 特点 |
|------|------|------|
| **首次适应 (FF)** | 找第一个够大的空闲区 | 最快 |
| **循环首次适应 (NF)** | 从上次位置开始找 | 更均匀 |
| **最佳适应 (BF)** | 找最接近的大小区 | 产生最多碎片 |
| **最差适应 (WF)** | 找最大的空闲区 | 剩余区仍较大 |

---

## 考试辨析
- 内部碎片：分区内未用的空间（固定分区）
- 外部碎片：分区外太小的空闲区（动态分区）
- 紧凑技术：移动进程消除外部碎片——需要**动态重定位**硬件支持`,
  },

  // --- 3.3 非连续分配 ---
  {
    id: 'os-3-3-1', chapterId: 'os-3', title: '非连续分配管理方式',
    keyConcepts: ['分页', '分段', '段页式', '页表', '段表', '快表', 'TLB'], relatedPoints: ['os-3-2-1', 'co-3-4-1'],
    content: `## 基本分页存储管理

> 内存划分成固定大小的**页框**，进程划分成等大小的**页**。

逻辑地址 = 页号 | 页内偏移
物理地址 = 页框号 × 页大小 + 页内偏移

**页表**：页号 → 页框号映射

---

## 基本分段存储管理

> 按程序的逻辑结构分段（代码段、数据段、栈...），段长可变。

逻辑地址 = 段号 | 段内偏移

**段表**：段号 → 段基址 + 段长 + 保护信息

---

## 分页 vs 分段
| | 分页 | 分段 |
|------|------|------|
| 划分依据 | 物理（固定大小） | 逻辑（变长） |
| 程序员感知 | 不感知 | **感知** |
| 目的 | 提高内存利用率 | 满足用户编程需求 |
| 共享/保护 | 困难（一页可能含多逻辑段） | 容易（一段 = 一逻辑单元） |

---

## 段页式
- 先分段，每段再分页——结合两者优点
- 段号 → 段表项 → 页表 → 物理地址

---

## 考试辨析
- 分页是为了**非连续分配**，解决外部碎片
- 分段是为了满足**用户编程**需求（逻辑分块）
- 分页只有内部碎片，分段有外部碎片
- 页大小是系统设定（4KB常见），段大小是程序员决定`,
  },

  // --- 3.4 虚拟内存 ---
  {
    id: 'os-3-4-1', chapterId: 'os-3', title: '虚拟内存管理',
    keyConcepts: ['虚拟内存', '请求分页', '缺页', '页面置换', 'FIFO', 'LRU', 'Clock', 'Belady异常', '抖动'], relatedPoints: ['os-3-3-1', 'co-3-4-1'],
    content: `## 虚拟内存的核心思想
> 只将程序**部分装入内存**即可运行——需要时再从磁盘调入。
**理论基础**：局部性原理（时间局部性 + 空间局部性）

---
## 请求分页系统
> 在基本分页基础上，增加**缺页中断**和**页面置换**功能。

### 页表项新增字段
| 字段 | 含义 |
|------|------|
| 状态位 (P) | 该页是否在内存中 |
| 访问位 (A) | 该页是否被访问过 |
| 修改位 (M/Dirty) | 该页是否被修改过 |
| 外存地址 | 该页在磁盘上的位置 |

### 缺页中断处理流程
1. 查页表，发现 P=0 → 缺页中断
2. OS 查找磁盘上的页 → 调入内存
3. 若内存满 → 选一页置换出去（若被修改则写回磁盘）
4. 更新页表，P=1
5. 重新执行缺页的那条指令

---
## 页面置换算法（高频考点）

| 算法 | 策略 | 特点 |
|------|------|------|
| **OPT** | 淘汰未来最远才用到的页 | 理论上最优，但无法实现（需预知未来） |
| **FIFO** | 淘汰最早进入内存的页 | 简单，但可能有 **Belady 异常** |
| **LRU** | 淘汰最久未被使用的页 | 效果好，利用时间局部性，实现复杂 |
| **Clock (NRU)** | 循环扫描，淘汰访问位=0 的页 | 近似 LRU，实际系统常用 |
| **改进Clock** | 同时考虑访问位和修改位 | 更精细，优先淘汰未修改的页 |

---
## Belady 异常
> **FIFO 算法中**，给进程分配的物理页框数增加，缺页次数反而增加的现象。
只有 FIFO 算法会出现 Belady 异常（LRU 和 OPT 不会）。

---
## 缺页率的影响因素
1. **页面大小**：页面越大，缺页率越低（但内部碎片增加）
2. **分配页框数**：越多，缺页率越低（但每个进程占内存增加）
3. **置换算法**：LRU > Clock > FIFO（缺页率从低到高）
4. **程序局部性**：局部性好的程序缺页率低

---
## 抖动 (Thrashing)
> 进程频繁换入换出，大部分时间用于页面置换而非执行——CPU 利用率急剧下降。
- **原因**：系统中活跃进程太多，每个进程分到的物理页框太少
- **解决**：降低多道程序度（减少并发进程数），增加物理内存

---
## 考试辨析
- **虚拟内存大小由逻辑地址空间决定**（与物理内存大小无关！）
- 缺页中断是**可重入中断**——中断处理完成后需重新执行缺页指令
- FIFO 是唯一有 Belady 异常的置换算法
- 页面缓冲算法 (Page Buffering) 可以进一步提高置换效率`,
  },

  // ===========================================================
  // 第4章 文件管理
  // ===========================================================

  // --- 4.1 文件系统基础 ---
  {
    id: 'os-4-1-1', chapterId: 'os-4', title: '文件系统基础',
    keyConcepts: ['文件', '目录', 'FCB', 'inode', '文件控制块', '目录结构'], relatedPoints: [],
    content: `## 文件的逻辑结构

| 类型 | 特点 |
|------|------|
| 无结构文件（流式） | 字节序列，OS 不关心内部结构 |
| 有结构文件（记录式） | 等长/变长记录 |

---

## 文件的物理结构

| 方式 | 原理 | 优点 | 缺点 |
|------|------|------|------|
| **连续分配** | 连续块 | 顺序存取快 | 有外部碎片 |
| **链接分配** | 块指针串联 | 无碎片 | 随机存取慢 |
| **索引分配** | 索引块集中存放 | 随机存取快 | 索引块占空间 |

---

## 文件控制块 (FCB)
> FCB 是文件存在的标志——包含文件名、大小、位置、权限、时间等。

**inode**（索引结点）：Unix 中 FCB 的核心部分。

---

## 目录结构
| 结构 | 说明 |
|------|------|
| 单级目录 | 所有文件在一起 |
| 两级目录 | 用户目录 + 文件 |
| 树形目录 | 层次结构（现代文件系统标准） |
| 无环图目录 | 允许共享（硬链接/软链接） |

---

## 考试辨析
- 打开文件时，FCB 被复制到内存（活跃文件表）
- 文件名 → FCB → 物理位置，这是文件查找的基本路径`,
  },

  // --- 4.2 文件系统实现 ---
  {
    id: 'os-4-2-1', chapterId: 'os-4', title: '文件系统实现',
    keyConcepts: ['文件分配表', 'FAT', '位示图', '空闲链表', '成组链接'], relatedPoints: ['os-4-1-1'],
    content: `## 空闲空间管理

| 方法 | 原理 | 优缺点 |
|------|------|--------|
| **空闲表** | 记录每个空闲区 | 简单，适合连续分配 |
| **空闲链表** | 空闲块链在一起 | 分配回收简单 |
| **位示图** | 每位对应一块 | 空间小，易找连续区 |
| **成组链接** | 分组记录（Unix采用） | 适合大型文件系统 |

---

## FAT（文件分配表）

> FAT 本质是**显式链接分配**——将所有文件的链指针统一存在 FAT 表中。

- FAT 表项 = 下一块号或结束标记
- FAT 放在磁盘固定位置
- 支持随机存取（先读 FAT 找到块链）

---

## 考试辨析
- 位示图空间开销小，适合查找连续空闲区
- FAT 表的大小 = 块数 × 每项位数
- 磁盘格式化时建立 FAT`,
  },

  // --- 4.3 磁盘组织 ---
  {
    id: 'os-4-3-1', chapterId: 'os-4', title: '磁盘组织与管理',
    keyConcepts: ['磁盘', '磁道', '扇区', '寻道', '旋转延迟', 'SCAN', 'C-SCAN', 'SSTF', '磁盘调度'], relatedPoints: [],
    content: `## 磁盘访问时间

$$T_{access} = T_{seek} + T_{rotation} + T_{transfer}$$

- **寻道时间 $T_{seek}$**：磁头移动到目标磁道——最耗时
- **旋转延迟 $T_{rotation}$**：扇区转到磁头下——平均值 = 半圈时间
- **传输时间 $T_{transfer}$**：数据读写

---

## 磁盘调度算法

| 算法 | 策略 | 特点 |
|------|------|------|
| **FCFS** | 按请求顺序 | 公平但可能慢 |
| **SSTF** | 最近磁道先服务 | 快但可能饥饿 |
| **SCAN（电梯）** | 单向扫描到底再折返 | 实际常用 |
| **C-SCAN** | 单向到底，快速归位 | 更公平 |
| **LOOK / C-LOOK** | 到最远请求即折返 | 优化版 |

---

## 考试辨析
- 磁盘调度主要减少的是**寻道时间**（最主要的时间开销）
- SCAN = 电梯算法——走到头才折返
- C-SCAN 只有单向服务，归位时不服务`,
  },

  // ===========================================================
  // 第5章 I/O管理
  // ===========================================================

  // --- 5.1 I/O概述 ---
  {
    id: 'os-5-1-1', chapterId: 'os-5', title: 'I/O管理概述',
    keyConcepts: ['I/O', '设备独立性', '逻辑设备', '物理设备', 'SPOOLing'], relatedPoints: ['os-5-2-1'],
    content: `## I/O 设备分类

| 按传输速率 | 按信息交换单位 | 按共享属性 |
|-----------|-------------|-----------|
| 低速：键盘鼠标 | **块设备**：磁盘 | 独占设备 |
| 中速：打印机 | **字符设备**：键盘 | 共享设备 |
| 高速：磁盘 | | 虚拟设备 |

---

## 设备独立性

> 用户程序使用**逻辑设备名**——OS 将其映射到**物理设备**。

好处：程序与具体物理设备解耦，可重定向。

---

## SPOOLing 技术（假脱机）

> 用磁盘（高速共享设备）模拟独占设备。

- 输入/输出井 + 输入/输出进程
- 可将独占设备（如打印机）变成**共享设备**

---

## 考试辨析
- SPOOLing 使"虚拟设备"成为可能
- 设备独立性通过 LUT（逻辑设备表）实现`,
  },

  // --- 5.2 I/O核心子系统 ---
  {
    id: 'os-5-2-1', chapterId: 'os-5', title: 'I/O核心子系统',
    keyConcepts: ['I/O调度', '缓冲区', '单缓冲', '双缓冲', '循环缓冲', '设备驱动'], relatedPoints: ['os-5-1-1'],
    content: `## I/O 软件层次

\`\`\`
用户层 I/O 软件
    ↓
设备独立性软件
    ↓
设备驱动程序
    ↓
中断处理程序
    ↓
硬件
\`\`\`

---

## 缓冲区

### 单缓冲
- 并行性：CPU 处理前一块 + I/O 读入下一块 → 时间缩短

### 双缓冲
- 进一步并行：一边读入一边写出
- 可实现连续数据流

### 循环缓冲
- 多个缓冲区循环使用——适合高速持续 I/O

---

## 考试辨析
- 缓冲的目的是**缓和 CPU 与 I/O 设备速度不匹配**
- 双缓冲可消除生产者-消费者间的等待
- 缓冲在内存中分配——由 OS 管理`,
  },
];
