import type { KnowledgePoint } from '@/types/knowledge';

export const coKnowledgePoints: KnowledgePoint[] = [

  // ===========================================================
  // 第1章 计算机系统概述
  // ===========================================================

  // --- 1.1 计算机发展历程 ---
  {
    id: 'co-1-1-1', chapterId: 'co-1', title: '计算机发展历程与冯·诺依曼结构',
    keyConcepts: ['冯·诺依曼', '存储程序', '五大部件', '二进制', '运算器中心'], relatedPoints: ['co-1-2-1'],
    content: `## 冯·诺依曼计算机的基本思想

> **"存储程序"**——将程序像数据一样存入存储器中，计算机自动逐条取出指令执行。

### 核心要点
1. 计算机由**五大部件**组成：运算器、控制器、存储器、输入设备、输出设备
2. 指令和数据以**二进制**形式存储
3. 以**运算器为中心**（现代计算机改为**以存储器为中心**）
4. 指令按顺序执行（程序计数器 PC 控制）

---

## 五大部件功能
| 部件 | 功能 |
|------|------|
| **运算器** | 算术运算 + 逻辑运算 |
| **控制器** | 控制指令执行顺序，产生控制信号 |
| **存储器** | 存储程序和数据 |
| **输入设备** | 将信息输入计算机 |
| **输出设备** | 将结果输出 |

---

## 考试辨析
- 冯·诺依曼机的核心是**存储程序**
- "以运算器为中心"是冯氏结构的标志——现代结构"以存储器为中心"
- 指令和数据同等存于存储器中——**从存储内容上无法区分**（只有执行时才区分）`,
  },

  // --- 1.2 计算机系统层次结构 ---
  {
    id: 'co-1-2-1', chapterId: 'co-1', title: '计算机系统的层次结构',
    keyConcepts: ['层次结构', '裸机', '虚拟机', '翻译程序', '解释程序', '编译', '汇编'], relatedPoints: [],
    content: `## 计算机系统的多级层次

\`\`\`
用户

高级语言 (C/Java/Python)  → 编译程序

汇编语言               → 汇编程序

操作系统               → 操作系统

机器语言（指令系统）

微程序（微指令）

硬件逻辑（电路）
\`\`\`

---

## 核心概念
| 概念 | 说明 |
|------|------|
| **翻译程序** | 将高级语言源代码转换为机器语言 |
| **编译程序** | 一次性翻译整个源程序 |
| **解释程序** | 逐条翻译并执行（不生成目标代码） |
| **汇编程序** | 将汇编语言翻译为机器语言 |

---

## 机器/汇编/高级语言对比
| 语言 | 特点 | 可读性 | 可移植性 |
|------|------|--------|----------|
| 机器语言 | 二进制代码 | 差 | 差 |
| 汇编语言 | 助记符 | 较好 | 差 |
| 高级语言 | 面向问题 | 好 | 好 |

---

## 考试辨析
- 编译与解释的区别：编译生成目标代码，解释不生成
- 汇编语言与机器指令一一对应（汇编 = 机器语言的助记符形式）`,
  },

  // --- 1.3 计算机性能指标 ---
  {
    id: 'co-1-3-1', chapterId: 'co-1', title: '计算机的性能指标',
    keyConcepts: ['主频', 'CPI', 'MIPS', 'MFLOPS', '机器字长', '数据通路带宽', '响应时间'], relatedPoints: ['co-5-5-1'],
    content: `## 核心性能指标

| 指标 | 含义 | 公式 |
|------|------|------|
| **机器字长** | CPU 一次处理的二进制位数 | — |
| **主频 (f)** | CPU 每秒的时钟周期数 | 单位 Hz |
| **时钟周期 (T)** | 一个时钟周期的时间 | $T = 1/f$ |
| **CPI** | 每条指令所需的平均时钟周期数 | — |
| **CPU 执行时间** | 执行某程序的总时间 | $T_{CPU} = N \\times CPI \\times T$ |
| **MIPS** | 每秒百万条指令 | $MIPS = f / (CPI \\times 10^6)$ |
| **MFLOPS** | 每秒百万次浮点运算 | — |

---

## CPU 执行时间公式（核心）
$$T_{CPU} = N \\times CPI \\times T = \\frac{N \\times CPI}{f}$$

- $N$ = 指令条数
- $CPI$ = 每条指令平均时钟周期数
- $T$ = 时钟周期

---

## 考试辨析
- $T_{CPU}$ 越小越好，MIPS/MFLOPS 越大越好
- 主频 ≠ 性能——受 CPI 影响很大
- 提高主频 $f$ 和降低 $CPI$ 都提高性能`,
  },

  // ===========================================================
  // 第2章 数据的表示和运算
  // ===========================================================

  // --- 2.1 数制与编码 ---
  {
    id: 'co-2-1-1', chapterId: 'co-2', title: '进位计数制与转换',
    keyConcepts: ['二进制', '八进制', '十六进制', '十进制', '进制转换'], relatedPoints: ['co-2-1-2'],
    content: `## 四种常用进制

| 进制 | 基数 | 数码 | 后缀 |
|------|------|------|------|
| 二进制 | 2 | 0,1 | B |
| 八进制 | 8 | 0~7 | O |
| 十进制 | 10 | 0~9 | D |
| 十六进制 | 16 | 0~9,A~F | H |

---

## 转换方法
| 转换方向 | 方法 |
|----------|------|
| 任意进制 → 十进制 | **按权展开求和** |
| 十进制 → 任意进制 | 整数**除基取余**，小数**乘基取整** |
| 二进制 ↔ 八进制 | 3 位一组 |
| 二进制 ↔ 十六进制 | 4 位一组 |

---

## 考试辨析
- BCD 码：用 4 位二进制表示 1 位十进制——有 8421 码(有权)、余 3 码(无权)等
- 进制转换是数据表示和运算的基础，不会单独出大题`,
  },
  {
    id: 'co-2-1-2', chapterId: 'co-2', title: '定点数的编码表示',
    keyConcepts: ['原码', '反码', '补码', '移码', '模运算', '符号位', '数值范围'], relatedPoints: ['co-2-2-1'],
    content: `## 四种编码对比（n 位字长，含符号位）

| 编码 | 正数 | 负数 | 0 的表示 | 数值范围 |
|------|------|------|----------|----------|
| **原码** | 符号位 0 + 绝对值 | 符号位 1 + 绝对值 | +0 / -0 | $-(2^{n-1}-1) \\sim +(2^{n-1}-1)$ |
| **反码** | 同原码 | 原码**除符号位外取反** | +0 / -0 | $-(2^{n-1}-1) \\sim +(2^{n-1}-1)$ |
| **补码** | 同原码 | 反码末位 **+1** | 只有 +0 | **$-2^{n-1} \\sim +(2^{n-1}-1)$** |
| **移码** | 补码符号位取反 | 补码符号位取反 | 只有 +0 | $-2^{n-1} \\sim +(2^{n-1}-1)$ |

---

## 补码（最重要）

> **计算机中整数统一使用补码表示**——原因：0 唯一表示，减法变加法。

补码求法口诀：**"按位取反末尾加一"**

$$[X]_{补} = \\begin{cases} X & X \\ge 0 \\\\ 2^n + X & X < 0 \\end{cases}$$

---

## 考试辨析
- **补码的数值范围不对称**：负数范围比正数多 1（如 8 位：$-128 \\sim +127$）
- 移码与补码符号位相反——移码用于**浮点数阶码**
- 原码加减需判断符号位和绝对值大小——实现复杂`,
  },

  // --- 2.2 定点数的表示与运算 ---
  {
    id: 'co-2-2-1', chapterId: 'co-2', title: '定点数的加减运算与溢出判断',
    keyConcepts: ['加法器', '溢出', 'OF', 'SF', 'CF', 'ZF', '进位', '双符号位'], relatedPoints: ['co-2-1-2', 'co-2-4-1'],
    content: `## 补码加减法

$$[A+B]_{补} = [A]_{补} + [B]_{补}$$
$$[A-B]_{补} = [A]_{补} + [-B]_{补}$$

> 补码可以将减法统一为加法——这是补码最大的价值。

---

## 溢出判断（三种方法）

### 方法一：单符号位法
若两个正数相加得负数，或两个负数相加得正数 → 溢出。

### 方法二：双符号位法（变形补码）
- 正数：00.xxx，负数：11.xxx
- **结果双符号位异号（01 或 10）→ 溢出**
- 01 = 正溢出（上溢），10 = 负溢出（下溢）

### 方法三：进位判断法
最高数值位的进位 $C_{n-1}$ 与符号位的进位 $C_n$ **异或 = 1** → 溢出。
$$OF = C_n \\oplus C_{n-1}$$

---

## 标志位
| 标志 | 含义 | 判断条件 |
|------|------|----------|
| **ZF** | 零标志 | 结果为 0 |
| **SF** | 符号标志 | 结果的最高位（符号位） |
| **CF** | 进位/借位 | 无符号运算的进位/借位 |
| **OF** | 溢出标志 | **有符号运算**溢出 |

---

## 考试辨析
- OF 只对**有符号数**有意义，CF 只对**无符号数**有意义
- 双符号位法是最直观的溢出判断——408 常考`,
  },
  {
    id: 'co-2-2-2', chapterId: 'co-2', title: '定点数的乘除运算',
    keyConcepts: ['乘法', 'Booth算法', '原码乘法', '补码乘法', '除法', '原码除法'], relatedPoints: ['co-2-2-1'],
    content: `## 乘法运算

### Booth 算法（补码一位乘法）
- 在乘数末位补 0
- 根据 $y_i - y_{i+1}$ 决定：1→+X，-1→+(-X)，0→+0
- 每次算术右移一位
- **n 次加 + n 次移位**

---

## 除法运算

### 原码除法（不恢复余数法/加减交替法）
- 够减商 1，不够减商 0 并加除数恢复
- 可改进为**加减交替法**：不够减时不恢复，下一步加除数

---

## 考试辨析
- 408 对乘法考察以理解 Booth 算法流程为主
- 除法考察较少，了解基本过程即可
`,
  },

  // --- 2.3 浮点数的表示与运算 ---
  {
    id: 'co-2-3-1', chapterId: 'co-2', title: 'IEEE 754 浮点数标准',
    keyConcepts: ['IEEE754', '单精度', '双精度', '阶码', '尾数', '规格化', '隐藏位', '移码'], relatedPoints: ['co-2-1-2'],
    content: `## IEEE 754 浮点数格式

> $N = (-1)^S \\times 1.M \\times 2^{E-bias}$

| 精度 | 总位数 | 符号S | 阶码E | 尾数M | bias |
|------|--------|--------|--------|--------|------|
| **单精度** | 32 位 | 1 | 8 | 23 | 127 |
| **双精度** | 64 位 | 1 | 11 | 52 | 1023 |

---

## 阶码与尾数
- **阶码用移码表示**（非标准移码，偏置常数为 127/1023）
- **尾数用原码**表示，有**隐藏位**（规格化后整数部分隐含为 1，不存）

---

## 特殊值
| 阶码 | 尾数 | 值 |
|------|------|-----|
| 全 0 | 全 0 | $\\pm 0$ |
| 全 0 | 非 0 | 非规格化数 |
| 全 1 | 全 0 | $\\pm \\infty$ |
| 全 1 | 非 0 | **NaN**（非数） |

---

## 考试辨析
- 阶码全1 + 尾数非0 = NaN——选择题常见选项
- 阶码全0 → 隐藏位变为0（非规格化数）
- 浮点数能表示的范围比同位数定点数大得多，但位数相同不代表精度更高`,
  },
  {
    id: 'co-2-3-2', chapterId: 'co-2', title: '浮点数的运算',
    keyConcepts: ['对阶', '规格化', '舍入', '溢出判断', '保护位', '大阶对小阶'], relatedPoints: ['co-2-3-1'],
    content: `## 浮点数加减的五个步骤

1. **对阶**：小阶对大阶（尾数右移，阶码增加）
2. **尾数求和**：对阶后尾数做加减
3. **规格化**：
   - 左规：尾数左移，阶码减 1（直到尾数最高位 = 1）
   - 右规：尾数右移，阶码加 1（溢出时）
4. **舍入**："0 舍 1 入"法或"恒置 1"法
5. **溢出判断**：阶码溢出 → 结果溢出

---

## 保护位
> 为提高精度，运算过程中在低位多保留几位（保护位 GRS）：Guard + Round + Sticky。

---

## 考试辨析
- **大阶对小阶时尾数右移，低位被丢弃**——这是浮点运算的主要误差来源
- 对阶操作仅操作尾数，不改变阶码符号
- 408 大题常考：给定两个浮点数，手工模拟加减全过程`,
  },

  // --- 2.4 算术逻辑单元 ALU ---
  {
    id: 'co-2-4-1', chapterId: 'co-2', title: '算术逻辑单元 (ALU)',
    keyConcepts: ['ALU', '全加器', '串行进位', '并行进位', '先行进位', 'SN74181'], relatedPoints: ['co-2-2-1', 'co-5-1-1'],
    content: `## ALU 的功能

> ALU 是运算器的核心部件，能完成算术运算和逻辑运算。

---

## 进位方式
| 方式 | 特点 | 速度 |
|------|------|------|
| **串行进位** | 每级进位依赖前一级 | 慢，$O(n)$ |
| **并行进位** | 各级同时生成进位 | 快，但电路复杂 |
| **先行进位 (CLA)** | 分组并行 | 折中 |

---

## 标志位产生
| 标志 | 全称 | 说明 |
|------|------|------|
| ZF | Zero Flag | 结果为 0 |
| SF | Sign Flag | 符号位 |
| CF | Carry Flag | 进位/借位 |
| OF | Overflow Flag | 溢出 |

---

## 考试辨析
- 加法器是 ALU 的核心——408 主要考加法器及进位生成
- 串行进位的关键路径 $O(n)$，并行进位 $O(\\log n)$——了解区别即可`,
  },

  // ===========================================================
  // 第3章 存储系统
  // ===========================================================

  // --- 3.1 存储器概述 ---
  {
    id: 'co-3-1-1', chapterId: 'co-3', title: '存储器分类与层次结构',
    keyConcepts: ['层次结构', 'Cache', '主存', '辅存', '局部性', '时间局部性', '空间局部性'], relatedPoints: ['co-3-3-1', 'ds-1-1-2'],
    content: `## 存储器的层次结构

\`\`\`
寄存器（CPU内部）  ← 最快/最小/最贵
    ↓
Cache (SRAM)
    ↓
主存 (DRAM)
    ↓
磁盘/SSD（辅存） ← 最慢/最大/最便宜
\`\`\`

---

## 局部性原理
| 类型 | 含义 | 举例 |
|------|------|------|
| **时间局部性** | 刚访问过的数据短期内可能再次访问 | 循环变量 |
| **空间局部性** | 刚访问位置附近的数据可能被访问 | 数组遍历 |

---

## 层次结构解决的核心矛盾
$$\\text{容量} \\uparrow \\quad \\text{速度} \\uparrow \\quad \\text{价格} \\downarrow$$
> 三者不可兼得——用层次结构追求"看起来很大、用起来很快、成本可接受"。

---

## 考试辨析
- Cache-主存层解决**速度**问题（硬件自动完成）
- 主存-辅存层解决**容量**问题（OS + 硬件配合）`,
  },

  // --- 3.2 主存储器 ---
  {
    id: 'co-3-2-1', chapterId: 'co-3', title: 'SRAM与DRAM',
    keyConcepts: ['SRAM', 'DRAM', '刷新', '电容', '触发器', '双稳态'], relatedPoints: ['co-3-1-1'],
    content: `## SRAM vs DRAM

| 特性 | SRAM | DRAM |
|------|------|------|
| **存储原理** | 双稳态触发器 | 电容充放电 |
| **速度** | 快 | 慢 |
| **集成度** | 低 | 高 |
| **功耗** | 大 | 小 |
| **是否需要刷新** | ❌ 不需要 | ✅ **需要定期刷新** |
| **价格** | 贵 | 便宜 |
| **用途** | Cache | 主存 |

---

## DRAM 刷新

> DRAM 利用电容存储信息——电容会漏电，需要**定期刷新**（即读出后重新写入）。

| 刷新方式 | 说明 |
|----------|------|
| **集中刷新** | 一段连续时间内完成全部刷新——有"死时间" |
| **分散刷新** | 每个存取周期后刷新一行——没有死时间 |
| **异步刷新** | 刷新周期均匀分散——折中方案 |

---

## 考试辨析
- **需要刷新的是 DRAM**（不是 SRAM）
- SRAM 不需要刷新是因为使用触发器（双稳态）存储
- 刷新一行 = 读出一行 + 重新写入——需要额外电路`,
  },
  {
    id: 'co-3-2-2', chapterId: 'co-3', title: 'ROM、Flash与SSD',
    keyConcepts: ['ROM', 'Flash', 'SSD', '非易失', 'NOR Flash', 'NAND Flash'], relatedPoints: ['co-3-2-1'],
    content: `## 只读存储器 (ROM)

| 类型 | 特点 |
|------|------|
| MROM | 出厂掩膜写入，不可改 |
| PROM | 用户可写一次 |
| EPROM | 可多次擦除（紫外线） |
| EEPROM | 电擦除（可在线改写） |
| Flash | 电擦除，速度快，成本低 |

---

## NOR Flash vs NAND Flash

| | NOR Flash | NAND Flash |
|------|----------|-----------|
| 读取 | 随机读取快 | 按页读取 |
| 写入/擦除 | 慢 | 快 |
| 容量 | 小 | 大 |
| 用途 | 存储 BIOS/固件 | SSD、U 盘、存储卡 |

---

## SSD (固态硬盘)
- 核心：NAND Flash + 控制器
- 优点：无机械部件，随机访问快，低功耗
- 缺点：写入次数有限（擦写寿命），需要磨损均衡

---

## 考试辨析
- ROM ≠ 只读——现代 EEPROM/Flash 可擦写
- Flash 写入前需先擦除——以"块"为单位擦除`,
  },

  // --- 3.3 高速缓冲存储器 Cache ---
  {
    id: 'co-3-3-1', chapterId: 'co-3', title: 'Cache的基本原理',
    keyConcepts: ['Cache', '命中', '缺失', '命中率', '块', '主存块', 'Cache行'], relatedPoints: ['co-3-1-1', 'co-3-3-2'],
    content: `## Cache 的工作原理

> 利用**局部性原理**，将主存中频繁访问的数据块复制到 Cache 中。

CPU 先访问 Cache → **命中**则直接取数据 → **缺失**则从主存取数据并替换到 Cache。

---

## 核心概念
| 术语 | 含义 |
|------|------|
| **命中 (Hit)** | CPU 要访问的数据在 Cache 中 |
| **缺失 (Miss)** | CPU 要访问的数据不在 Cache 中 |
| **命中率 $h$** | Cache 命中的概率 |
| **缺失率** | $= 1 - h$ |

---

## 性能计算（大题常考）

$$T_{avg} = h \\times T_{cache} + (1-h) \\times T_{main}$$

**加速比** $= T_{main} / T_{avg}$

---

## 考试辨析
- Cache 对程序员透明（硬件自动管理）
- 命中时间的定义：从 Cache 读取数据 + 判定的时间
- 缺失损失 = 从主存取回数据块的时间`,
  },
  {
    id: 'co-3-3-2', chapterId: 'co-3', title: 'Cache的地址映射方式',
    keyConcepts: ['直接映射', '全相联映射', '组相联映射', 'Cache地址', '主存地址', '标记', '组号'], relatedPoints: ['co-3-3-1', 'co-3-3-3'],
    content: `## 三种映射方式

### 1. 直接映射
$$\\text{Cache 行号} = \\text{主存块号 } \\bmod \\text{ Cache 行数}$$
- 每块主存只能映射到 Cache 中**唯一**的行
- 优点：实现简单；缺点：**冲突缺失多**

### 2. 全相联映射
- 每块主存可映射到 Cache 中**任意**行
- 优点：最灵活，冲突少；缺点：比较电路复杂，成本高

### 3. 组相联映射
$$\\text{Cache 组号} = \\text{主存块号 } \\bmod \\text{ 组数}$$
- 组间直接映射，组内全相联
- 最常用（现代 CPU 都采用）

---

## 主存地址拆分（必考）

直接映射：\`| 标记 | Cache行号 | 块内地址 |\`
组相联映射：\`| 标记 | 组号 | 块内地址 |\`
全相联映射：\`| 标记 | 块内地址 |\`

---

## 三种映射对比
| 映射方式 | 灵活性 | 冲突 | 电路复杂度 |
|----------|--------|------|-----------|
| 直接映射 | 低 | 多 | 简单 |
| 组相联 | 中 | 中 | 中等 |
| 全相联 | 高 | 少 | 复杂 |

---

## 考试辨析
- Cache地址映射是 **408 大题必考内容**
- 关键在于：正确拆分主存地址的**标记/组号/块内地址**
- 块大小决定块内地址位数，组数决定组号位数，其余是标记位`,
  },
  {
    id: 'co-3-3-3', chapterId: 'co-3', title: 'Cache的替换算法与写策略',
    keyConcepts: ['LRU', 'FIFO', '随机替换', '写直达', '写回', '写分配', '非写分配'], relatedPoints: ['co-3-3-2'],
    content: `## 替换算法

| 算法 | 策略 |
|------|------|
| **随机替换** | 随机选择一块替换 |
| **FIFO** | 替换最先进入的块 |
| **LRU** | 替换最久未使用的块（最优） |
| **LFU** | 替换使用频率最低的块 |

---

## 写策略

### 写命中时：
| 策略 | 说明 | 优点 | 缺点 |
|------|------|------|------|
| **写直达** | 同时写入 Cache 和主存 | 简单，保证一致性 | 写速度慢 |
| **写回** | 只写 Cache，被替换时再写回主存 | 写速度快 | 一致性复杂 |

### 写缺失时：
| 策略 | 说明 |
|------|------|
| **写分配** | 先调入主存块到 Cache，再写入 Cache（通常与**写回**搭配） |
| **非写分配** | 直接写主存，不调入 Cache（通常与**写直达**搭配） |

---

## 考试辨析
- **LRU 是最常用的替换算法**——利用局部性
- 写回 + 写分配是常用组合（对同一地址的后续写操作命中率高）
- 多核 Cache 一致性（MESI 协议）了解即可`,
  },

  // --- 3.4 虚拟存储器 ---
  {
    id: 'co-3-4-1', chapterId: 'co-3', title: '虚拟存储器',
    keyConcepts: ['虚拟地址', '物理地址', '页表', 'TLB', '快表', '页式存储', '缺页'], relatedPoints: ['co-3-1-1', 'co-3-3-1', 'os-3-4-1'],
    content: `## 虚拟存储器的目的
> 将**主存**和**辅存**统一编址，向程序员提供一个比主存大得多的地址空间。

---

## 核心概念
| 概念 | 含义 |
|------|------|
| **虚拟地址（逻辑地址）** | 程序员看到的地址 |
| **物理地址** | 实际主存的地址 |
| **页表** | 虚拟页号 → 物理页号映射表 |
| **页表项 (PTE)** | 含物理页号 + 有效位/保护位等 |
| **缺页 (Page Fault)** | 访问的页不在主存中 |

---

## TLB (快表)

> TLB = Translation Lookaside Buffer，页表的高速缓存。

| TLB | 说明 |
|-----|------|
| 存什么 | 近期使用的页表项 |
| 为什么要 | 减少访问页表的次数（一次访存 → 一次查 TLB） |
| 特点 | 容量小、速度快、全相联 |

---

## 虚拟地址 → 物理地址流程
1. CPU 产生虚拟地址
2. 查 TLB → 命中则直接得到物理地址
3. TLB 缺失 → 查页表 → 更新 TLB
4. 页表缺失 → 缺页中断 → OS 调入页 → 更新页表

---

## 考试辨析
- 虚拟存储器由 OS + 硬件共同管理（主存-辅存层）
- 虚拟地址空间大小取决于**地址线位数**，不取决于主存大小
- TLB = 页表的 Cache——用 SRAM 实现`,
  },

  // ===========================================================
  // 第4章 指令系统
  // ===========================================================

  // --- 4.1 指令格式 ---
  {
    id: 'co-4-1-1', chapterId: 'co-4', title: '指令格式与操作码编码',
    keyConcepts: ['指令格式', '操作码', '地址码', '定长编码', '变长编码', '扩展操作码'], relatedPoints: ['co-5-1-1'],
    content: `## 指令的基本格式

一条指令由**操作码 (OP)** 和**地址码 (A)** 组成：

\`\`\`
| 操作码 OP | 地址码 A1 | A2 | A3 |
\`\`\`

| 字段 | 含义 |
|------|------|
| 操作码 | 指明指令做什么（加、减、跳转...） |
| 地址码 | 操作数的地址信息 |

---

## 按地址数分类
| 类型 | 格式 | 特点 |
|------|------|------|
| 三地址 | OP A1, A2, A3 | (A1) OP (A2) → A3 |
| 二地址 | OP A1, A2 | (A1) OP (A2) → A1 |
| 一地址 | OP A | (AC) OP (A) → AC |
| 零地址 | OP | 栈顶两个数操作（堆栈机） |

---

## 操作码编码方式
| 方式 | 说明 | 优缺点 |
|------|------|--------|
| **定长编码** | 所有指令操作码长度相同 | 译码简单，空间利用率低 |
| **变长编码** | 常用指令短编码，不常用指令长编码 | 提高利用率，译码复杂 |
| **扩展操作码** | 在多地址中用一部分操作码做扩展 | 平衡定长和变长 |

---

## 考试辨析
- 扩展操作码就是"用地址位做操作码"——减少地址字段，增加操作码位
- $n$ 位操作码最多表示 $2^n$ 条指令`,
  },

  // --- 4.2 指令的寻址方式 ---
  {
    id: 'co-4-2-1', chapterId: 'co-4', title: '寻址方式',
    keyConcepts: ['立即寻址', '直接寻址', '间接寻址', '寄存器寻址', '基址寻址', '变址寻址', '相对寻址'], relatedPoints: ['co-4-1-1'],
    content: `## 常见寻址方式

| 方式 | 操作数位置 | 有效地址 | 特点 |
|------|-----------|----------|------|
| **立即寻址** | 指令中直接给出 | — | 不用访存，但数值范围有限 |
| **直接寻址** | 主存 | EA = A | 一次访存 |
| **间接寻址** | 主存中的地址指向主存 | EA = (A) | 需多次访存 |
| **寄存器直接** | 寄存器 | EA = R | 快，不用访存 |
| **寄存器间接** | 寄存器中地址指向主存 | EA = (R) | 一次访存 |
| **基址寻址** | 主存 | EA = (BR) + A | 程序重定位 |
| **变址寻址** | 主存 | EA = (IX) + A | **数组访问** |
| **相对寻址** | 主存 | EA = (PC) + A | 转移指令 |

---

## 基址 vs 变址
| | 基址寻址 | 变址寻址 |
|------|----------|----------|
| 基准 | 基址寄存器 BR | 形式地址 A |
| 偏移 | 形式地址 A | 变址寄存器 IX |
| 用途 | 程序重定位 | **数组/循环** |
| 谁变 | BR 变（OS 设） | IX 变（程序员设） |

---

## 考试辨析
- 相对寻址中 EA = (PC) + 偏移量——**PC 已是下一条指令地址**（注意！）
- 变址寻址特别适合**数组元素访问**——A 是首地址，IX 是下标
- 基址面向 OS 的程序重定位，变址面向用户程序的数组访问`,
  },

  // --- 4.3 CISC 和 RISC ---
  {
    id: 'co-4-3-1', chapterId: 'co-4', title: 'CISC与RISC',
    keyConcepts: ['CISC', 'RISC', '复杂指令集', '精简指令集', '流水线'], relatedPoints: ['co-5-5-1', 'co-4-1-1'],
    content: `## CISC vs RISC

| 特征 | CISC | RISC |
|------|------|------|
| 指令数量 | **多** | **少** |
| 指令长度 | **变长** | **定长** |
| 寻址方式 | 多 | 少 |
| 指令执行时间 | 差异大 | 大多单周期 |
| 流水线 | 难以实现 | **容易实现** |
| 编译 | 简单 | 复杂（需优化编译器） |
| **典型** | x86 | ARM、MIPS、RISC-V |

---

## 考试辨析
- RISC 指令定长 → 译码简单 → **适合流水线**
- CISC 变长 → 指令功能强 → 程序代码短
- 现代 CPU：x86 外部 CISC、内部微码转 RISC（融合两者）
`,
  },

  // ===========================================================
  // 第5章 中央处理器
  // ===========================================================

  // --- 5.1 CPU 的功能和基本结构 ---
  {
    id: 'co-5-1-1', chapterId: 'co-5', title: 'CPU的功能与基本结构',
    keyConcepts: ['CPU', '运算器', '控制器', 'PC', 'IR', 'MAR', 'MDR', '寄存器'], relatedPoints: ['co-2-4-1', 'co-4-1-1'],
    content: `## CPU 的两大组成部分

\`\`\`
CPU
├── 运算器
│   ├── ALU（算术逻辑单元）
│   ├── ACC（累加器）
│   ├── 通用寄存器组
│   └── 标志寄存器 (FR/PSW)
│
└── 控制器
    ├── PC（程序计数器）
    ├── IR（指令寄存器）
    ├── ID（指令译码器）
    └── CU（控制单元）
\`\`\`

---

## 关键寄存器
| 寄存器 | 全称 | 功能 |
|--------|------|------|
| **PC** | 程序计数器 | 存放下一条指令地址（取指后自动递增） |
| **IR** | 指令寄存器 | 存放当前正在执行的指令 |
| **MAR** | 存储器地址寄存器 | 存放要访问的存储单元地址 |
| **MDR** | 存储器数据寄存器 | 存放与存储器交换的数据 |

---

## 考试辨析
- MAR 和 MDR 在 CPU 内部（不是主存中）
- PC 取指后自动递增——具体增量取决于指令长度`,
  },

  // --- 5.2 数据通路 ---
  {
    id: 'co-5-2-1', chapterId: 'co-5', title: '数据通路',
    keyConcepts: ['数据通路', '总线结构', '专用通路', '单总线', '双总线', '三总线'], relatedPoints: ['co-5-1-1'],
    content: `## 数据通路的功能

> 数据通路是 CPU 中各部件之间数据流动的路径。

---

## 数据通路的结构
| 结构 | 特点 | 优缺点 |
|------|------|--------|
| **单总线** | 所有部件共享一条总线 | 结构简单，但**瓶颈明显** |
| **双总线** | 主存总线和 I/O 总线分离 | 提高吞吐 |
| **三总线** | 再加一条执行总线 | 更快 |
| **专用通路** | 各部件间直接连线 | 最快，但线路复杂 |

---

## 考试辨析
- 数据通路与 CPU 结构有关——408 可能考通路图分析
- 总线冲突：同一时刻只能有一个部件向总线发送数据`,
  },

  // --- 5.3 控制器 ---
  {
    id: 'co-5-3-1', chapterId: 'co-5', title: '硬布线控制器与微程序控制器',
    keyConcepts: ['硬布线', '微程序', '控制存储器', '微命令', '微操作'], relatedPoints: ['co-5-1-1'],
    content: `## 硬布线控制器

> 用组合逻辑电路产生控制信号——门电路直接实现。

| 优点 | 缺点 |
|------|------|
| 速度快 | 设计复杂，难修改 |

---

## 微程序控制器

> 将每条机器指令分解为一组微指令，存入**控制存储器 (CM)**。

| 优点 | 缺点 |
|------|------|
| 规整、易修改 | 慢（多了读 CM 的一层） |

---

## 对比
| | 硬布线 | 微程序 |
|------|--------|--------|
| 实现 | 组合逻辑 | 微指令序列 |
| 速度 | **快** | 慢 |
| 灵活性 | 差 | **好** |
| 适用 | RISC | CISC |

---

## 考试辨析
- 微程序控制器多了一层——每条指令 → 一段微程序 → 若干微指令 → 若干微命令
- RISC 通常采用硬布线（追求速度），CISC 常采用微程序（处理复杂指令）`,
  },

  // --- 5.4 异常和中断 ---
  {
    id: 'co-5-4-1', chapterId: 'co-5', title: '异常与中断',
    keyConcepts: ['异常', '中断', '内中断', '外中断', '中断向量', '中断响应', '中断返回'], relatedPoints: ['co-7-3-1', 'os-1-3-1'],
    content: `## 异常 vs 中断

| | 异常 (Exception) | 中断 (Interrupt) |
|------|-------------------|------------------|
| 来源 | **CPU 内部** | **CPU 外部** |
| 原因 | 指令执行过程中的错误 | 外部设备或时钟 |
| 例子 | 除 0、缺页、非法指令 | 键盘输入、DMA 完成 |
| 同义词 | 内中断 | 外中断 |

---

## 中断处理流程
1. 中断请求 → CPU 响应
2. 关中断（防止嵌套）
3. **保存断点**（PC + PSW 入栈）
4. 中断服务程序入口 → PC
5. 保存现场（寄存器入栈）
6. 执行中断服务程序
7. 恢复现场
8. 开中断
9. **中断返回**（IRET指令）

---

## 中断向量

> 中断向量 = 中断服务程序的入口地址。

**中断向量表**：放在内存固定位置，用中断类型号查表得到向量。

---

## 考试辨析
- 异常是**同步**的（与指令执行相关），中断是**异步**的（随时可能发生）
- 408 常考中断响应周期的操作（关中断、保存断点、取中断向量）`,
  },

  // --- 5.5 指令流水线 ---
  {
    id: 'co-5-5-1', chapterId: 'co-5', title: '指令流水线',
    keyConcepts: ['流水线', 'IF', 'ID', 'EX', 'MEM', 'WB', '吞吐率', '加速比', '流水线冲突'], relatedPoints: ['co-5-1-1', 'co-4-3-1'],
    content: `## 指令流水线的思想

> 将指令执行过程分为多个阶段，多条指令的不同阶段可以**并行**执行。

经典五段流水线：**IF → ID → EX → MEM → WB**

---

## 性能指标

设指令数为 $n$，流水线段数为 $k$，每段时间为 $\\tau$：

| 指标 | 公式 |
|------|------|
| 第一条指令完成时间 | $k\\tau$ |
| 后续每条指令 | $+\\tau$（理想情况） |
| 总时间 | $(k + n - 1)\\tau$ |
| **吞吐率** | $\\frac{n}{(k+n-1)\\tau}$ |
| **加速比** | $\\frac{nk\\tau}{(k+n-1)\\tau} = \\frac{nk}{k+n-1}$ |

当 $n \\gg k$ 时，加速比 → **$k$**（理想情况）。

---

## 流水线冲突（三种）
| 冲突类型 | 原因 | 解决 |
|----------|------|------|
| **结构冲突** | 硬件资源不够用 | 增加硬件 |
| **数据冲突** | 后续指令依赖前一条的结果 | **转发**(Forwarding) + 停顿 |
| **控制冲突** | 分支指令导致 PC 不确定 | 分支预测 + 延迟槽 |

---

## 考试辨析
- 流水线并不缩短单条指令执行时间——缩短的是**吞吐率**
- 最长段的时间决定流水线周期
- 关键公式 $T = (k+n-1)\\tau$ 必须掌握`,
  },

  // ===========================================================
  // 第6章 总线
  // ===========================================================

  // --- 6.1 总线概述 ---
  {
    id: 'co-6-1-1', chapterId: 'co-6', title: '总线概述与分类',
    keyConcepts: ['总线', '数据总线', '地址总线', '控制总线', '系统总线', '局部总线', '通信总线'], relatedPoints: [],
    content: `## 总线的定义

> 总线是连接多个部件的信息传输线，各部件**共享**的传输介质。

---

## 按传输信息分类
| 总线 | 功能 | 方向 |
|------|------|------|
| **数据总线 (DB)** | 传输数据 | 双向 |
| **地址总线 (AB)** | 传输地址 | 单向（CPU→存储器） |
| **控制总线 (CB)** | 传输控制信号 | 各线方向不同 |

---

## 按连接对象分类
| 类型 | 连接 |
|------|------|
| 片内总线 | CPU 芯片内部 |
| 系统总线 | CPU、主存、I/O 接口之间 |
| 局部总线 | 高速设备之间 |
| 通信总线 | 计算机系统之间 |

---

## 考试辨析
- 总线是**共享**传输介质——同一时刻只能有一个设备发送
- 地址总线的位数决定**可寻址范围**（$n$ 根地址线 → $2^n$ 个地址）`,
  },

  // --- 6.2 总线仲裁 ---
  {
    id: 'co-6-2-1', chapterId: 'co-6', title: '总线仲裁',
    keyConcepts: ['总线仲裁', '集中式', '链式查询', '计数器定时', '独立请求'], relatedPoints: [],
    content: `## 为什么需要仲裁
> 多个设备可能同时请求使用总线——需要决定给谁用。

---

## 集中式仲裁

| 方式 | 原理 | 优点 | 缺点 |
|------|------|------|------|
| **链式查询** | 信号链依次传递 | 简单 | 优先级固定，对电路故障敏感 |
| **计数器定时** | 计数器轮询各设备号 | 优先级可编程 | 线数多 |
| **独立请求** | 每个设备独立请求线 | 响应快 | 线数最多 |

---

## 考试辨析
- 总线仲裁考得较少
- 链式查询的优先级由**物理位置**决定——离控制器越近优先级越高`,
  },

  // --- 6.3 总线操作和定时 ---
  {
    id: 'co-6-3-1', chapterId: 'co-6', title: '总线操作与定时',
    keyConcepts: ['总线周期', '同步通信', '异步通信', '半同步通信', '分离式通信'], relatedPoints: [],
    content: `## 总线的四个阶段

1. **申请阶段**：设备申请总线使用权
2. **寻址阶段**：发送地址
3. **传输阶段**：数据传送
4. **结束阶段**：释放总线

---

## 通信定时方式

| 方式 | 特点 | 优缺点 |
|------|------|--------|
| **同步通信** | 统一时钟控制 | 简单快速，但所有设备必须同频 |
| **异步通信** | 握手应答 | 灵活（支持异频设备） |
| **半同步通信** | 同步 + 等待信号 | 折中 |

---

## 考试辨析
- 同步通信要求**所有设备同一时钟**——对设备要求高
- 异步通信不需要统一时钟——不同速度设备可以通信`,
  },

  // ===========================================================
  // 第7章 输入/输出系统
  // ===========================================================

  // --- 7.1 I/O 系统基本概念 ---
  {
    id: 'co-7-1-1', chapterId: 'co-7', title: 'I/O接口',
    keyConcepts: ['I/O接口', '端口', '数据端口', '状态端口', '控制端口', '编址'], relatedPoints: [],
    content: `## I/O 接口的功能
1. 数据缓冲（速度匹配）
2. 信号转换（电平、格式）
3. 中断请求
4. 设备选择

---

## 端口分类
| 端口 | 说明 |
|------|------|
| **数据端口** | 数据缓冲 |
| **状态端口** | 设备状态（忙/闲/错） |
| **控制端口** | 控制命令 |

---

## I/O 端口编址
| 方式 | 说明 |
|------|------|
| **独立编址** | I/O 端口有独立地址空间——需要专用 I/O 指令 |
| **统一编址** | I/O 端口与主存统一编址——用 Load/Store 访问 |

---

## 考试辨析
- 统一编址又称"存储器映射 I/O"——不需专用 I/O 指令
- 独立编址需要 IN/OUT 指令（x86 采用）`,
  },

  // --- 7.2 程序查询方式 ---
  {
    id: 'co-7-2-1', chapterId: 'co-7', title: '程序查询方式',
    keyConcepts: ['程序查询', '轮询', 'CPU等待', '独占'], relatedPoints: ['co-7-3-1', 'co-7-4-1'],
    content: `## 程序查询方式

> CPU 不断查询 I/O 设备状态——"忙等"。

\`\`\`
while (设备忙)
    ;  // 等待
送数据
\`\`\`

---

## 特点
| 特性 | 说明 |
|------|------|
| CPU 利用率 | **低**（一直在轮询等待） |
| 数据传送 | CPU 直接执行 |
| 优先级 | 取决于查询顺序 |
| 优点 | 实现简单 |

---

## 考试辨析
- 程序查询是**最简单的 I/O 方式**——CPU 与外设串行工作
- 不适合高速设备——CPU 被绑定等待`,
  },

  // --- 7.3 程序中断方式 ---
  {
    id: 'co-7-3-1', chapterId: 'co-7', title: '程序中断方式',
    keyConcepts: ['中断', '中断请求', '中断响应', '中断向量', '中断优先级', '中断屏蔽'], relatedPoints: ['co-5-4-1', 'co-7-2-1'],
    content: `## 中断 I/O 的思想

> 外设准备好后**主动通知 CPU**——CPU 暂停当前程序，执行中断服务程序。

---

## 中断处理流程
1. 外设发出中断请求
2. CPU 当前指令结束后响应
3. 关中断
4. 保存断点和现场
5. 取中断向量 → 中断服务程序
6. 执行中断服务
7. 恢复现场 → 开中断 → 返回

---

## 中断优先级
| 方式 | 说明 |
|------|------|
| 硬件排队器 | 固定优先级（菊花链） |
| 软件查询 | 可编程优先级 |
| **中断屏蔽** | 动态改变优先级 |

---

## 考试辨析
- 与程序查询的关键区别：CPU 不需等待——**并行工作**
- 中断响应在**当前指令执行结束后**（不是当前总线周期）
- 多重中断：高优先级中断可以打断低优先级中断服务`,
  },

  // --- 7.4 DMA 方式 ---
  {
    id: 'co-7-4-1', chapterId: 'co-7', title: 'DMA方式',
    keyConcepts: ['DMA', '直接存储器访问', 'DMA控制器', '周期窃取', '批量传送'], relatedPoints: ['co-7-3-1', 'co-6-1-1'],
    content: `## DMA 方式的思想

> 用 **DMA 控制器** 直接完成外设与主存之间的数据传送——**不需要 CPU 干预**。

---

## DMA 传送的三个阶段

| 阶段 | 操作 |
|------|------|
| **预处理** | CPU 设置 DMA 参数（源/目的地/长度） |
| **数据传送** | DMA 控制器接管总线，批量传送数据 |
| **后处理** | 传送完成，DMA 向 CPU 发中断通知 |

---

## DMA vs 中断 vs 程序查询

| | 程序查询 | 中断 | DMA |
|------|----------|------|-----|
| 数据传送 | CPU | CPU | DMA 控制器 |
| CPU 参与度 | 全程参与 | 中断服务时 | **仅预处理/后处理** |
| 适用 | 慢速设备 | 中速设备 | **高速批量**（磁盘） |
| 并行度 | 无 | 部分并行 | 高 |

---

## 考试辨析
- DMA 的"周期窃取"——DMA 窃取 CPU 的**总线周期**（不是 CPU 时钟周期）
- DMA 与 CPU 同时访存时：DMA 优先级**高于** CPU（数据可能丢失）
- DMA 控制器 ≠ DMA 方式——DMA 控制器实现 DMA 方式`,
  },
];
