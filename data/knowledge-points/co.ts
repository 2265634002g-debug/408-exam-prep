import type { KnowledgePoint } from '@/types/knowledge';

export const coKnowledgePoints: KnowledgePoint[] = [

  // ===========================================================
  // 第1章 计算机系统概述
  // ===========================================================

  // --- 1.1 计算机发展历程 ---
  {
    id: 'co-1-1-1', chapterId: 'co-1', title: '计算机发展历程与冯·诺依曼结构',
    keyConcepts: ['冯·诺依曼', '存储程序', '五大部件', '二进制', '运算器中心'], relatedPoints: ['co-1-2-1'],
    content: `## 冯·诺依曼计算机的基本思想

> **"存储程序"**——将程序像数据一样存入存储器中，计算机自动逐条取出指令执行。

### 核心要点
1. 计算机由**五大部件**组成：运算器、控制器、存储器、输入设备、输出设备
2. 指令和数据以**二进制**形式存储
3. 以**运算器为中心**（现代计算机改为**以存储器为中心**）
4. 指令按顺序执行（程序计数器 PC 控制）

---

## 五大部件功能
| 部件 | 功能 |
|------|------|
| **运算器** | 算术运算 + 逻辑运算 |
| **控制器** | 控制指令执行顺序，产生控制信号 |
| **存储器** | 存储程序和数据 |
| **输入设备** | 将信息输入计算机 |
| **输出设备** | 将结果输出 |

---

## 考试辨析
- 冯·诺依曼机的核心是**存储程序**
- "以运算器为中心"是冯氏结构的标志——现代结构"以存储器为中心"
- 指令和数据同等存于存储器中——**从存储内容上无法区分**（只有执行时才区分）`,
  },

  // --- 1.2 计算机系统层次结构 ---
  {
    id: 'co-1-2-1', chapterId: 'co-1', title: '计算机系统的层次结构',
    keyConcepts: ['层次结构', '裸机', '虚拟机', '翻译程序', '解释程序', '编译', '汇编'], relatedPoints: [],
    content: `## 计算机系统的多级层次

\`\`\`
用户

高级语言 (C/Java/Python)  → 编译程序

汇编语言               → 汇编程序

操作系统               → 操作系统

机器语言（指令系统）

微程序（微指令）

硬件逻辑（电路）
\`\`\`

---

## 核心概念
| 概念 | 说明 |
|------|------|
| **翻译程序** | 将高级语言源代码转换为机器语言 |
| **编译程序** | 一次性翻译整个源程序 |
| **解释程序** | 逐条翻译并执行（不生成目标代码） |
| **汇编程序** | 将汇编语言翻译为机器语言 |

---

## 机器/汇编/高级语言对比
| 语言 | 特点 | 可读性 | 可移植性 |
|------|------|--------|----------|
| 机器语言 | 二进制代码 | 差 | 差 |
| 汇编语言 | 助记符 | 较好 | 差 |
| 高级语言 | 面向问题 | 好 | 好 |

---

## 考试辨析
- 编译与解释的区别：编译生成目标代码，解释不生成
- 汇编语言与机器指令一一对应（汇编 = 机器语言的助记符形式）`,
  },

  // --- 1.3 计算机性能指标 ---
  {
    id: 'co-1-3-1', chapterId: 'co-1', title: '计算机的性能指标',
    keyConcepts: ['主频', 'CPI', 'MIPS', 'MFLOPS', '机器字长', '数据通路带宽', '响应时间'], relatedPoints: ['co-5-5-1'],
    content: `## 核心性能指标

| 指标 | 含义 | 公式 |
|------|------|------|
| **机器字长** | CPU 一次处理的二进制位数 | — |
| **主频 (f)** | CPU 每秒的时钟周期数 | 单位 Hz |
| **时钟周期 (T)** | 一个时钟周期的时间 | $T = 1/f$ |
| **CPI** | 每条指令所需的平均时钟周期数 | — |
| **CPU 执行时间** | 执行某程序的总时间 | $T_{CPU} = N \\times CPI \\times T$ |
| **MIPS** | 每秒百万条指令 | $MIPS = f / (CPI \\times 10^6)$ |
| **MFLOPS** | 每秒百万次浮点运算 | — |

---

## CPU 执行时间公式（核心）
$$T_{CPU} = N \\times CPI \\times T = \\frac{N \\times CPI}{f}$$

- $N$ = 指令条数
- $CPI$ = 每条指令平均时钟周期数
- $T$ = 时钟周期

---

## 考试辨析
- $T_{CPU}$ 越小越好，MIPS/MFLOPS 越大越好
- 主频 ≠ 性能——受 CPI 影响很大
- 提高主频 $f$ 和降低 $CPI$ 都提高性能`,
  },

  // ===========================================================
  // 第2章 数据的表示和运算
  // ===========================================================

  // --- 2.1 数制与编码 ---
  {
    id: 'co-2-1-1', chapterId: 'co-2', title: '进位计数制与转换',
    keyConcepts: ['二进制', '八进制', '十六进制', '十进制', '进制转换'], relatedPoints: ['co-2-1-2'],
    content: `## 四种常用进制

| 进制 | 基数 | 数码 | 后缀 |
|------|------|------|------|
| 二进制 | 2 | 0,1 | B |
| 八进制 | 8 | 0~7 | O |
| 十进制 | 10 | 0~9 | D |
| 十六进制 | 16 | 0~9,A~F | H |

---

## 转换方法
| 转换方向 | 方法 |
|----------|------|
| 任意进制 → 十进制 | **按权展开求和** |
| 十进制 → 任意进制 | 整数**除基取余**，小数**乘基取整** |
| 二进制 ↔ 八进制 | 3 位一组 |
| 二进制 ↔ 十六进制 | 4 位一组 |

---

## 考试辨析
- BCD 码：用 4 位二进制表示 1 位十进制——有 8421 码(有权)、余 3 码(无权)等
- 进制转换是数据表示和运算的基础，不会单独出大题`,
  },
  {
    id: 'co-2-1-2', chapterId: 'co-2', title: '定点数的编码表示',
    keyConcepts: ['原码', '反码', '补码', '移码', '模运算', '符号位', '数值范围'], relatedPoints: ['co-2-2-1'],
    content: `## 四种编码对比（n 位字长，含符号位）

| 编码 | 正数 | 负数 | 0 的表示 | 数值范围 |
|------|------|------|----------|----------|
| **原码** | 符号位 0 + 绝对值 | 符号位 1 + 绝对值 | +0 / -0 | $-(2^{n-1}-1) \\sim +(2^{n-1}-1)$ |
| **反码** | 同原码 | 原码**除符号位外取反** | +0 / -0 | $-(2^{n-1}-1) \\sim +(2^{n-1}-1)$ |
| **补码** | 同原码 | 反码末位 **+1** | 只有 +0 | **$-2^{n-1} \\sim +(2^{n-1}-1)$** |
| **移码** | 补码符号位取反 | 补码符号位取反 | 只有 +0 | $-2^{n-1} \\sim +(2^{n-1}-1)$ |

---

## 补码（最重要）

> **计算机中整数统一使用补码表示**——原因：0 唯一表示，减法变加法。

补码求法口诀：**"按位取反末尾加一"**

$$[X]_{补} = \\begin{cases} X & X \\ge 0 \\\\ 2^n + X & X < 0 \\end{cases}$$

---

## 考试辨析
- **补码的数值范围不对称**：负数范围比正数多 1（如 8 位：$-128 \\sim +127$）
- 移码与补码符号位相反——移码用于**浮点数阶码**
- 原码加减需判断符号位和绝对值大小——实现复杂`,
  },

  // --- 2.2 定点数的表示与运算 ---
  {
    id: 'co-2-2-1', chapterId: 'co-2', title: '定点数的加减运算与溢出判断',
    keyConcepts: ['加法器', '溢出', 'OF', 'SF', 'CF', 'ZF', '进位', '双符号位'], relatedPoints: ['co-2-1-2', 'co-2-4-1'],
    content: `## 补码加减法

$$[A+B]_{补} = [A]_{补} + [B]_{补}$$
$$[A-B]_{补} = [A]_{补} + [-B]_{补}$$

> 补码可以将减法统一为加法——这是补码最大的价值。

---

## 溢出判断（三种方法）

### 方法一：单符号位法
若两个正数相加得负数，或两个负数相加得正数 → 溢出。

### 方法二：双符号位法（变形补码）
- 正数：00.xxx，负数：11.xxx
- **结果双符号位异号（01 或 10）→ 溢出**
- 01 = 正溢出（上溢），10 = 负溢出（下溢）

### 方法三：进位判断法
最高数值位的进位 $C_{n-1}$ 与符号位的进位 $C_n$ **异或 = 1** → 溢出。
$$OF = C_n \\oplus C_{n-1}$$

---

## 标志位
| 标志 | 含义 | 判断条件 |
|------|------|----------|
| **ZF** | 零标志 | 结果为 0 |
| **SF** | 符号标志 | 结果的最高位（符号位） |
| **CF** | 进位/借位 | 无符号运算的进位/借位 |
| **OF** | 溢出标志 | **有符号运算**溢出 |

---

## 考试辨析
- OF 只对**有符号数**有意义，CF 只对**无符号数**有意义
- 双符号位法是最直观的溢出判断——408 常考`,
  },  {
    id: 'co-2-2-2', chapterId: 'co-2', title: '定点数的乘除运算',
    keyConcepts: ['乘法', 'Booth算法', '原码乘法', '补码乘法', '除法', '不恢复余数', '加减交替'], relatedPoints: ['co-2-2-1'],
    content: `## 乘法运算

### Booth 算法（补码一位乘法）—— 核心步骤
设 X 为被乘数，Y 为乘数，均用补码表示。

**算法流程：**
1. 在乘数 Y 末位补 0（即 $y_{n+1}=0$）
2. 根据 $(y_i, y_{i+1})$ 决定操作：
- 0,0 或 1,1 → 部分积 +0
- 0,1（上升沿） → 部分积 +[X]补
- 1,0（下降沿） → 部分积 +[-X]补
3. 部分积和乘数一起**算术右移**一位
4. 重复 n 次（n = 字长）

**手算示例：X = -3 (1101补), Y = 2 (0010)**
\`\`\`
初始: 部分积 0000, 乘数 0010, y_{n+1}=0
Step1: (0,0) → +0, 右移 → 0000 0001 0
Step2: (1,0) → +[X]补=1101 → 1101 0001 0, 右移 → 1110 1000 1
Step3: (0,1) → +[-X]补=0011 → 0001 1000 1, 右移 → 0000 1100 0
Step4: (0,0) → +0, 右移 → 0000 0110 0
结果: 0000 0110 = +6 (补码) = (-3)×2 = -6 的补码 → 1111 1010
\`\`\`
时间复杂度：**n 次加法 + n 次移位**

---
## 除法运算
### 不恢复余数法（加减交替法）
1. 被除数 - 除数 → 若余数 ≥ 0，商 1；余数 < 0，商 0 并恢复余数（加回去）
2. **优化**：够减商 1，下一步 - 除数；不够减商 0，下一步 **+ 除数**（不恢复，直接用加减交替）
3. 重复 n 次，商 n 位

---
## 考试辨析
- Booth 算法每次看 $(y_i, y_{i+1})$ 两位作出决策——不是简单地"同加异减"
- 补码除法的商也以补码表示——商的符号由被除数和除数符号异或决定
- 除法运算在 408 中考得相对少，重点掌握补码一位乘法（Booth算法）的手工模拟`,
  },

  // --- 2.3 浮点数的表示与运算 ---
  {
    id: 'co-2-3-1', chapterId: 'co-2', title: 'IEEE 754 浮点数标准',
    keyConcepts: ['IEEE754', '单精度', '双精度', '阶码', '尾数', '规格化', '隐藏位', '移码', 'NaN', '非规格化数'], relatedPoints: ['co-2-1-2', 'co-2-3-2'],
    content: `## IEEE 754 浮点数格式
> $N = (-1)^S \\times 1.M \\times 2^{E-bias}$

| 精度 | 总位数 | 符号S | 阶码E | 尾数M | bias | 真值范围 |
|------|--------|--------|--------|--------|------|----------|
| **单精度** | 32 位 | 1 | 8 | 23 | 127 | $\\pm(10^{-38} \\sim 10^{38})$ |
| **双精度** | 64 位 | 1 | 11 | 52 | 1023 | $\\pm(10^{-308} \\sim 10^{308})$ |

---
## 阶码与尾数细节
- **阶码用移码表示**（非标准移码，偏置值 bias = $2^{k-1}-1$）
- 单精度：8位阶码，bias = 127，实际阶码 = E - 127
- 双精度：11位阶码，bias = 1023，实际阶码 = E - 1023
- **尾数用原码**表示，规格化后有**隐藏位**（整数部分隐含为 1，不存）
- 例如：单精度尾数 23 位，实际精度 = 24 位（1 隐藏位 + 23 小数位）

---
## 特殊值的位模式（高频考点）
| E（阶码） | M（尾数） | 表示的值 | 说明 |
|-----------|----------|----------|------|
| 全 0 | 全 0 | $\\pm 0$ | 根据 S 位分 +0 / -0 |
| 全 0 | 非 0 | **非规格化数** | 隐藏位变为 0，E 视为 1-bias |
| 全 1 | 全 0 | $\\pm \\infty$ | 根据 S 位 |
| 全 1 | 非 0 | **NaN**（非数） | 如 $\\sqrt{-1}$、$0/0$ |
| 其他 | 任意 | **规格化数** | 正常浮点数 |

---
## 非规格化数（Denormalized）
- 用于表示比最小规格化数更接近 0 的值（逐渐下溢）
- 格式：$N = (-1)^S \\times 0.M \\times 2^{1-bias}$（隐藏位 = 0）
- 好处：避免**突然下溢**（从最小正数直接跳到 0）

---
## 手算示例
**【例】单精度浮点数 C0A00000H 的真值是多少？**

C0A00000H = 1100 0000 1010 0000 0000 0000 0000 0000B
- S = 1 → 负数
- E = 1000 0001B = 129，实际阶码 = 129 - 127 = 2
- M = 010 0000...（隐藏位为1）→ 尾数 = 1.01B = 1.25
- 真值 = $(-1)^1 \\times 1.25 \\times 2^2 = -5.0$

**【例】将 -12.75 转为单精度浮点数**
- 12 = 1100B，0.75 = 0.11B → 12.75 = 1100.11B = $1.10011 \\times 2^3$
- S = 1（负数），实际阶码 = 3，E = 3 + 127 = 130 = 10000010B
- M = 10011 0000 0000 0000 0000 000（23位，去掉隐藏位1）
- 结果：1 10000010 10011000000000000000000 = C14C0000H

---
## 考试辨析
- 阶码全1 + 尾数非0 = NaN（不是无穷大）——选择题常见错误选项
- 阶码全0 → 隐藏位变为0（非规格化数），此时 E 按 1-bias 处理
- 单精度阶码范围 1~254（全0和全1有特殊含义），实际范围 -126~+127
- 浮点数能表示的范围大但**精度有限**（有效位数决定精度）`
  },  {
    id: 'co-2-3-2', chapterId: 'co-2', title: '浮点数的运算',
    keyConcepts: ['对阶', '规格化', '舍入', '溢出判断', '保护位', '大阶对小阶'], relatedPoints: ['co-2-3-1'],
    content: `## 浮点数加减的五个步骤

设有 $X = M_x \\times 2^{E_x}$，$Y = M_y \\times 2^{E_y}$，计算 $X \\pm Y$：

### 步骤一：对阶
> **小阶对大阶**——阶码小的尾数右移，阶码增加，直到两数阶码相同。
- 例如：$E_x = 3$，$E_y = 5$，则 $M_x$ 右移 $5-3=2$ 位，$E_x$ 增为 5
- **右移 = 丢失低位精度**——这是浮点运算误差的主要来源
- 对阶时不改变符号位

### 步骤二：尾数求和
- 对阶后两数的尾数做 $\\pm$ 运算（即定点加减）
- 可能产生溢出，交给规格化处理

### 步骤三：规格化
| 情况 | 操作 | 说明 |
|------|------|------|
| **左规** | 尾数左移，阶码减 1 | 结果尾数最高位不为 1 时（即 $|M| < 1$） |
| **右规** | 尾数右移，阶码加 1 | 尾数溢出时（$|M| \\ge 2$，即双符号位为 01 或 10） |
> 规格化的目标是使尾数变为 $1.xxx$ 或 $0.1xxx$（正数）。

### 步骤四：舍入
> 由于尾数位数有限，运算过程中多余的低位需要舍去。
| 方法 | 规则 | 优缺点 |
|------|------|--------|
| **0舍1入** | 舍去位最高位为 1 → 入；0 → 舍 | 误差小，但可能再次溢出（需再右规） |
| **恒置1** | 无论舍去什么，末位恒置为 1 | 简单，但误差偏大 |
| **截断** | 直接丢掉多余位 | 最简单，误差最大 |

### 步骤五：溢出判断
- **阶码上溢** → 结果溢出（$> 2^{127}$ 即 $\\approx 10^{38}$）
- **阶码下溢** → 结果视为 0（非规格化数或 $\\pm 0$）
- 尾数溢出 → 右规处理（阶码+1，可能引发阶码上溢）

---
## 保护位（Guard、Round、Sticky）
> IEEE 754 规定在运算过程中保留 3 个附加位，提高精度。
| 附加位 | 含义 |
|--------|------|
| G (Guard) | 保护位——运算时保留的一位 |
| R (Round) | 舍入位——Guard 右移的一位 |
| S (Sticky) | 粘滞位——舍入位之后所有位的逻辑或 |

---
## 考试辨析
- 对阶时**尾数右移 = 精度降低**——这是浮点运算误差的主因
- 舍入后可能尾数再次溢出 → 需要二次右规
- 背熟五个步骤的名称和顺序——408 各种题型都以此为基础
- 每年都有"浮点数加减法手工模拟"的选择题或简答题`
  },

  // --- 2.4 算术逻辑单元 ALU ---
  {
    id: 'co-2-4-1', chapterId: 'co-2', title: '算术逻辑单元 (ALU)',
    keyConcepts: ['ALU', '全加器', '串行进位', '并行进位', '先行进位', 'SN74181'], relatedPoints: ['co-2-2-1', 'co-5-1-1'],
    content: `## ALU 的功能

> ALU 是运算器的核心部件，能完成算术运算和逻辑运算。

---

## 进位方式
| 方式 | 特点 | 速度 |
|------|------|------|
| **串行进位** | 每级进位依赖前一级 | 慢，$O(n)$ |
| **并行进位** | 各级同时生成进位 | 快，但电路复杂 |
| **先行进位 (CLA)** | 分组并行 | 折中 |

---

## 标志位产生
| 标志 | 全称 | 说明 |
|------|------|------|
| ZF | Zero Flag | 结果为 0 |
| SF | Sign Flag | 符号位 |
| CF | Carry Flag | 进位/借位 |
| OF | Overflow Flag | 溢出 |

---

## 考试辨析
- 加法器是 ALU 的核心——408 主要考加法器及进位生成
- 串行进位的关键路径 $O(n)$，并行进位 $O(\\log n)$——了解区别即可`,
  },

  // ===========================================================
  // 第3章 存储系统
  // ===========================================================

  // --- 3.1 存储器概述 ---
  {
    id: 'co-3-1-1', chapterId: 'co-3', title: '存储器分类与层次结构',
    keyConcepts: ['层次结构', 'Cache', '主存', '辅存', '局部性', '时间局部性', '空间局部性'], relatedPoints: ['co-3-3-1', 'ds-1-1-2', 'os-3-1-1'],
    content: `## 存储器的层次结构

\`\`\`
寄存器（CPU内部）  ← 最快/最小/最贵
    ↓
Cache (SRAM)
    ↓
主存 (DRAM)
    ↓
磁盘/SSD（辅存） ← 最慢/最大/最便宜
\`\`\`

---

## 局部性原理
| 类型 | 含义 | 举例 |
|------|------|------|
| **时间局部性** | 刚访问过的数据短期内可能再次访问 | 循环变量 |
| **空间局部性** | 刚访问位置附近的数据可能被访问 | 数组遍历 |

---

## 层次结构解决的核心矛盾
$$\\text{容量} \\uparrow \\quad \\text{速度} \\uparrow \\quad \\text{价格} \\downarrow$$
> 三者不可兼得——用层次结构追求"看起来很大、用起来很快、成本可接受"。

---

## 考试辨析
- Cache-主存层解决**速度**问题（硬件自动完成）
- 主存-辅存层解决**容量**问题（OS + 硬件配合）`,
  },

  // --- 3.2 主存储器 ---
  {
    id: 'co-3-2-1', chapterId: 'co-3', title: 'SRAM与DRAM',
    keyConcepts: ['SRAM', 'DRAM', '刷新', '电容', '触发器', '双稳态', '存取周期'], relatedPoints: ['co-3-1-1'],
    content: `## SRAM vs DRAM 详细对比
| 特性 | SRAM | DRAM |
|------|------|------|
| **存储原理** | 6管双稳态触发器 | 电容充电（1T1C） |
| **破坏性读出** | 否 | **是**（读出后需重写） |
| **速度** | 快（几 ns） | 慢（几十 ns） |
| **集成度** | 低（6管/bit） | 高（1T1C/bit） |
| **功耗** | 大（持续通电） | 小（电容不耗电） |
| **刷新** | 不需要 | **必须定期刷新**（2ms内） |
| **价格/GB** | 贵 | 便宜 |
| **用途** | Cache（L1/L2/L3） | 主存（DDR4/DDR5） |

---
## DRAM 刷新机制
> DRAM 电容会漏电——信息保留时间约 **2ms**，必须在 2ms 内完成一次全部刷新。

### 三种刷新方式
| 方式 | 做法 | 优点 | 缺点 |
|------|------|------|------|
| **集中刷新** | 在 2ms 末尾连续完成全部刷新 | 实现简单 | 有一段"死时间"（停止读写） |
| **分散刷新** | 每个存取周期后刷新一行 | 无死时间 | 存取周期变长（×2） |
| **异步刷新** | 将刷新均匀分散在 2ms 内 | 折中方案 | 略复杂 |

例：磁芯存储器 2ms 刷新 256 行
- 集中刷新：死时间 = 256 × 刷新周期
- 分散刷新：存取周期 = 原周期 × 2
- 异步刷新：每 2ms/256 = 7.8125μs 刷新一行

---
## 考试辨析
- **只有 DRAM 需要刷新**——SRAM 用触发器，不需要
- 刷新的本质：**读出** → 放大 → **重新写入**
- 刷新一行 = 所有同行的存储单元同时进行（不需要列地址）
- 刷新操作对 CPU 透明——由存储器控制器独立完成
-- 「[2010-选择16] [2012-选择17] [2015-选择18] [2022-选择16」`,
  },
  {
    id: 'co-3-2-2', chapterId: 'co-3', title: 'ROM、Flash与SSD',
    keyConcepts: ['ROM', 'Flash', 'SSD', '非易失', 'NOR Flash', 'NAND Flash'], relatedPoints: ['co-3-2-1'],
    content: `## 只读存储器 (ROM)

| 类型 | 特点 |
|------|------|
| MROM | 出厂掩膜写入，不可改 |
| PROM | 用户可写一次 |
| EPROM | 可多次擦除（紫外线） |
| EEPROM | 电擦除（可在线改写） |
| Flash | 电擦除，速度快，成本低 |

---

## NOR Flash vs NAND Flash

| | NOR Flash | NAND Flash |
|------|----------|-----------|
| 读取 | 随机读取快 | 按页读取 |
| 写入/擦除 | 慢 | 快 |
| 容量 | 小 | 大 |
| 用途 | 存储 BIOS/固件 | SSD、U 盘、存储卡 |

---

## SSD (固态硬盘)
- 核心：NAND Flash + 控制器
- 优点：无机械部件，随机访问快，低功耗
- 缺点：写入次数有限（擦写寿命），需要磨损均衡

---

## 考试辨析
- ROM ≠ 只读——现代 EEPROM/Flash 可擦写
- Flash 写入前需先擦除——以"块"为单位擦除`,
  },

  // --- 3.3 高速缓冲存储器 Cache ---
  {
    id: 'co-3-3-1', chapterId: 'co-3', title: 'Cache的基本原理',
    keyConcepts: ['Cache', '命中', '缺失', '命中率', '块', '主存块', 'Cache行'], relatedPoints: ['co-3-1-1', 'co-3-3-2', 'os-3-4-1'],
    content: `## Cache 的工作原理

> 利用**局部性原理**，将主存中频繁访问的数据块复制到 Cache 中。

CPU 先访问 Cache → **命中**则直接取数据 → **缺失**则从主存取数据并替换到 Cache。

---

## 核心概念
| 术语 | 含义 |
|------|------|
| **命中 (Hit)** | CPU 要访问的数据在 Cache 中 |
| **缺失 (Miss)** | CPU 要访问的数据不在 Cache 中 |
| **命中率 $h$** | Cache 命中的概率 |
| **缺失率** | $= 1 - h$ |

---

## 性能计算（大题常考）

$$T_{avg} = h \\times T_{cache} + (1-h) \\times T_{main}$$

**加速比** $= T_{main} / T_{avg}$

---

## 考试辨析
- Cache 对程序员透明（硬件自动管理）
- 命中时间的定义：从 Cache 读取数据 + 判定的时间
- 缺失损失 = 从主存取回数据块的时间`,
  },  {
    id: 'co-3-3-2', chapterId: 'co-3', title: 'Cache的地址映射方式',
    keyConcepts: ['直接映射', '全相联映射', '组相联映射', 'Cache行', '主存块', '标记', '组号', '块内地址'], relatedPoints: ['co-3-3-1', 'co-3-3-3', 'co-3-3-4'],
    content: `## 三种映射方式的核心原理

Cache 地址映射的核心问题：**主存块应该放到 Cache 的哪个位置？**

---
## 一、直接映射 (Direct Mapping)
$$\\text{Cache 行号} = \\text{主存块号} \\bmod \\text{Cache 行数}$$

> 每块主存只能映射到 Cache 中**唯一固定**的行。

| 优点 | 缺点 |
|------|------|
| 硬件简单（比较电路少） | **冲突缺失率最高** |
| 查找快（只查一行） | 空间利用率低（可能大量空行被闲置） |

**主存地址拆分**：\`| 标记(tag) | Cache行号(index) | 块内地址(offset) |\`

---
## 二、全相联映射 (Fully Associative)
> 每块主存可映射到 Cache 中**任意**行。

| 优点 | 缺点 |
|------|------|
| **冲突缺失率最低** | 比较电路复杂（需同时比较所有行） |
| 空间利用率最高 | 成本高、速度慢 |

**主存地址拆分**：\`| 标记(tag) | 块内地址(offset) |\`（无 index 字段）

---
## 三、组相联映射 (Set-Associative)
$$\\text{Cache 组号} = \\text{主存块号} \\bmod \\text{组数}$$

> **组间直接映射，组内全相联**——最实用的折中方案。

| 参数 | 含义 |
|------|------|
| $s$ | Cache 总行数 |
| $w$ | 每组行数（路数，$w$-way） |
| $q = s/w$ | 组数 |

**主存地址拆分**：\`| 标记(tag) | 组号(set) | 块内地址(offset) |\`

---
## 主存地址拆分计算（必考题型）

**已知条件**: 主存地址 $n$ 位，Cache 容量 $C$，块大小 $B$，路数 $w$

| 分步计算 | 直接映射 | $w$-路组相联 | 全相联 |
|----------|---------|-------------|--------|
| Cache行数 | $s = C/B$ | $s = C/B$ | $s = C/B$ |
| 组数 | $=$ 行数 | $q = s/w$ | 1 组 |
| 块内偏移位数 | $\\log_2 B$ | $\\log_2 B$ | $\\log_2 B$ |
| Index/Set位数 | $\\log_2 s$ | $\\log_2 q$ | 0 |
| **Tag位数** | **$n - \\log_2 s - \\log_2 B$** | **$n - \\log_2 q - \\log_2 B$** | **$n - \\log_2 B$** |

---
## 手算示例

**【题】主存 4GB(32位地址)，Cache 8KB，块大小 64B。求各映射下地址结构。**

解：
- 块内偏移 = $\\log_2 64 = 6$ 位
- Cache 行数 = $8KB \\div 64B = 128$ 行
- 副存块数 = $4GB \\div 64B = 2^{26}$ 块

| 映射 | Index/组号位 | Tag 位 |
|------|------------|--------|
| 直接映射 | $\\log_2 128 = 7$ | $32-7-6 = 19$ |
| 2路组相联 | $\\log_2 64 = 6$ | $32-6-6 = 20$ |
| 4路组相联 | $\\log_2 32 = 5$ | $32-5-6 = 21$ |
| 全相联 | 0 | $32-0-6 = 26$ |

---
## 三种映射对比
| | 直接 | 组相联 | 全相联 |
|------|------|--------|--------|
| 冲突缺失 | 多 | 中 | 少 |
| 比较器数 | 1 | $w$ | $s$ |
| Tag 位数 | 最短 | 中等 | 最长 |
| 实际使用 | 小容量/简单设计 | **现代 CPU 标配** | TLB |

---
## 考试辨析
- **主存地址拆分是 408 大题必考内容**——几乎每年都有一道 Cache 综合题
- 关键：先确定块大小→块内位数→行数/组数→Index/Set位数→剩余为 Tag
- TLB 通常用全相联或组相联，因为条目少、要求高命中率
- 组相联的"路数"增加 → 更接近全相联 → 冲突减少但成本增加`
  },  {
    id: 'co-3-3-3', chapterId: 'co-3', title: 'Cache的替换算法与写策略',
    keyConcepts: ['LRU', 'FIFO', '随机替换', '写直达', '写回', '写分配', '非写分配', '伪LRU'], relatedPoints: ['co-3-3-2'],
    content: `## 替换算法
> 当 Cache 满且需要调入新块时，选择哪个块替换出去。

| 算法 | 策略 | 实现难度 | 命中率 | 说明 |
|------|------|---------|--------|------|
| **随机替换(RAND)** | 随机选一块 | 最简单 | 最低 | 硬件生成随机数 |
| **FIFO** | 替换最早进入的 | 简单 | 中等 | 可能替换掉频繁使用的块 |
| **LRU** | 替换最久未使用的 | 复杂 | **最高** | 利用时间局部性 |
| **伪LRU (PLRU)** | LRU 的简化版 | 中等 | 接近 LRU | 二叉树近似 |

---
## LRU 的实现（2路组相联）
> 每组只需 1 位 LRU 位：0=最近用的是左路，替换右路；1=相反。

对于 4 路组相联，需要跟踪 4! = 24 种顺序 → 硬件成本高 → 通常用 PLRU。

---
## 写策略

### 写命中 (Write Hit) 时的策略
| 策略 | 做法 | 优点 | 缺点 |
|------|------|------|------|
| **写直达 (Write-Through)** | 同时写 Cache 和主存 | 数据一致性好 | 写速度慢（受主存限制） |
| **写回 (Write-Back)** | 只写 Cache，替换时再写回主存 | 写速度快 | 一致性复杂（需 dirty 位） |

> **Dirty 位**：标记 Cache 行是否被修改过。脏位=1 → 替换时须写回主存。

### 写缺失 (Write Miss) 时的策略
| 策略 | 做法 | 通常搭配 |
|------|------|----------|
| **写分配 (Write-Allocate)** | 先调入主存块到 Cache，再写入 | 写回 |
| **非写分配 (No-Write-Allocate)** | 直接写主存，不调入 Cache | 写直达 |

> **常用组合**：写回 + 写分配（Write-Back + Write-Allocate）

---
## 多核 Cache 一致性
> 多个 CPU 核各有独立 Cache，当一个核修改数据时，其他核的 Cache 需要感知。

### MESI 协议（4 种状态）
| 状态 | 全称 | 含义 |
|------|------|------|
| **M (Modified)** | 已修改 | 只在本 Cache 有，脏数据，写回后变为 E |
| **E (Exclusive)** | 独占 | 只在本地，与主存一致 |
| **S (Shared)** | 共享 | 多 Cache 同时拥有，与主存一致 |
| **I (Invalid)** | 无效 | 该行数据已过时 |

核心操作：**总线嗅探 (Bus Snooping)**——所有 Cache 控制器监视总线上的写操作。

---
## 考试辨析
- LRU 最适合利用**时间局部性**（刚访问的可能很快再访问）
- 写回策略必须有 **Dirty 位**——选择题常见考点
- 多核 Cache 一致性是近年来 408 的新热点（2019、2022年考过）
- 写直达 + 非写分配是经典组合（简单、一致性高）
- 写回 + 写分配是性能最佳组合（现代 CPU 常用）`
  },

  // --- 3.4 虚拟存储器 ---
  // --- 做题技巧 ---
  {
    id: 'co-3-3-4', chapterId: 'co-3', title: 'Cache 常见题型与解题技巧',
    keyConcepts: ['Cache', '组相联', '地址拆分', 'LRU', '命中率', '平均访存时间'], relatedPoints: ['co-3-3-2', 'co-3-3-3', 'os-3-4-3'],
    content: `## 题型一：主存地址拆分
**已知**：主存地址n位，Cache容量C，块大小B，路数w
**步骤**：
1. 块内偏移位 = log2(B)
2. Cache行数 s = C/B；组数 q = s/w
3. Index位 = log2(s)或log2(q)；标记位 = n - Index位 - 偏移位

**经典陷阱**：给定单元地址，先求块号再取模！
例：129号单元，块大小32B -> 块号=floor(129/32)=4，不是129

---
## 题型二：LRU命中率计算
组相联+LRU替换时，需逐地址模拟替换过程：
1. 确定每个地址的块号和组号
2. 维护每组的LRU栈（最近使用的在栈顶）
3. 访问命中=已在组内，缺失=不在组内(需替换栈底块)

---
## 题型三：平均访存时间
Tavg = h x Tcache + (1-h) x Tmain
两Cache串联：Tavg = h1 x T1 + (1-h1) x [h2 x T2 + (1-h2) x Tmain]

---
## 易混淆结论
- TLB命中 != Cache命中（两者独立）
- TLB是页表的Cache——用SRAM，全相联
- Cache命中 != 页表命中（两者不相关）`,
  },

  {
    id: 'co-3-4-1', chapterId: 'co-3', title: '虚拟存储器',
    keyConcepts: ['虚拟地址', '物理地址', '页表', 'TLB', '快表', '页式存储', '缺页'], relatedPoints: ['co-3-1-1', 'co-3-3-1', 'os-3-4-1', 'os-3-3-1'],
    content: `## 虚拟存储器的目的
> 将**主存**和**辅存**统一编址，向程序员提供一个比主存大得多的地址空间。

---

## 核心概念
| 概念 | 含义 |
|------|------|
| **虚拟地址（逻辑地址）** | 程序员看到的地址 |
| **物理地址** | 实际主存的地址 |
| **页表** | 虚拟页号 → 物理页号映射表 |
| **页表项 (PTE)** | 含物理页号 + 有效位/保护位等 |
| **缺页 (Page Fault)** | 访问的页不在主存中 |

---

## TLB (快表)

> TLB = Translation Lookaside Buffer，页表的高速缓存。

| TLB | 说明 |
|-----|------|
| 存什么 | 近期使用的页表项 |
| 为什么要 | 减少访问页表的次数（一次访存 → 一次查 TLB） |
| 特点 | 容量小、速度快、全相联 |

---

## 虚拟地址 → 物理地址流程
1. CPU 产生虚拟地址
2. 查 TLB → 命中则直接得到物理地址
3. TLB 缺失 → 查页表 → 更新 TLB
4. 页表缺失 → 缺页中断 → OS 调入页 → 更新页表

---

## 考试辨析
- 虚拟存储器由 OS + 硬件共同管理（主存-辅存层）
- 虚拟地址空间大小取决于**地址线位数**，不取决于主存大小
- TLB = 页表的 Cache——用 SRAM 实现`,
  },

  // ===========================================================
  // 第4章 指令系统
  // ===========================================================

  // --- 4.1 指令格式 ---
  {
    id: 'co-4-1-1', chapterId: 'co-4', title: '指令格式与操作码编码',
    keyConcepts: ['指令格式', '操作码', '地址码', '定长编码', '变长编码', '扩展操作码'], relatedPoints: ['co-5-1-1'],
    content: `## 指令的基本格式

一条指令由**操作码 (OP)** 和**地址码 (A)** 组成：

\`\`\`
| 操作码 OP | 地址码 A1 | A2 | A3 |
\`\`\`

| 字段 | 含义 |
|------|------|
| 操作码 | 指明指令做什么（加、减、跳转...） |
| 地址码 | 操作数的地址信息 |

---

## 按地址数分类
| 类型 | 格式 | 特点 |
|------|------|------|
| 三地址 | OP A1, A2, A3 | (A1) OP (A2) → A3 |
| 二地址 | OP A1, A2 | (A1) OP (A2) → A1 |
| 一地址 | OP A | (AC) OP (A) → AC |
| 零地址 | OP | 栈顶两个数操作（堆栈机） |

---

## 操作码编码方式
| 方式 | 说明 | 优缺点 |
|------|------|--------|
| **定长编码** | 所有指令操作码长度相同 | 译码简单，空间利用率低 |
| **变长编码** | 常用指令短编码，不常用指令长编码 | 提高利用率，译码复杂 |
| **扩展操作码** | 在多地址中用一部分操作码做扩展 | 平衡定长和变长 |

---

## 考试辨析
- 扩展操作码就是"用地址位做操作码"——减少地址字段，增加操作码位
- $n$ 位操作码最多表示 $2^n$ 条指令`,
  },

  // --- 4.2 指令的寻址方式 ---
  {
    id: 'co-4-2-1', chapterId: 'co-4', title: '寻址方式',
    keyConcepts: ['立即寻址', '直接寻址', '间接寻址', '寄存器寻址', '基址寻址', '变址寻址', '相对寻址'], relatedPoints: ['co-4-1-1'],
    content: `## 常见寻址方式

| 方式 | 操作数位置 | 有效地址 | 特点 |
|------|-----------|----------|------|
| **立即寻址** | 指令中直接给出 | — | 不用访存，但数值范围有限 |
| **直接寻址** | 主存 | EA = A | 一次访存 |
| **间接寻址** | 主存中的地址指向主存 | EA = (A) | 需多次访存 |
| **寄存器直接** | 寄存器 | EA = R | 快，不用访存 |
| **寄存器间接** | 寄存器中地址指向主存 | EA = (R) | 一次访存 |
| **基址寻址** | 主存 | EA = (BR) + A | 程序重定位 |
| **变址寻址** | 主存 | EA = (IX) + A | **数组访问** |
| **相对寻址** | 主存 | EA = (PC) + A | 转移指令 |

---

## 基址 vs 变址
| | 基址寻址 | 变址寻址 |
|------|----------|----------|
| 基准 | 基址寄存器 BR | 形式地址 A |
| 偏移 | 形式地址 A | 变址寄存器 IX |
| 用途 | 程序重定位 | **数组/循环** |
| 谁变 | BR 变（OS 设） | IX 变（程序员设） |

---

## 考试辨析
- 相对寻址中 EA = (PC) + 偏移量——**PC 已是下一条指令地址**（注意！）
- 变址寻址特别适合**数组元素访问**——A 是首地址，IX 是下标
- 基址面向 OS 的程序重定位，变址面向用户程序的数组访问`,
  },

  // --- 4.3 CISC 和 RISC ---
  {
    id: 'co-4-3-1', chapterId: 'co-4', title: 'CISC与RISC',
    keyConcepts: ['CISC', 'RISC', '复杂指令集', '精简指令集', '流水线'], relatedPoints: ['co-5-5-1', 'co-4-1-1'],
    content: `## CISC vs RISC

| 特征 | CISC | RISC |
|------|------|------|
| 指令数量 | **多** | **少** |
| 指令长度 | **变长** | **定长** |
| 寻址方式 | 多 | 少 |
| 指令执行时间 | 差异大 | 大多单周期 |
| 流水线 | 难以实现 | **容易实现** |
| 编译 | 简单 | 复杂（需优化编译器） |
| **典型** | x86 | ARM、MIPS、RISC-V |

---

## 考试辨析
- RISC 指令定长 → 译码简单 → **适合流水线**
- CISC 变长 → 指令功能强 → 程序代码短
- 现代 CPU：x86 外部 CISC、内部微码转 RISC（融合两者）
`,
  },

  // ===========================================================
  // 第5章 中央处理器
  // ===========================================================

  // --- 5.1 CPU 的功能和基本结构 ---
  // --- 补充 ---
  {
    id: 'co-3-4-2', chapterId: 'co-3', title: '虚实地址转换全流程 (TLB+页表+Cache)',
    keyConcepts: ['TLB', '页表', 'Cache', '虚实地址', '缺页', '访存次数'], relatedPoints: ['co-3-4-1', 'co-3-3-1', 'co-3-3-2', 'os-3-3-2', 'os-3-4-1'],
    content: `## 完整地址转换流程
\`\`\`
CPU给出虚拟地址VA
  -> 查TLB(快表)
    命中? -> 得页框号 -> 物理地址PA
    未命中? -> 查页表(慢表,在内存中)
      命中? -> 得页框号 -> 更新TLB -> 物理地址PA
      未命中(缺页)? -> 缺页中断 -> OS调页 -> 更新页表+TLB
物理地址PA -> 查Cache
  命中? -> 得数据
  未命中? -> 访主存 -> 填Cache
\`\`\`

---
## 各场景访存次数
| 场景 | 访存次数 |
|------|---------|
| TLB命中 + Cache命中 | **1次** |
| 无TLB + 页表命中 | 2次(查页表+访存) |
| TLB缺失 + 页表命中 | 2次(查页表+更新TLB+访存...实际上慢表1次+访存1次=2) |
| TLB缺失 + 缺页 | 2次 + 缺页处理(磁盘I/O) |
| 多级页表命中TLB | 1次(TLB存完整多级页号) |

---
## TLB（快表）要点
- 本质=页表的Cache，SRAM实现，属于MMU
- 映射方式：通常全相联或组相联（直接映射会抖动）
- TLB表项：有效位 + Tag(虚页号) + 页框号 + 引用位 + 脏位
- **TLB命中 = 一定不发生缺页**（TLB是页表的副本）
- 缺页时对应TLB表项被无效化
- **进程切换 = TLB全部清空**

---
## Cache vs 虚拟存储器
| | Cache | 虚拟存储器 |
|------|-------|-----------|
| 目的 | 解决速度问题 | 解决容量问题 |
| 实现 | 纯硬件 | 硬件+OS软件 |
| 透明性 | 所有程序员透明 | 应用程序员透明 |
| 交换单位 | 块(较小) | 页(较大) |
| 写策略 | 均可 | 必须写回 |

## 考试辨析
- TLB命中 != Cache命中（两者独立，不相关）
- 页表命中 != Cache命中（两者独立）
- 缺页中断=内中断中的故障（返回重新执行当前指令）
- [[2011-大题44] [2015-大题44] [2018-大题44]]`,
  },

  // --- 补充 ---
  {
    id: 'co-4-2-2', chapterId: 'co-4', title: '程序的机器级代码表示',
    keyConcepts: ['机器代码', '汇编', 'AT&T', 'Intel', '栈帧', 'call', 'ret'], relatedPoints: ['co-4-1-1', 'co-4-2-1'],
    content: `## AT&T vs Intel 汇编格式
| | AT&T | Intel |
|------|------|-------|
| 操作数顺序 | src, dst | dst, src |
| 寄存器 | %eax | eax |
| 立即数 | $1 | 1 |
> 408考研采用AT&T格式(Unix/Linux)。

---
## 函数调用栈帧切换
调用者：push参数(右到左) -> call (压返回地址+跳转)
被调用者：push %ebp -> mov %esp,%ebp -> sub N,%esp
返回：leave -> ret (弹出返回地址到PC)
栈帧布局：参数 | 返回地址 | 旧ebp | 局部变量

## 考试辨析
- call = 压返回地址 + 跳转 (两步)
- ret = 弹出返回地址到PC (一步)
- 408近年不考汇编大题，选择题可能涉及`,
  },

  {
    id: 'co-5-1-1', chapterId: 'co-5', title: 'CPU的功能与基本结构',
    keyConcepts: ['CPU', '运算器', '控制器', 'PC', 'IR', 'MAR', 'MDR', '寄存器'], relatedPoints: ['co-2-4-1', 'co-4-1-1'],
    content: `## CPU 的两大组成部分

\`\`\`
CPU
├── 运算器
│   ├── ALU（算术逻辑单元）
│   ├── ACC（累加器）
│   ├── 通用寄存器组
│   └── 标志寄存器 (FR/PSW)
│
└── 控制器
    ├── PC（程序计数器）
    ├── IR（指令寄存器）
    ├── ID（指令译码器）
    └── CU（控制单元）
\`\`\`

---

## 关键寄存器
| 寄存器 | 全称 | 功能 |
|--------|------|------|
| **PC** | 程序计数器 | 存放下一条指令地址（取指后自动递增） |
| **IR** | 指令寄存器 | 存放当前正在执行的指令 |
| **MAR** | 存储器地址寄存器 | 存放要访问的存储单元地址 |
| **MDR** | 存储器数据寄存器 | 存放与存储器交换的数据 |

---

## 考试辨析
- MAR 和 MDR 在 CPU 内部（不是主存中）
- PC 取指后自动递增——具体增量取决于指令长度`,
  },

  // --- 5.2 数据通路 ---
  // --- 续 ---
  {
    id: 'co-5-1-3', chapterId: 'co-5', title: 'CPU寄存器与透明性',
    keyConcepts: ['寄存器', '用户可见', '透明', 'PC', 'IR', 'MAR', 'MDR', 'ACC', 'PSW', 'GPR'], relatedPoints: ['co-5-1-1'],
    content: `## 寄存器分类
| 寄存器 | 全称 | 用户可见 | 可修改 |
|--------|------|----------|--------|
| **PC** | 程序计数器 | ✅(间接) | 转移指令间接改 |
| **IR** | 指令寄存器 | ❌ 透明 | ❌ |
| **MAR** | 存储器地址寄存器 | ❌ 透明 | ❌ |
| **MDR** | 存储器数据寄存器 | ❌ 透明 | ❌ |
| **ACC** | 累加器 | ✅ | ✅ |
| **PSW/FR** | 程序状态字/标志寄存器 | ✅ | 硬件自动设 |
| **GPR** | 通用寄存器组 | ✅ | ✅ |
| **暂存器(Y/Z)** | ALU输入输出暂存 | ❌ 透明 | ❌ |

---
## 透明性的含义
> **对程序员透明** = 程序员写程序时不感知其存在，不能直接读写。
- 透明寄存器：MAR、MDR、IR、暂存器Y/Z
- 非透明寄存器：PC（跳转指令间接影响）、ACC、通用寄存器

---
## 考试辨析
- MAR/MDR 在**CPU内部**（不是主存的一部分！）
- PSW/FR 用户可读，但标志位由硬件自动设置——不能直接赋值
- 暂存器 ≠ ACC——暂存器透明，ACC不透明`,
  },

  // --- 续 ---
  {
    id: 'co-5-1-2', chapterId: 'co-5', title: '指令执行过程与数据流',
    keyConcepts: ['指令周期', '取指', '间址', '执行', '中断', '机器周期', '时钟周期'], relatedPoints: ['co-5-1-1'],
    content: `## 三个时间概念
| 概念 | 定义 | 关系 |
|------|------|------|
| **时钟周期(T)** | CPU最小时间单位 | — |
| **机器周期(CPU周期)** | 一次总线操作时间 | = N个时钟周期 |
| **指令周期** | 取指+执行一条指令总时间 | = 若干机器周期 |

---
## 指令周期四阶段
| 阶段 | 触发器 | 任务 |
|------|--------|------|
| **取指(FE)** | FE=1 | (PC)到MAR到M到MDR到IR, (PC)+1到PC |
| **间址(IND)** | IND=1 | 取操作数有效地址 |
| **执行(EX)** | EX=1 | 执行具体操作 |
| **中断(INT)** | INT=1 | 保存断点返回地址 |

---
## 考试辨析
- 取指操作是控制器固有功能——不需要操作码控制
- 间址周期不是每条指令都有
- PC 在无条件跳转指令周期内被修改2次`,
  },

  {
    id: 'co-5-2-1', chapterId: 'co-5', title: '数据通路',
    keyConcepts: ['数据通路', '总线结构', '专用通路', '单总线', '双总线', '三总线'], relatedPoints: ['co-5-1-1'],
    content: `## 数据通路的功能

> 数据通路是 CPU 中各部件之间数据流动的路径。

---

## 数据通路的结构
| 结构 | 特点 | 优缺点 |
|------|------|--------|
| **单总线** | 所有部件共享一条总线 | 结构简单，但**瓶颈明显** |
| **双总线** | 主存总线和 I/O 总线分离 | 提高吞吐 |
| **三总线** | 再加一条执行总线 | 更快 |
| **专用通路** | 各部件间直接连线 | 最快，但线路复杂 |

---

## 考试辨析
- 数据通路与 CPU 结构有关——408 可能考通路图分析
- 总线冲突：同一时刻只能有一个部件向总线发送数据`,
  },

  // --- 5.3 控制器 ---
  {
    id: 'co-5-3-1', chapterId: 'co-5', title: '硬布线控制器与微程序控制器',
    keyConcepts: ['硬布线', '微程序', '控制存储器', '微命令', '微操作'], relatedPoints: ['co-5-1-1', 'co-5-3-2', 'co-5-3-3'],
    content: `## 硬布线控制器

> 用组合逻辑电路产生控制信号——门电路直接实现。

| 优点 | 缺点 |
|------|------|
| 速度快 | 设计复杂，难修改 |

---

## 微程序控制器

> 将每条机器指令分解为一组微指令，存入**控制存储器 (CM)**。

| 优点 | 缺点 |
|------|------|
| 规整、易修改 | 慢（多了读 CM 的一层） |

---

## 对比
| | 硬布线 | 微程序 |
|------|--------|--------|
| 实现 | 组合逻辑 | 微指令序列 |
| 速度 | **快** | 慢 |
| 灵活性 | 差 | **好** |
| 适用 | RISC | CISC |

---

## 考试辨析
- 微程序控制器多了一层——每条指令 → 一段微程序 → 若干微指令 → 若干微命令
- RISC 通常采用硬布线（追求速度），CISC 常采用微程序（处理复杂指令）`,
  },

  // --- 5.4 异常和中断 ---
  // --- 续 ---
  {
    id: 'co-5-3-2', chapterId: 'co-5', title: '微指令的编码方式与格式',
    keyConcepts: ['直接编码', '字段直接编码', '字段间接编码', '水平型', '垂直型', '微地址形成'], relatedPoints: ['co-5-3-1'],
    content: `## 微指令编码三种方式
| 方式 | 特点 | 优缺点 |
|------|------|--------|
| **直接编码** | 1bit对应1个微命令 | 速度快并行好，但控存容量极大 |
| **字段直接编码** | 互斥命令分同一段，相容命令分不同段 | 缩短字长，408重点考 |
| **字段间接编码** | 一个字段含义需另一字段解释 | 进一步缩短，但并行性降低 |

### 字段直接编码分段原则
1. **互斥性**微命令放同一段，**相容性**微命令放不同段
2. 每段信息位不能太多（3位→最多7个互斥命令+1个空状态）
3. 每段必须留一个状态表示"不操作"

---
## 水平型 vs 垂直型微指令
| | 水平型 | 垂直型 |
|------|--------|--------|
| 长度 | 长 | 短 |
| 并行性 | 强（一次多个微命令） | 弱（一次一个） |
| 微程序长度 | 短 | 长 |
| 速度 | 快 | 慢 |
| 编写 | 麻烦 | 规整 |

---
## 微地址形成方式
1. 后继地址字段直接给出
2. 机器指令操作码经微地址形成部件产生
3. 增量计数器 (μPC)+1→μPC
4. 根据标志位分支转移
5. 硬件直接产生微程序入口地址(如开机0#)

---
## 考试辨析
- 水平型微指令并行性强——适合RISC；垂直型规整——适合CISC
- 微程序控制器比硬布线控制器慢——因为多了一层读CM
- 互斥微命令=不能在同一微指令中同时有效的命令`,
  },

  // --- 做题技巧 ---
  {
    id: 'co-5-5-2', chapterId: 'co-5', title: '流水线 常见题型与解题技巧',
    keyConcepts: ['流水线', '吞吐率', '加速比', '瓶颈段', '时钟周期'], relatedPoints: ['co-5-5-1'],
    content: `## 题型一：各段时间相等
T = (k + n - 1) x t
TP = n / T；S = nk / (k+n-1)

## 题型二：各段时间不等
**时钟周期 = max{各段时间}**（瓶颈段决定一切）
各段操作时间必须对齐到时钟周期的整数倍

## 题型三：加速比极限
当 n 趋近无穷大时，加速比趋近于 k（段数）
实际加速比受限于：段数k、瓶颈段占比、冲突开销

## 关键结论
- 流水线不缩短单条指令（反而因锁存器变长）
- 转发技术不会引起阻塞
- 超标量=多发射；超流水线=高频再分段`,
  },

  // --- 补充 ---
  {
    id: 'co-5-3-3', chapterId: 'co-5', title: '硬布线控制器设计方法',
    keyConcepts: ['硬布线', '组合逻辑', '微操作', '操作时间表', '布尔函数'], relatedPoints: ['co-5-3-1', 'co-5-3-2'],
    content: `## 硬布线设计六步
1. 列出各指令各周期的微操作序列
2. 选择CPU控制方式
3. 安排微操作时序 (三原则)
   原则一：先后顺序不可改
   原则二：不冲突的放同一节拍
   原则三：耗时短的尽量一个节拍完成
4. 列出操作时间表
5. 综合布尔函数: 微命令=f(操作码,机器周期,节拍,状态)
6. 画组合逻辑电路图

---
## 硬布线 vs 微程序
| | 硬布线 | 微程序 |
|------|--------|--------|
| 实现 | 组合逻辑门 | 控制存储器ROM |
| 速度 | 快 | 慢(多一层读CM) |
| 扩展性 | 难(改电路) | 易(改CM内容) |
| 适用 | RISC | CISC |

## 考试辨析
- 微程序控制器多一个节拍用于读控存CM
- 现代CPU常混合：简单指令硬布线+复杂指令微程序`,
  },

  {
    id: 'co-5-4-1', chapterId: 'co-5', title: '异常与中断',
    keyConcepts: ['异常', '中断', '内中断', '外中断', '中断向量', '中断响应', '中断返回'], relatedPoints: ['co-7-3-1', 'os-1-3-1'],
    content: `## 异常 vs 中断

| | 异常 (Exception) | 中断 (Interrupt) |
|------|-------------------|------------------|
| 来源 | **CPU 内部** | **CPU 外部** |
| 原因 | 指令执行过程中的错误 | 外部设备或时钟 |
| 例子 | 除 0、缺页、非法指令 | 键盘输入、DMA 完成 |
| 同义词 | 内中断 | 外中断 |

---

## 中断处理流程
1. 中断请求 → CPU 响应
2. 关中断（防止嵌套）
3. **保存断点**（PC + PSW 入栈）
4. 中断服务程序入口 → PC
5. 保存现场（寄存器入栈）
6. 执行中断服务程序
7. 恢复现场
8. 开中断
9. **中断返回**（IRET指令）

---

## 中断向量

> 中断向量 = 中断服务程序的入口地址。

**中断向量表**：放在内存固定位置，用中断类型号查表得到向量。

---

## 考试辨析
- 异常是**同步**的（与指令执行相关），中断是**异步**的（随时可能发生）
- 408 常考中断响应周期的操作（关中断、保存断点、取中断向量）`,
  },

  // --- 5.5 指令流水线 ---
  {
    id: 'co-5-5-1', chapterId: 'co-5', title: '指令流水线',
    keyConcepts: ['流水线', 'IF', 'ID', 'EX', 'MEM', 'WB', '吞吐率', '加速比', '数据冲突', '控制冲突', '转发', '分支预测'], relatedPoints: ['co-5-1-1', 'co-4-3-1', 'co-5-5-2'],
    content: `## 指令流水线的思想
> 将指令执行过程分为多个阶段，多条指令的不同阶段可以**并行**执行。

经典五段流水线：**IF（取指）→ ID（译码）→ EX（执行）→ MEM（访存）→ WB（写回）**

---
## 性能指标
设指令数为 $n$，流水线段数为 $k$，每段时间为 $\\tau$：
| 指标 | 公式 |
|------|------|
| 串行执行时间 | $nk\\tau$ |
| 流水执行时间 | **$(k + n - 1)\\tau$** |
| **吞吐率 (TP)** | $\\frac{n}{(k+n-1)\\tau}$ |
| **加速比 (S)** | $\\frac{nk}{k+n-1}$ |

当 $n \\gg k$ 时：$S \\to k$，$TP \\to 1/\\tau$（理想情况）

---
## 流水线冲突（三种）

### 1. 结构冲突 (Structural Hazard)
- 原因：硬件资源不够用（如只有一个存储器，IF 和 MEM 同时访存冲突）
- 解决：增加硬件资源（分离指令 Cache 和数据 Cache）

### 2. 数据冲突 (Data Hazard)
- 原因：后续指令依赖前一条指令的结果（还未写回）
- 类型：RAW(真相关)、WAR、WAW（后两者不出现在简单流水线中）
- 解决：
- **转发 (Forwarding / Bypassing)**：EX 阶段的结果直接转发给下一条指令的 EX
- **流水线停顿 (Stall / Bubble)**：插入 nop（气泡），直到数据可用
- **编译优化**：调整指令顺序，使依赖指令间隔 $\\ge 1$ 条

### 3. 控制冲突 (Control Hazard)
- 原因：分支指令导致 PC 不确定（下一条指令地址未知）
- 解决：
- **流水线停顿**：等待分支结果
- **分支预测**：静态（永远预测不跳转）/ 动态（基于历史）
- **延迟分支**：在分支指令后放入一条"延迟槽"指令（总是执行）

---
## 考试辨析
- 流水线**不缩短单条指令时间**——缩短的是指令间的**间隔**（吞吐率）
- **最长段的时间 = 流水线周期**（瓶颈段决定整体速度）
- 分段时间应尽量均衡——不均衡导致流水线效率低
- 转发可以解决大多数 RAW 冲突——但 Load-Use 冲突仍需停顿
- 流水线深度不是越深越好——深度增加 = 冲突增加 + 分支惩罚增大`
  },

  // ===========================================================
  // 第6章 总线
  // ===========================================================

  // --- 6.1 总线概述 ---
  {
    id: 'co-6-1-1', chapterId: 'co-6', title: '总线概述与分类',
    keyConcepts: ['总线', '数据总线', '地址总线', '控制总线', '系统总线', '局部总线', '通信总线'], relatedPoints: [],
    content: `## 总线的定义

> 总线是连接多个部件的信息传输线，各部件**共享**的传输介质。

---

## 按传输信息分类
| 总线 | 功能 | 方向 |
|------|------|------|
| **数据总线 (DB)** | 传输数据 | 双向 |
| **地址总线 (AB)** | 传输地址 | 单向（CPU→存储器） |
| **控制总线 (CB)** | 传输控制信号 | 各线方向不同 |

---

## 按连接对象分类
| 类型 | 连接 |
|------|------|
| 片内总线 | CPU 芯片内部 |
| 系统总线 | CPU、主存、I/O 接口之间 |
| 局部总线 | 高速设备之间 |
| 通信总线 | 计算机系统之间 |

---

## 考试辨析
- 总线是**共享**传输介质——同一时刻只能有一个设备发送
- 地址总线的位数决定**可寻址范围**（$n$ 根地址线 → $2^n$ 个地址）`,
  },

  // --- 6.2 总线仲裁 ---
  {
    id: 'co-6-2-1', chapterId: 'co-6', title: '总线仲裁',
    keyConcepts: ['链式查询', '计数器定时查询', '独立请求', '集中仲裁', '分布仲裁'], relatedPoints: [],
    content: `## 集中仲裁方式
| 方式 | 控制线数 | 优先级 | 优点 | 缺点 |
|------|---------|--------|------|------|
| **链式查询** | 3根(BR/BG/BS) | 固定(越近越高) | 简单易扩充 | 对故障敏感，优先级固定 |
| **计数器定时** | ⌈log2n⌉+2根 | 可编程 | 优先级可动态改变 | 控制较复杂 |
| **独立请求** | 2n+1根 | 最灵活 | 响应最快 | 控制线最多 |

---
## 链式查询详解
\`\`\`
BR(总线请求) ← 所有设备共享
BG(总线允许) → 链式串行传递
BS(总线忙)   ← 获权设备发出
\`\`\`
> **BS信号由获得总线控制权的设备发出**——不是总线控制器！

---
## 分布仲裁方式
- 无需中央仲裁器，每个设备有自己的仲裁号和仲裁逻辑
- 设备将仲裁号发到共享仲裁总线上进行比较
- 号最大（或最小）者获胜

---
## 考试辨析
- 链式查询的优先级由**物理位置**决定——离控制器越近越高
- 独立请求方式最灵活（优先级可编程），但线最多
-- 「[2011-选择19] [2015-选择20」`,
  },

  // --- 6.3 总线操作和定时 ---
  {
    id: 'co-6-3-1', chapterId: 'co-6', title: '总线操作与定时',
    keyConcepts: ['同步通信', '异步通信', '半同步', '分离式', '握手', '互锁'], relatedPoints: [],
    content: `## 总线传输四阶段
1. **申请分配** —— 总线仲裁，获得使用权
2. **寻址** —— 发送地址和命令
3. **传数** —— 数据交换（读/写）
4. **结束** —— 释放总线

---
## 四种通信定时方式
### 一、同步通信
- 统一时钟信号协调收发；每个时钟构成一个总线周期
- 优点：速度快、逻辑简单；缺点：可靠性差
- 适用：总线短、各部件速度接近

### 二、异步通信（握手应答）
| 互锁类型 | 请求 | 回答 | 速度 | 可靠性 |
|----------|------|------|------|--------|
| **不互锁** | 自动定时撤 | 自动定时撤 | 最快 | 最低 |
| **半互锁** | 等回答才撤 | 自动撤 | 中等 | 中等 |
| **全互锁** | 等回答才撤 | 等请求撤才撤 | 最慢 | 最高 |

### 三、半同步通信
- 同步时钟 + **WAIT等待信号**
- 快速设备可全速同步，慢速设备发WAIT让总线等

### 四、分离式通信
- 总线周期分为两子周期：主模块发→从模块发
- 各模块均有权申请总线——充分利用，适合多主设备系统

---
## 总线带宽计算（核心公式）
$$B = f \\times W$$
- $B$ = 总线带宽(Byte/s)，$f$ = 总线工作频率(Hz)，$W$ = 总线宽度(Byte)
- 总线工作频率 = 时钟频率 / 总线周期所需时钟数
---
## 考试辨析
- 同步通信可靠性差的原因：没有应答机制——发完就走
- 分离式通信的"分离"——把一个总线周期拆成两个子周期
- 带宽计算选择题常考——注意单位换算(bit→Byte)`,
  },

  // ===========================================================
  // 第7章 输入/输出系统
  // ===========================================================

  // --- 7.1 I/O 系统基本概念 ---
  // --- 6.4 总线标准 ---
  {
    id: 'co-6-4-1', chapterId: 'co-6', title: '总线标准',
    keyConcepts: ['PCI', 'PCI-E', 'USB', 'ISA', 'AGP', 'RS-232C', '即插即用', '热插拔'], relatedPoints: ['co-6-1-1'],
    content: `## 常见总线标准
| 标准 | 类型 | 宽度 | 频率 | 带宽 | 特点 |
|------|------|------|------|------|------|
| **ISA** | 并行 | 16位 | 8MHz | 16MB/s | 最早PC总线，已淘汰 |
| **PCI** | 并行 | 32/64位 | 33/66MHz | 132~528MB/s | **即插即用**，独立于CPU |
| **PCI-E** | **串行** | ×1/×4/×16 | — | 极高 | 点对点，取代PCI/AGP |
| **AGP** | 并行 | 32位 | 66MHz+ | 266~2133MB/s | 显卡专用 |
| **USB** | 串行 | — | — | — | 即插即用+**热插拔** |
| **RS-232C** | 串行 | — | — | — | 经典串行通信 |

---
## 关键概念
- **即插即用 (PnP)**：插入设备后系统自动识别和配置驱动程序
- **热插拔**：带电插拔设备而不损坏硬件（USB/PCI-E支持）
- **猝发传输 (Burst)**：一个地址+一批连续数据，减少地址传输开销

---
## 考试辨析
- PCI-E 是**串行**总线（高速差分信号对），不是并行
- 408 对总线标准要求较低，了解 ISA→PCI→PCI-E 的演进即可`,
  },

  {
    id: 'co-7-1-1', chapterId: 'co-7', title: 'I/O接口',
    keyConcepts: ['I/O接口', '端口', '数据端口', '状态端口', '控制端口', '编址'], relatedPoints: [],
    content: `## I/O 接口的功能
1. 数据缓冲（速度匹配）
2. 信号转换（电平、格式）
3. 中断请求
4. 设备选择

---

## 端口分类
| 端口 | 说明 |
|------|------|
| **数据端口** | 数据缓冲 |
| **状态端口** | 设备状态（忙/闲/错） |
| **控制端口** | 控制命令 |

---

## I/O 端口编址
| 方式 | 说明 |
|------|------|
| **独立编址** | I/O 端口有独立地址空间——需要专用 I/O 指令 |
| **统一编址** | I/O 端口与主存统一编址——用 Load/Store 访问 |

---

## 考试辨析
- 统一编址又称"存储器映射 I/O"——不需专用 I/O 指令
- 独立编址需要 IN/OUT 指令（x86 采用）`,
  },

  // --- 7.2 程序查询方式 ---
  {
    id: 'co-7-2-1', chapterId: 'co-7', title: '程序查询方式',
    keyConcepts: ['程序查询', '轮询', 'CPU等待', '独占'], relatedPoints: ['co-7-3-1', 'co-7-4-1'],
    content: `## 程序查询方式

> CPU 不断查询 I/O 设备状态——"忙等"。

\`\`\`
while (设备忙)
    ;  // 等待
送数据
\`\`\`

---

## 特点
| 特性 | 说明 |
|------|------|
| CPU 利用率 | **低**（一直在轮询等待） |
| 数据传送 | CPU 直接执行 |
| 优先级 | 取决于查询顺序 |
| 优点 | 实现简单 |

---

## 考试辨析
- 程序查询是**最简单的 I/O 方式**——CPU 与外设串行工作
- 不适合高速设备——CPU 被绑定等待`,
  },

  // --- 7.3 程序中断方式 ---
  // --- 7.3 程序中断方式（续）---
  {
    id: 'co-7-3-2', chapterId: 'co-7', title: '多重中断与中断屏蔽',
    keyConcepts: ['多重中断', '中断屏蔽', '屏蔽字', '中断嵌套', '响应优先级', '处理优先级'], relatedPoints: ['co-7-3-1', 'co-5-4-1'],
    content: `## 多重中断（中断嵌套）
> 执行中断服务程序期间，可响应**更高优先级**的新中断请求。

### 实现条件
1. 中断服务程序中提前设置**开中断**指令
2. 高优先级中断源有权打断低优先级的中断服务

---
## 中断屏蔽字 ⭐
> 每个中断源对应一个屏蔽字。'1'=屏蔽，'0'=允许。
> **屏蔽字中'1'越多，优先级越高**——能屏蔽更多其他中断。

### 示例
设有4个中断源 A>B>C>D（优先级从高到低）：
| 中断源 | 优先级 | 屏蔽字(ABCD) |
|--------|--------|-------------|
| A | 最高 | 1111 |
| B | 高 | 0111 |
| C | 低 | 0011 |
| D | 最低 | 0001 |
> 每个屏蔽字至少屏蔽自身（至少一个'1'）。

---
## 响应优先级 vs 处理优先级
| | 响应优先级 | 处理优先级 |
|------|----------|----------|
| 决定者 | 硬件排队器 | **中断屏蔽字** |
| 可变性 | 固定 | **可动态调整** |
| 作用阶段 | 中断响应时 | 中断处理时 |

---
## 中断处理完整流程
1. **硬件自动**：关中断→保存断点→取中断向量
2. **软件（服务程序）**：保护现场→设屏蔽字→**开中断**→执行服务→关中断→恢复现场→恢复屏蔽字→开中断→返回
> 开中断在保护现场**之后**——确保保护现场过程不被中断。

---
## 考试辨析
- 屏蔽字中'1'的数量 = 该中断源能屏蔽的中断数 + 1（自身）
- 响应优先级由硬件决定(不可改)，处理优先级由屏蔽字决定(可改)
- 这是408常考题型——给定中断源优先级，写出各屏蔽字`,
  },

  {
    id: 'co-7-3-1', chapterId: 'co-7', title: '程序中断方式',
    keyConcepts: ['中断', '中断请求', '中断响应', '中断向量', '中断优先级', '中断屏蔽'], relatedPoints: ['co-5-4-1', 'co-7-2-1', 'os-2-1-1', 'os-1-3-1'],
    content: `## 中断 I/O 的思想

> 外设准备好后**主动通知 CPU**——CPU 暂停当前程序，执行中断服务程序。

---

## 中断处理流程
1. 外设发出中断请求
2. CPU 当前指令结束后响应
3. 关中断
4. 保存断点和现场
5. 取中断向量 → 中断服务程序
6. 执行中断服务
7. 恢复现场 → 开中断 → 返回

---

## 中断优先级
| 方式 | 说明 |
|------|------|
| 硬件排队器 | 固定优先级（菊花链） |
| 软件查询 | 可编程优先级 |
| **中断屏蔽** | 动态改变优先级 |

---

## 考试辨析
- 与程序查询的关键区别：CPU 不需等待——**并行工作**
- 中断响应在**当前指令执行结束后**（不是当前总线周期）
- 多重中断：高优先级中断可以打断低优先级中断服务`,
  },

  // --- 7.4 DMA 方式 ---
  {
    id: 'co-7-4-1', chapterId: 'co-7', title: 'DMA方式',
    keyConcepts: ['DMA', 'DMAC', '周期窃取', 'DMA请求', '中断请求', 'AR', 'WC', 'DAR'], relatedPoints: ['co-7-3-1', 'co-6-1-1', 'os-5-1-2'],
    content: `## DMA 方式核心思想
> 在主存与外设间开辟**直接数据通路**，传送过程**不经CPU**。

---
## DMA 控制器 (DMAC) 组成
| 部件 | 全称 | 功能 |
|------|------|------|
| **AR** | 主存地址计数器 | 存放要交换的主存地址，每传一字自动+1 |
| **WC** | 传送长度计数器 | 剩余字数，WC溢出→传送完成→发中断 |
| **DAR** | 设备地址寄存器 | I/O设备地址 |
| **DR** | 数据缓冲寄存器 | 暂存每次传送的数据 |

---
## DMA 传送三阶段
| 阶段 | 执行者 | 操作 |
|------|--------|------|
| **预处理** | CPU | 设置AR/WC/DAR → 启动设备 → 继续执行其他程序 |
| **数据传送** | DMAC | 设备数据→DR→请求总线→获权→传一字→AR+1, WC-1 |
| **后处理** | CPU | WC溢出→中断请求→CPU执行DMA结束善后 |

---
## DMA 与 CPU 访存冲突解决
| 方式 | 原理 | CPU效率 |
|------|------|--------|
| **停止CPU访存** | DMA期间CPU完全让出总线 | 低 |
| **交替访存** | 时间片轮流(C1→DMA, C2→CPU) | 中 |
| **周期挪用** | DMA临时占用1个存取周期 | 高 |
> **访存优先级**：I/O > CPU（I/O不及时访存会丢数据）

---
## DMA vs 中断 全面对比 ⭐
| 对比维度 | 程序中断 | DMA |
|----------|----------|-----|
| 数据传送 | CPU执行程序 | **硬件(DMAC)** |
| 中断请求时机 | 每条指令执行结束 | **每个机器周期结束** |
| CPU干预 | 每传一字都要 | 仅开始+结束 |
| 优先级 | 低于DMA | **高于中断** |
| 适用场景 | 中低速字符设备 | **高速块设备(磁盘)** |
| 传送方式 | 每字一次中断 | 整块一次中断 |
> DMA请求的是**总线使用权**；中断请求的是**CPU处理时间**——两者不同！

---
## 考试辨析
- DMA的"周期窃取"——窃取的是**总线周期**，不是CPU时钟周期
- DMA请求 ≠ DMA中断请求——前者是请求传一字(总线)，后者是整块传完请求善后
- 预处理(RAM/ROM/Flash)和后处理(DMA中断)才需要CPU参与
-- 「[2010-大题44] [2012-大题44] [2015-大题44」`,
  },
];
