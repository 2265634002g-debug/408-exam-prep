import type { KnowledgePoint } from '@/types/knowledge';

export const dsKnowledgePoints: KnowledgePoint[] = [

  // ===========================================================
  // 第1章 绪论
  // ===========================================================

  // --- 1.1 数据结构的基本概念 ---
  {
    id: 'ds-1-1-1',
    chapterId: 'ds-1',
    title: '数据、数据元素、数据项与数据对象',
    keyConcepts: ['数据', '数据元素', '数据项', '数据对象', '数据结构'],
    relatedPoints: [],
    content: `## 基本术语定义

| 术语 | 定义 | 说明 |
|------|------|------|
| **数据** (Data) | 信息的载体，能够输入计算机并被计算机程序识别和处理的符号集合 | 包括数字、字符、图像、音频等 |
| **数据元素** (Data Element) | 数据的基本单位，通常作为一个整体进行考虑和处理 | 一个学生记录、图中的顶点 |
| **数据项** (Data Item) | 构成数据元素的不可分割的最小单位 | 学号、姓名、年龄 |
| **数据对象** (Data Object) | 具有相同性质的数据元素的集合，是数据的一个子集 | 整数集合、所有学生记录 |
| **数据结构** (Data Structure) | 相互之间存在一种或多种特定关系的数据元素的集合 | 线性表、二叉树、图 |

## 层次关系

$$
\\text{数据} \\supset \\text{数据对象} \\supset \\text{数据元素} \\supset \\text{数据项}
$$

## 考试辨析

- 数据元素是**数据的基本单位**，数据项是**数据的最小单位**
- 区分「数据」与「数据对象」：数据是全集，数据对象是子集
- 408 选择题中常见「以下说法正确的是/错误的是」的选项，需精确对应定义`,
  },
  {
    id: 'ds-1-1-2',
    chapterId: 'ds-1',
    title: '数据结构三要素',
    keyConcepts: ['逻辑结构', '存储结构', '数据运算', '三要素'],
    relatedPoints: ['os-3-1-1'],
    content: `## 数据结构由三要素组成

> **数据结构 = 逻辑结构 + 存储结构（物理结构）+ 数据的运算**

三要素缺一不可，共同构成一个完整的数据结构定义。

---

## 一、逻辑结构

**定义**：数据元素之间的逻辑关系，与数据的存储无关，独立于计算机。

| 类型 | 数据元素关系 | 举例 |
|------|-------------|------|
| **集合** | 无特定关系（同属一个集合） | 集合 |
| **线性结构** | 一对一 | 线性表、栈、队列、串、数组 |
| **树形结构** | 一对多 | 一般树、二叉树 |
| **图状/网状结构** | 多对多 | 有向图、无向图 |

> 线性结构与非线性结构的划分：集合、树、图属于**非线性结构**。

---

## 二、存储结构（物理结构）

**定义**：数据结构在计算机中的表示（映像），即用计算机语言实现逻辑结构。

| 存储方式 | 特点 | 优点 | 缺点 |
|----------|------|------|------|
| **顺序存储** | 逻辑相邻 → 物理相邻，连续空间 | 随机存取，存储密度高 | 外部碎片多 |
| **链式存储** | 通过指针连接，可不连续 | 无碎片，充分利用空间 | 指针额外占用空间 |
| **索引存储** | 附加索引表（关键字→地址） | 检索快 | 索引表额外占空间 |
| **散列存储** | 根据关键字直接计算地址 | 增删改查都快 | 冲突需额外处理 |

---

## 三、数据的运算

- **运算的定义**：针对**逻辑结构**，描述运算的功能（做什么）
- **运算的实现**：针对**存储结构**，描述运算的具体步骤（怎么做）

常见运算：插入、删除、查找、修改、排序、遍历。

---

## 核心辨析（408高频考点）

> **逻辑结构独立于存储结构**。
>
> 同一个逻辑结构可以采用不同的存储结构实现。
> 例如：线性表（逻辑结构）既可以用顺序表（顺序存储）实现，也可以用链表（链式存储）实现。
>
> 反过来：存储结构**不能独立于**逻辑结构而存在。`,
  },
  {
    id: 'ds-1-1-3',
    chapterId: 'ds-1',
    title: '数据类型与抽象数据类型 (ADT)',
    keyConcepts: ['数据类型', '原子类型', '结构类型', '抽象数据类型', 'ADT'],
    relatedPoints: [],
    content: `## 数据类型 (Data Type)

**定义**：一个**值的集合**和定义在此集合上的**一组操作**的总称。

| 类别 | 说明 | 举例 |
|------|------|------|
| **原子类型** | 不可再分的数据类型 | int（值的集合 + 加减乘除等运算） |
| **结构类型** | 可由原子类型组合而成 | struct、数组、类 |

---

## 抽象数据类型 (Abstract Data Type, ADT)

**定义**：从数学模型抽象出来的**逻辑结构**和**运算**，只关心"做什么"，不关心"怎么做"。

ADT 通过一个三元组定义：

$$
\\text{ADT} = (D, S, P)
$$

- $D$：数据元素集合
- $S$：$D$ 上的关系集合
- $P$：对 $D$ 的基本操作集合

> ADT 的核心特征：**封装** + **信息隐藏**。对外只暴露接口，不暴露实现细节。

## 考试辨析

- 数据类型关注「值 + 操作」；抽象数据类型关注「逻辑结构 + 运算定义」。
- ADT 是面向对象思想的理论基础，408 常在绪论选择题中考查其概念辨析。`,
  },

  // --- 1.2 算法和算法评价 ---
  {
    id: 'ds-1-2-1',
    chapterId: 'ds-1',
    title: '算法的基本概念与特性',
    keyConcepts: ['算法', '五大特性', '有穷性', '确定性', '可行性', '好算法的标准'],
    relatedPoints: [],
    content: `## 算法 (Algorithm) 的定义

算法是对特定问题求解步骤的一种描述，是指令的有限序列。

---

## 算法的五大特性（必须全部满足）

| 特性 | 含义 | 反例 |
|------|------|------|
| **有穷性** | 执行有限步后必然结束 | 死循环 |
| **确定性** | 每一条指令语义唯一、无二义性 | 自然语言描述 |
| **可行性** | 可以通过已实现的基本运算执行 | 不可计算的操作 |
| **输入** | 有 $0$ 个或多个输入（外部提供的数据） | — |
| **输出** | 至少 $1$ 个输出 | 无返回值的纯副作用代码 |

> **注意**：程序 ≠ 算法。程序可以不满足有穷性（如操作系统）。

---

## 好算法的四个目标

1. **正确性** — 能正确处理输入
2. **可读性** — 易于理解和维护
3. **健壮性** — 能处理非法输入
4. **高效率 + 低存储量** — 时间和空间复杂度尽可能优

---

## 考试辨析

- 「有穷性」是算法区别于程序的关键特征
- 确定性 ≠ 有限性 — 确定性指指令无歧义，有穷性指执行步骤有限`,
  },
  {
    id: 'ds-1-2-2',
    chapterId: 'ds-1',
    title: '算法的时间复杂度',
    keyConcepts: ['时间复杂度', '大O记号', '渐近分析', '最坏情况', '加法规则', '乘法规则'],
    relatedPoints: ['ds-1-2-3'],
    content: `## 定义

算法中所有语句的**执行次数（频度）之和**是问题规模 $n$ 的函数 $f(n)$。

> **时间复杂度** $T(n) = O(f(n))$ 表示算法的**渐近时间增长率**，即当 $n \\to \\infty$ 时，$T(n)$ 的增长不超过 $f(n)$ 的常数倍。

**严格数学定义**（大O记号）：

$$T(n) = O(f(n)) \\iff \\exists\\ C > 0,\\ n_0 > 0,\\ \\forall n \\ge n_0,\\ 0 \\le T(n) \\le C \\cdot f(n)$$

---

## 分析规则

### 加法规则
$$T(n) = T_1(n) + T_2(n) = O(\\max(f(n), g(n)))$$

### 乘法规则
$$T(n) = T_1(n) \\times T_2(n) = O(f(n) \\times g(n))$$

> 常见结论：若 $A(n) = a_m n^m + \\dots + a_0$ 是 $m$ 次多项式，则 $A(n) = O(n^m)$。

---

## 常见时间复杂度排序（牢记）

$$O(1) < O(\\log n) < O(n) < O(n\\log n) < O(n^2) < O(n^3) < O(2^n) < O(n!) < O(n^n)$$

---

## 计算技巧总结

| 代码模式 | 复杂度 | 分析方法 |
|----------|--------|----------|
| 单层循环 | $O(n)$ | 循环变量从 $1$ 到 $n$ 线性增长 |
| 变量指数增长 | $O(\\log n)$ | 如 i = i * 2，解 $2^t \\ge n \\Rightarrow t = O(\\log n)$ |
| 双重独立循环 | $O(n^2)$ | 内外层复杂度相乘 |
| 递归 $T(n) = T(n-1) + O(1)$ | $O(n)$ | 递推展开为 $n$ 次相加 |
| 递归 $T(n) = 2T(n/2) + n$ | $O(n\\log n)$ | 主定理第二类 |

> 408 选择题通常考**最坏情况复杂度**。分析时重点关注最深层循环内的**基本操作**执行次数。

---

## 历年真题示例

**（2011年）**
\`\`\`c
x = 2;
while (x < n/2)
    x = 2 * x;
\`\`\`
设执行 $t$ 次，有 $2 \\times 2^t < n/2 \\Rightarrow t < \\log_2 n - 2$，故时间复杂度为 $O(\\log n)$。

**（2012年）**
\`\`\`c
int fact(int n) {
    if (n <= 1) return 1;
    return n * fact(n - 1);
}
\`\`\`
递推：$T(n) = T(n-1) + O(1)$，展开得 $T(n) = n \\cdot O(1) = O(n)$。`,
  },
  {
    id: 'ds-1-2-3',
    chapterId: 'ds-1',
    title: '算法的空间复杂度',
    keyConcepts: ['空间复杂度', '原地工作', '递归空间'],
    relatedPoints: ['ds-1-2-2'],
    content: `## 定义

算法运行过程中所需的**额外辅助存储空间**是问题规模 $n$ 的函数。

$$S(n) = O(g(n))$$

> 空间复杂度只计入**辅助空间**，不计入存储输入数据本身的空间。

---

## 常见情形

| 情形 | 空间复杂度 | 说明 |
|------|-----------|------|
| **原地工作** | $O(1)$ | 所需辅助空间为常量，不随 $n$ 增长 |
| **一维辅助数组** | $O(n)$ | 如归并排序的临时数组 |
| **递归** | $O(\\text{递归深度})$ | 递归调用栈的深度 × 每层空间 |

---

## 递归空间复杂度分析

递归算法的空间复杂度 $=$ **递归深度** $\\times$ **每层所需辅助空间**。

> 例如：fact(n) 递归深度为 $n$，每层仅保存返回地址和参数（常量），故 $S(n) = O(n)$。

---

## 考试辨析

- 时间复杂度 vs 空间复杂度：时间优先是主流，408 常以"时间换空间"或"空间换时间"作为选项陷阱
- 「算法原地工作」特指辅助空间为 $O(1)$，不排除输入数据本身占用的空间`,
  },

  // ===========================================================
  // 第2章 线性表
  // ===========================================================

  // --- 2.1 线性表的定义和基本操作 ---
  {
    id: 'ds-2-1-1',
    chapterId: 'ds-2',
    title: '线性表的定义与特点',
    keyConcepts: ['线性表', '有限序列', '表头', '表尾', '直接前驱', '直接后继', '一对一'],
    relatedPoints: ['ds-2-1-2', 'ds-3-1-1'],
    content: `## 线性表的定义

> 线性表是具有**相同数据类型**的 $n$（$n \\ge 0$）个数据元素的**有限序列**。

记作：

$$L = (a_1, a_2, \\ldots, a_i, \\ldots, a_n)$$

其中 $a_1$ 是**表头**元素（唯一的第一元素），$a_n$ 是**表尾**元素（唯一的最后元素）。

---

## 逻辑特征（必考）

| 特征 | 说明 |
|------|------|
| **有限性** | 元素个数 $n$ 是有限的（$n$ 称为表长，$n=0$ 为空表） |
| **相同数据类型** | 每个数据元素 $a_i$ 具有相同的数据类型 |
| **顺序性** | 元素之间有先后次序（序列） |
| **一对一关系** | 除表头外，每个元素有且仅有一个**直接前驱**；除表尾外，每个元素有且仅有一个**直接后继** |

> 线性表是一种**逻辑结构**，表示元素之间一对一的相邻关系。

---

## 线性表中元素的位序

- $a_i$ 是第 $i$ 个元素，称 $i$ 为数据元素 $a_i$ 在线性表中的**位序**
- 位序从 **1** 开始（区别于数组下标从 0 开始！408 常在此设陷阱）

---

## 考试辨析

- **线性表 ≠ 顺序表**：线性表是逻辑结构，顺序表/链表是存储结构（实现方式）
- **位序从 1 开始**，数组下标从 0 开始——选择题常见陷阱
- $a_1$ 无前驱，$a_n$ 无后继——这两个边界条件经常在算法题中出现`,
  },
  {
    id: 'ds-2-1-2',
    chapterId: 'ds-2',
    title: '线性表的基本操作 (ADT)',
    keyConcepts: ['InitList', 'ListInsert', 'ListDelete', 'LocateElem', 'GetElem', 'ADT'],
    relatedPoints: ['ds-1-1-3', 'ds-2-1-1'],
    content: `## 线性表 ADT 定义

线性表上的基本操作是理解后续章节（顺序表 / 链表）实现的基础。

---

## 核心操作一览

| 操作 | 函数原型 | 说明 |
|------|----------|------|
| **初始化** | InitList(&L) | 构造一个空的线性表，分配内存空间 |
| **销毁** | DestroyList(&L) | 销毁线性表，释放其所占内存 |
| **判空** | ListEmpty(L) | 若 $L$ 为空表返回 true |
| **求长** | ListLength(L) | 返回 $L$ 中数据元素个数 |
| **按位查找** | GetElem(L, i, &e) | 用 $e$ 返回 $L$ 中第 $i$ 个元素 |
| **按值查找** | LocateElem(L, e) | 返回 $L$ 中第一个值为 $e$ 的元素的位序 |
| **插入** | ListInsert(&L, i, e) | 在 $L$ 的第 $i$ 个位置插入新元素 $e$，表长 $+1$ |
| **删除** | ListDelete(&L, i, &e) | 删除 $L$ 中第 $i$ 个元素，用 $e$ 返回其值，表长 $-1$ |
| **输出** | PrintList(L) | 按前后顺序输出 $L$ 中所有元素值 |

---

## 操作设计要点

- **插入/删除的合法位置**：插入 $1 \\le i \\le n + 1$，删除 $1 \\le i \\le n$
- **参数传递**：修改线性表的操作（Insert/Delete）需用**引用 &** 传参
- **抽象性**：ADT 只定义"做什么"，不规定"怎么做"（具体实现取决于存储结构）

---

## 考试辨析

- 408 算法设计题通常基于这些基本操作组合实现更复杂的操作（如逆置、归并）
- 选择题常问"某操作的合法参数范围"`,
  },

  // --- 2.2 线性表的顺序表示 ---
  {
    id: 'ds-2-2-1',
    chapterId: 'ds-2',
    title: '顺序表的定义与存储结构',
    keyConcepts: ['顺序表', '随机存取', '存储密度', '连续空间', 'LOC公式'],
    relatedPoints: ['ds-1-1-2', 'ds-2-2-2'],
    content: `## 顺序表的定义

> 顺序表是用一组**地址连续**的存储单元依次存储线性表中的数据元素，使得逻辑上相邻的元素在物理位置上也相邻。

---

## 存储地址计算公式（核心公式）

设首地址为 $\\text{LOC}(a_1)$，每个元素占用 $s$ 个存储单元，则第 $i$ 个元素的存储地址：

$$\\text{LOC}(a_i) = \\text{LOC}(a_1) + (i - 1) \\times s$$

> **关键性质**：由此公式可知，顺序表支持**随机存取**——访问任意位置元素的时间复杂度为 $O(1)$。

---

## 实现方式对比

| 方式 | 特点 | 适用场景 |
|------|------|----------|
| **静态分配** | 数组大小固定（编译时确定），空间不可扩展 | 规模已知且不变 |
| **动态分配** | 用 malloc 申请，realloc 扩展，空间不够时可扩大 | 通用场景 |

\`\`\`c
// 静态分配
#define MaxSize 50
typedef struct {
    ElemType data[MaxSize];
    int length;
} SqList;

// 动态分配
typedef struct {
    ElemType *data;
    int length, MaxSize;
} SeqList;
\`\`\`

---

## 顺序表的特点

| 特点 | 说明 |
|------|------|
| **随机存取** | $O(1)$ 时间内访问任意位置元素 |
| **存储密度高** | 存储密度 $=1$，结点只存储数据，不存储指针 |
| **插入删除慢** | 需移动大量元素，平均移动约一半元素 |
| **空间利用率** | 需预分配，可能浪费空间或空间不足 |

> **存储密度** $= \\frac{\\text{数据元素本身占用的存储量}}{\\text{结点结构占用的存储总量}}$

---

## 考试辨析

- 「顺序表支持随机存取」是顺序表与链表最本质的区别之一
- 存储密度 $=1$ 仅对"结点只含数据"成立（若结点含其他字段则 $<1$）
- 区分「线性表」「有序表」和「顺序表」：有序表是逻辑概念（元素按值有序），顺序表是存储概念`,
  },
  {
    id: 'ds-2-2-2',
    chapterId: 'ds-2',
    title: '顺序表的插入操作',
    keyConcepts: ['插入', '移动元素', '平均移动次数', '时间复杂度O(n)'],
    relatedPoints: ['ds-2-2-3', 'ds-2-2-4'],
    content: `## 插入操作

在线性表 $L$ 的第 $i$（$1 \\le i \\le n+1$）个位置插入新元素 $e$。

---

## 算法步骤

1. 判断插入位置 $i$ 是否合法（$1 \\le i \\le n+1$）
2. 判断存储空间是否已满（满则不能插入）
3. 将第 $n$ 个到第 $i$ 个元素**依次向后移动一位**
4. 将新元素 $e$ 放入第 $i$ 个位置
5. 表长 $+1$

\`\`\`c
bool ListInsert(SqList *L, int i, ElemType e) {
    if (i < 1 || i > L->length + 1) return false;
    if (L->length >= MaxSize) return false;
    for (int j = L->length; j >= i; j--)
        L->data[j] = L->data[j - 1];
    L->data[i - 1] = e;
    L->length++;
    return true;
}
\`\`\`

---

## 时间复杂度分析

| 插入位置 | 移动元素个数 | 概率 |
|----------|-------------|------|
| 表尾（$i = n+1$） | $0$ | $\\frac{1}{n+1}$ |
| 表头（$i = 1$） | $n$ | $\\frac{1}{n+1}$ |
| 第 $i$ 个位置 | $n - i + 1$ | $\\frac{1}{n+1}$ |

> **平均移动次数**（等概率假设）：
>
> $$E_{\\text{insert}} = \\sum_{i=1}^{n+1} \\frac{1}{n+1} \\cdot (n-i+1) = \\frac{n}{2}$$

| 复杂度类型 | 值 |
|------------|-----|
| 最好情况 | $O(1)$ — 在表尾插入 |
| 最坏情况 | $O(n)$ — 在表头插入 |
| 平均情况 | $O(n)$ — 平均移动 $n/2$ 个元素 |

---

## 考试辨析

- 插入位置 $i$ 的合法范围是 **$1 \\le i \\le n+1$**（可在表尾后一个位置插入）
- 移动元素 **从后往前** 遍历（$j$ 从 $n$ 到 $i$），若从前往后会覆盖未移动的值`,
  },
  {
    id: 'ds-2-2-3',
    chapterId: 'ds-2',
    title: '顺序表的删除操作',
    keyConcepts: ['删除', '移动元素', '平均移动次数', '时间复杂度O(n)'],
    relatedPoints: ['ds-2-2-2', 'ds-2-2-4'],
    content: `## 删除操作

删除线性表 $L$ 的第 $i$（$1 \\le i \\le n$）个位置的元素，用 $e$ 返回其值。

---

## 算法步骤

1. 判断删除位置 $i$ 是否合法（$1 \\le i \\le n$）
2. 将被删元素的值赋给 $e$
3. 将第 $i+1$ 个到第 $n$ 个元素**依次向前移动一位**
4. 表长 $-1$

\`\`\`c
bool ListDelete(SqList *L, int i, ElemType *e) {
    if (i < 1 || i > L->length) return false;
    *e = L->data[i - 1];
    for (int j = i; j < L->length; j++)
        L->data[j - 1] = L->data[j];
    L->length--;
    return true;
}
\`\`\`

---

## 时间复杂度分析

| 删除位置 | 移动元素个数 | 概率 |
|----------|-------------|------|
| 表尾（$i = n$） | $0$ | $\\frac{1}{n}$ |
| 表头（$i = 1$） | $n-1$ | $\\frac{1}{n}$ |
| 第 $i$ 个位置 | $n - i$ | $\\frac{1}{n}$ |

> **平均移动次数**（等概率假设）：
>
> $$E_{\\text{delete}} = \\sum_{i=1}^{n} \\frac{1}{n} \\cdot (n-i) = \\frac{n-1}{2}$$

| 复杂度类型 | 值 |
|------------|-----|
| 最好情况 | $O(1)$ — 删除表尾 |
| 最坏情况 | $O(n)$ — 删除表头 |
| 平均情况 | $O(n)$ — 平均移动 $(n-1)/2$ 个元素 |

---

## 考试辨析（插入 vs 删除移动次数对比）

| 比较 | 插入 | 删除 |
|------|------|------|
| 合法范围 | $1 \\le i \\le n+1$ | $1 \\le i \\le n$ |
| 移动方向 | **从后往前** | **从前往后** |
| 第 $i$ 个位置移动个数 | $n - i + 1$ | $n - i$ |
| 平均移动次数 | $n / 2$ | $(n-1) / 2$ |

> 选择题常直接要求计算：给定 $n$ 和 $i$，计算移动的元素个数。`,
  },
  {
    id: 'ds-2-2-4',
    chapterId: 'ds-2',
    title: '顺序表的查找操作',
    keyConcepts: ['按位查找', '按值查找', '顺序查找', '随机存取', 'O(1)', 'O(n)'],
    relatedPoints: ['ds-7-2-1', 'ds-2-2-1'],
    content: `## 顺序表的两种查找

---

## 一、按位查找（随机存取）

获取顺序表 $L$ 中第 $i$ 个位置的元素值。

\`\`\`c
bool GetElem(SqList L, int i, ElemType *e) {
    if (i < 1 || i > L.length) return false;
    *e = L.data[i - 1];
    return true;
}
\`\`\`

> **时间复杂度：$O(1)$** — 利用 LOC 公式直接计算出存储地址，这是顺序表最大的优势。

---

## 二、按值查找（顺序查找）

在顺序表 $L$ 中查找第一个值为 $e$ 的元素，返回其位序。

\`\`\`c
int LocateElem(SqList L, ElemType e) {
    for (int i = 0; i < L.length; i++)
        if (L.data[i] == e)
            return i + 1;    // 位序从1开始
    return 0;                // 查找失败
}
\`\`\`

---

## 按值查找时间复杂度

| 查找位置 | 比较次数 | 概率 |
|----------|---------|------|
| 第 1 个 | $1$ | $\\frac{1}{n}$ |
| 第 $n$ 个 | $n$ | $\\frac{1}{n}$ |
| 查找失败 | $n$ | — |

> 平均查找长度（等概率）：
>
> $$\\text{ASL}_{\\text{成功}} = \\frac{n+1}{2}$$

| 复杂度类型 | 值 |
|------------|-----|
| 最好情况 | $O(1)$ |
| 最坏情况 | $O(n)$ |
| 平均情况 | $O(n)$ |

---

## 考试辨析

- **按位查找 $O(1)$ 是顺序表的核心优势**，408 选择题经常以此为干扰项对比链表
- 按值查找的 ASL = (n+1)/2，若包含查找失败则需另计
- 若顺序表**有序**，则按值查找可优化为**折半查找** $O(\\log n)$（见第7章）`,
  },

  // --- 2.3 线性表的链式表示 ---
  {
    id: 'ds-2-3-1',
    chapterId: 'ds-2',
    title: '单链表的定义与结点结构',
    keyConcepts: ['单链表', '结点', '数据域', '指针域', '头指针', '头结点'],
    relatedPoints: ['ds-2-2-1', 'ds-2-3-2'],
    content: `## 单链表结点结构

> 单链表的每个结点包含两部分：**数据域 (data)** 和**指针域 (next)**，指针域指向直接后继结点。

\`\`\`c
typedef struct LNode {
    ElemType data;           // 数据域
    struct LNode *next;      // 指针域：指向直接后继
} LNode, *LinkList;
\`\`\`

---

## 核心概念辨析（高频考点）

| 术语 | 含义 | 说明 |
|------|------|------|
| **头指针 (head)** | 指向链表第一个结点的指针 | 链表必须通过头指针访问 |
| **头结点** | 在首元结点之前附加的一个结点 | 数据域不放内容（或存放表长），指针域指向首元结点 |
| **首元结点** | 链表中存储第一个数据元素 $a_1$ 的结点 | 实际数据的开端 |
| **头指针 → 头结点 → 首元结点 → ...** | — | 标准的带头结点链表结构 |

---

## 带头结点 vs 不带头结点

**408 考研绝大多数题目使用带头结点的链表。**

| 特性 | 带头结点 | 不带头结点 |
|------|----------|------------|
| 判空条件 | L->next == NULL | L == NULL |
| 首元结点操作 | 与其它结点**统一处理** | 需**特殊处理** |
| 插入/删除代码 | 统一，简洁 | 在表头需特殊判断 |

> **头结点存在的意义**：使空表和非空表的操作统一，简化代码。

---

## 链表特点

| 方面 | 特点 |
|------|------|
| **存取方式** | 顺序存取（必须从头遍历），不支持随机存取 |
| **存储密度** | $< 1$（需额外存放指针域） |
| **存储空间** | 不需要连续空间，动态分配，灵活 |
| **插入/删除** | 已知前驱结点时只需修改指针，$O(1)$ |

---

## 考试辨析

- 「链表是否为空」的判断条件取决于**是否带头结点**——408 选择题常以此设陷阱
- 头指针 ≠ 头结点：头结点是实际存在的结点（分配了空间），头指针只是一个指针变量`,
  },
  {
    id: 'ds-2-3-2',
    chapterId: 'ds-2',
    title: '单链表的基本操作（插入与删除）',
    keyConcepts: ['插入', '删除', '前插', '后插', '先连后断', '查找前驱'],
    relatedPoints: ['ds-2-3-1', 'ds-2-3-3'],
    content: `## 单链表的插入操作

### 后插操作（已知插入位置的前驱结点 $p$）

在结点 $p$ **之后**插入新结点 $s$：

\`\`\`c
s->next = p->next;   // (1) 先连后继
p->next = s;          // (2) 再断前驱链
\`\`\`

> **口诀：先连后断！** 顺序不能反。若先执行 p->next = s，则原来 $p$ 的后继结点地址丢失。

时间复杂度：**$O(1)$**

---

### 前插操作（在结点 $p$ 之前插入）

由于单链表只能向后遍历，无法直接获取 $p$ 的前驱，有两种方法：

**方法一**：从头遍历找到 $p$ 的前驱，再做后插 — $O(n)$

**方法二（巧法）**：在 $p$ 之后插入新结点 $s$，然后**交换 $s$ 和 $p$ 的数据域** — $O(1)$

\`\`\`c
// 方法二：时间复杂度 O(1)，无需查找前驱
s->next = p->next;
p->next = s;
// 交换数据域
swap(s->data, p->data);
\`\`\`

---

## 单链表的删除操作

### 删除结点 $p$ 的后继结点 $q$

\`\`\`c
q = p->next;
p->next = q->next;   // 绕过结点 q
free(q);              // 释放内存
\`\`\`

时间复杂度：**$O(1)$**

### 删除结点 $p$ 本身

由于单链表无法回溯到 $p$ 的前驱，有两种方法：

**方法一**：从头遍历找到 $p$ 的前驱 — $O(n)$

**方法二（巧法）**：将后继结点 $q$ 的数据复制到 $p$，再删除 $q$ — $O(1)$

\`\`\`c
q = p->next;
p->data = q->data;    // 复制数据
p->next = q->next;    // 绕过 q
free(q);
\`\`\`

> **注意**：方法二**不能删除最后一个结点**（$p$ 无后继时无法使用）。

---

## 查找操作

| 操作 | 时间复杂度 | 说明 |
|------|-----------|------|
| 按位查找 | $O(n)$ | 从头遍历 $i$ 次 |
| 按值查找 | $O(n)$ | 从头遍历比较 |
| 求表长 | $O(n)$ | 需遍历整个链表 |

---

## 考试辨析

- 插入/删除语句的**先后顺序**是必考题（先连后断原则）
- 巧法的适用条件（需要有后继结点）是易错点
- 单链表不支持从后往前的遍历，凡是需要前驱的操作代价较高`,
  },
  {
    id: 'ds-2-3-3',
    chapterId: 'ds-2',
    title: '单链表的建立（头插法与尾插法）',
    keyConcepts: ['头插法', '尾插法', '逆序', '原序', '尾指针'],
    relatedPoints: ['ds-2-3-2', 'ds-3-1-1'],
    content: `## 头插法建立单链表

每次将新结点插入到**头结点之后**（即作为新的首元结点）。

\`\`\`c
LinkList List_HeadInsert(LinkList *L) {
    LNode *s;
    ElemType x;
    *L = (LinkList)malloc(sizeof(LNode));
    (*L)->next = NULL;    // 初始化空表
    scanf("%d", &x);
    while (x != 9999) {   // 以 9999 结束
        s = (LNode*)malloc(sizeof(LNode));
        s->data = x;
        s->next = (*L)->next;
        (*L)->next = s;
        scanf("%d", &x);
    }
    return *L;
}
\`\`\`

> **头插法结果**：链表中结点的顺序与输入顺序**相反**（逆序）。

---

## 尾插法建立单链表

每次将新结点插入到**链表尾部**，需要维护一个**尾指针** $r$。

\`\`\`c
LinkList List_TailInsert(LinkList *L) {
    LNode *s, *r;   // r 始终指向尾结点
    ElemType x;
    *L = (LinkList)malloc(sizeof(LNode));
    (*L)->next = NULL;
    r = *L;          // r 初始指向头结点
    scanf("%d", &x);
    while (x != 9999) {
        s = (LNode*)malloc(sizeof(LNode));
        s->data = x;
        r->next = s;
        r = s;        // r 移到新的尾结点
        scanf("%d", &x);
    }
    r->next = NULL;   // 尾结点指针域置空
    return *L;
}
\`\`\`

> **尾插法结果**：链表中结点的顺序与输入顺序**相同**（原序）。

---

## 头插法 vs 尾插法对比

| 方法 | 结果顺序 | 额外辅助 | 时间复杂度 |
|------|----------|----------|-----------|
| **头插法** | 逆序 | 无 | $O(n)$ |
| **尾插法** | 原序 | 尾指针 $r$ | $O(n)$ |

---

## 考试辨析

- 头插法的时间复杂度为 $O(n)$，**不是** $O(1)$（每次都要创建结点并插入）
- 头插法建表产生逆序 → 可以利用此性质实现**链表逆置**
- 尾插法建表保持原序 → 更适合读入数据后保持原有顺序的场景`,
  },
  {
    id: 'ds-2-3-4',
    chapterId: 'ds-2',
    title: '双链表',
    keyConcepts: ['双链表', 'prior指针', '双向遍历', '前驱', '后继'],
    relatedPoints: ['ds-2-3-1', 'ds-2-3-2', 'ds-2-3-5'],
    content: `## 双链表结点结构

> 双链表结点有两个指针域：**prior**（指向前驱）和 **next**（指向后继）。

\`\`\`c
typedef struct DNode {
    ElemType data;
    struct DNode *prior, *next;
} DNode, *DLinkList;
\`\`\`

---

## 双链表的插入操作

在结点 $p$ **之后**插入结点 $s$：

\`\`\`c
// 4 步操作，顺序很重要！
s->next = p->next;               // (1)
if (p->next != NULL)
    p->next->prior = s;          // (2)
s->prior = p;                    // (3)
p->next = s;                     // (4)
\`\`\`

时间复杂度：**$O(1)$**

---

## 双链表的删除操作

删除结点 $p$ 的后继结点 $q$：

\`\`\`c
p->next = q->next;               // (1)
if (q->next != NULL)
    q->next->prior = p;          // (2)
free(q);
\`\`\`

时间复杂度：**$O(1)$**

---

## 双链表 vs 单链表

| 特性 | 单链表 | 双链表 |
|------|--------|--------|
| 结点指针数 | 1 个 (next) | 2 个 (prior + next) |
| 查找前驱 | $O(n)$ — 必须从头遍历 | $O(1)$ — prior 指针直达 |
| 存储密度 | 较高 | 较低（多一个指针） |
| 插入/删除（已知结点 p） | 删除 p 本身需 $O(n)$ | 全部 $O(1)$ |
| 判空（带头结点） | L->next == NULL | L->next == NULL && L->prior == NULL |

> 双链表**以空间换时间**：多一个 prior 指针，换取所有操作都能在 $O(1)$ 完成。

---

## 考试辨析

- 双链表插入删除时的指针修改顺序是常考题——**先处理新结点/被删结点的指针，再处理周围结点的指针**
- 表尾边界条件：插入/删除时若 $p$ 是尾结点，其 p->next == NULL，需做特殊判断`,
  },
  {
    id: 'ds-2-3-5',
    chapterId: 'ds-2',
    title: '循环链表与静态链表',
    keyConcepts: ['循环单链表', '循环双链表', '静态链表', '游标', '判空条件'],
    relatedPoints: ['ds-2-3-1', 'ds-2-3-4'],
    content: `## 循环单链表

> 尾结点的 next 指针指向**头结点**（而非 NULL），整个链表形成一个环。

| 特性 | 说明 |
|------|------|
| 判空条件 | L->next == L（头结点的 next 指向自己） |
| 访问尾结点 | 若**只设头指针**，访问尾结点需 $O(n)$ |
| 优化方案 | **只设尾指针**（不设头指针），则访问表头和表尾都是 $O(1)$ |

---

## 循环双链表

> 头结点的 prior 指向尾结点，尾结点的 next 指向头结点。

| 特性 | 说明 |
|------|------|
| 判空条件 | L->next == L && L->prior == L |
| 对称性 | 对任意结点 $p$：p->next->prior == p && p->prior->next == p（空表也成立） |

---

## 静态链表

> 借助**数组**来描述线性表的链式存储结构，用**游标 (cur)** 代替指针。

\`\`\`c
#define MaxSize 50
typedef struct {
    ElemType data;
    int cur;    // 游标：存放下一个结点的数组下标
} SLinkList[MaxSize];
\`\`\`

---

## 静态链表特点

| 特点 | 说明 |
|------|------|
| **不需移动元素** | 插入/删除只需修改游标，不需要移动元素 |
| **仍需连续空间** | 整个存储空间是数组（连续），但逻辑结构是链式的 |
| **不支持随机存取** | 只能从头开始通过游标遍历 |
| **适用场景** | 早期没有指针的语言（如 Basic、Fortran）；操作系统中的文件分配表 |

> **与顺序表的关键区别**：静态链表插入/删除**不需要移动元素**，只需要修改游标。408 选择题常以此辨析。

---

## 考试辨析

- **循环链表判空条件**是高频考点：
- 循环单链表：L->next == L
- 循环双链表：L->next == L && L->prior == L
- 静态链表插入删除"不移动元素"是常见干扰选项的考点`,
  },
  {
    id: 'ds-2-3-6',
    chapterId: 'ds-2',
    title: '顺序表与链表的比较',
    keyConcepts: ['顺序表', '链表', '对比', '存取方式', '存储密度', '选择依据'],
    relatedPoints: ['ds-2-2-1', 'ds-2-3-1', 'ds-2-3-4'],
    content: `## 综合对比

| 比较维度 | 顺序表 | 链表 |
|----------|--------|------|
| **存取方式** | **随机存取** $O(1)$ | **顺序存取** $O(n)$ |
| **逻辑/物理结构** | 逻辑相邻 → 物理相邻 | 逻辑相邻 → 指针相连（物理可不邻） |
| **按位查找** | $O(1)$ | $O(n)$ |
| **按值查找** | $O(n)$ | $O(n)$ |
| **插入/删除** | $O(n)$（移动元素） | $O(1)$（已知结点指针时改指针） |
| **存储密度** | $= 1$（高） | $< 1$（低，需存指针） |
| **空间分配** | 静态或动态预分配，需要连续空间 | 只需分配单个结点，灵活 |
| **缓存友好** | 好（连续存储，空间局部性好） | 差（结点分散，缓存缺失多） |

---

## 选择依据

### 基于存储考虑
- 难以估计规模 → **链表**
- 存储密度要求高 → **顺序表**

### 基于运算考虑
- 经常按位存取 → **顺序表**
- 经常插入/删除 → **链表**

### 基于环境考虑
- 实现简单 → **顺序表**
- 空间灵活 → **链表**

---

## 考试辨析

- **「顺序表 = 逻辑结构」是常见错误**：顺序表既是逻辑结构又是存储结构，而链表描述的是存储结构
- **时间复杂度陷阱**：链表插入/删除是 $O(1)$ **前提是已知插入/删除位置的结点指针**。若需先查找位置，则总时间是查找 $O(n)$ + 插入 $O(1) = O(n)$
- 在实际工程中，顺序表因缓存友好（cache-friendly）而常常比链表性能更好——这是 408 选择题常见选项`,
  },

  // ===========================================================
  // 第3章 栈、队列和数组
  // ===========================================================

  // --- 3.1 栈 ---
  {
    id: 'ds-3-1-1',
    chapterId: 'ds-3',
    title: '栈的定义与基本操作',
    keyConcepts: ['栈', 'LIFO', '后进先出', '栈顶', '栈底', 'Push', 'Pop'],
    relatedPoints: ['ds-2-1-1', 'ds-2-1-2'],
    content: `## 栈的定义

> 栈 (Stack) 是**只允许在一端**进行插入和删除操作的**线性表**。

| 术语 | 含义 |
|------|------|
| **栈顶 (Top)** | 允许插入和删除的那一端 |
| **栈底 (Bottom)** | 不允许插入和删除的另一端 |
| **空栈** | 不含任何元素的栈 |

---

## 栈的特点：LIFO

> **后进先出 (Last In First Out)** — 最后入栈的元素最先出栈。

类比羽毛球筒：只能从筒口放入/取出羽毛球。

---

## 栈的基本操作

| 操作 | 函数原型 | 说明 |
|------|----------|------|
| **初始化** | InitStack(&S) | 构造一个空栈 |
| **判空** | StackEmpty(S) | 若栈空返回 true |
| **入栈** | Push(&S, x) | 将 $x$ 压入栈顶 |
| **出栈** | Pop(&S, &x) | 弹出栈顶元素，用 $x$ 返回 |
| **读栈顶** | GetTop(S, &x) | 读取栈顶元素但不删除 |

---

## 栈与线性表的关系

> **栈是操作受限的线性表**。限制：只能在栈顶操作。

- 线性表可以在任意位置插入/删除
- 栈只能在栈顶插入（Push）/ 删除（Pop）

---

## n 个元素的不同出栈序列数 — 卡特兰数（高频考点）

$$C_n = \\\\frac{1}{n+1} \\\\binom{2n}{n} = \\\\frac{(2n)!}{(n+1)!\\\\ n!}$$

| n | 出栈序列数 |
|---|----------|
| 3 | 5 |
| 4 | 14 |
| 5 | 42 |

---

## 考试辨析

- 入栈序列为 $1,2,\\\\ldots,n$，出栈序列中**每个元素之后小于它的元素必须降序排列**（合法出栈序列的判定条件）
- **卡特兰数**是408常考公式，$n=3$ 时出栈序列数为 5
- 栈是线性表——只不过操作位置被限制在一端`,
  },
  {
    id: 'ds-3-1-2',
    chapterId: 'ds-3',
    title: '顺序栈的实现',
    keyConcepts: ['顺序栈', 'top指针', '入栈', '出栈', '判空', '判满'],
    relatedPoints: ['ds-2-2-1', 'ds-3-1-1', 'ds-3-1-3'],
    content: `## 顺序栈的存储结构

> 使用一组地址连续的存储单元存放从栈底到栈顶的数据元素。

\`\`\`c
#define MaxSize 50
typedef struct {
    ElemType data[MaxSize];
    int top;          // 栈顶指针
} SqStack;
\`\`\`

---

## 两种 top 指针定义（必须区分！）

### 方式一：top = -1 初始化（408 主流方式）

| 操作 | 代码 | 说明 |
|------|------|------|
| 初始化 | \`S.top = -1\` | 栈空时 top = -1 |
| 判空 | \`S.top == -1\` | |
| 判满 | \`S.top == MaxSize - 1\` | |
| 入栈 | \`S.data[++S.top] = x\` | 先 +1，再赋值 |
| 出栈 | \`x = S.data[S.top--]\` | 先取值，再 -1 |
| 栈顶元素 | \`S.data[S.top]\` | |
| 元素个数 | \`S.top + 1\` | |

### 方式二：top = 0 初始化

| 操作 | 代码 | 说明 |
|------|------|------|
| 初始化 | \`S.top = 0\` | 栈空时 top = 0 |
| 判空 | \`S.top == 0\` | |
| 判满 | \`S.top == MaxSize\` | |
| 入栈 | \`S.data[S.top++] = x\` | 先赋值，再 +1 |
| 出栈 | \`x = S.data[--S.top]\` | 先 -1，再取值 |
| 栈顶元素位置 | \`S.top - 1\` | S.top 指向栈顶的下一个位置 |

> 408 考研以**方式一 (top = -1)**为主流，没特别说明默认此方式。

---

## 完整代码示例（top = -1）

\`\`\`c
// 初始化
void InitStack(SqStack *S) { S->top = -1; }

// 判空
bool StackEmpty(SqStack S) { return S.top == -1; }

// 入栈
bool Push(SqStack *S, ElemType x) {
    if (S->top == MaxSize - 1) return false;
    S->data[++S->top] = x;
    return true;
}

// 出栈
bool Pop(SqStack *S, ElemType *x) {
    if (S->top == -1) return false;
    *x = S->data[S->top--];
    return true;
}

// 读栈顶
bool GetTop(SqStack S, ElemType *x) {
    if (S.top == -1) return false;
    *x = S.data[S.top];
    return true;
}
\`\`\`

---

## 考试辨析

- 两种 top 定义方式的不同导致**入栈/出栈/判空/栈顶位置**的代码完全不同——做题时先确认题目采用哪种方式
- 顺序栈所有操作时间复杂度均为 $O(1)$`,
  },
  {
    id: 'ds-3-1-3',
    chapterId: 'ds-3',
    title: '共享栈',
    keyConcepts: ['共享栈', '两个栈', '相向增长', '栈满条件'],
    relatedPoints: ['ds-3-1-2'],
    content: `## 共享栈的定义

> 利用栈底位置相对不变的特性，让两个顺序栈**共享同一个一维数组空间**，两个栈的栈底分别设置在数组的两端，各自向数组中间延伸。

\`\`\`
数组: [0] [1] [2] ...       ... [n-3] [n-2] [n-1]
        ^---- 栈1增长 ---->        <---- 栈2增长 ----^
       top1=-1                                       top2=n
\`\`\`

---

## 共享栈的数据结构

\`\`\`c
#define MaxSize 100
typedef struct {
    ElemType data[MaxSize];
    int top1;   // 栈1的栈顶指针
    int top2;   // 栈2的栈顶指针
} SqDoubleStack;
\`\`\`

---

## 关键参数

| 项目 | 栈1 | 栈2 |
|------|-----|-----|
| 初始 top | \`top1 = -1\` | \`top2 = MaxSize\` |
| 判空条件 | \`top1 == -1\` | \`top2 == MaxSize\` |
| **栈满条件** | **\`top1 + 1 == top2\`** | 两个栈共享判断 |
| 入栈方向 | 向右（下标增大） | 向左（下标减小） |

---

## 入栈/出栈操作

\`\`\`c
// 入栈
bool Push(SqDoubleStack *S, ElemType x, int stackNum) {
    if (S->top1 + 1 == S->top2) return false;  // 栈满
    if (stackNum == 1)
        S->data[++S->top1] = x;   // 栈1：向右增长
    else
        S->data[--S->top2] = x;   // 栈2：向左增长
    return true;
}

// 出栈
bool Pop(SqDoubleStack *S, ElemType *x, int stackNum) {
    if (stackNum == 1) {
        if (S->top1 == -1) return false;
        *x = S->data[S->top1--];
    } else {
        if (S->top2 == MaxSize) return false;
        *x = S->data[S->top2++];
    }
    return true;
}
\`\`\`

---

## 共享栈的优点

> **空间利用率更高**：一个栈的空间不够时，可以借用另一个栈的空闲空间。相比两个独立顺序栈，更不容易发生溢出。

---

## 考试辨析

- **栈满条件 top1 + 1 == top2** 是高频考点
- 共享栈常用于需要两个栈，且两者所需空间有反向增长趋势的场景`,
  },
  {
    id: 'ds-3-1-4',
    chapterId: 'ds-3',
    title: '链栈',
    keyConcepts: ['链栈', '链式存储', '单链表', '头插法'],
    relatedPoints: ['ds-3-1-1', 'ds-3-1-2', 'ds-2-3-1'],
    content: `## 链栈的定义

> 采用**链式存储**的栈称为链栈。

- 链栈通常**没有头结点**（用栈顶指针代替头指针）
- 入栈/出栈都在**链表头部**进行（头插法 + 删除首元结点）

---

## 链栈的结点结构

\`\`\`c
typedef struct LinkNode {
    ElemType data;
    struct LinkNode *next;
} *LiStack;
\`\`\`

---

## 链栈 vs 顺序栈

| 特性 | 顺序栈 | 链栈 |
|------|--------|------|
| 存储空间 | 连续、固定大小 | 动态分配，灵活 |
| 溢出风险 | 栈满可能溢出 | 无栈满问题（除非内存耗尽） |
| 额外空间开销 | 无 | 每个结点多一个指针域 |
| 所有操作 | $O(1)$ | $O(1)$ |

---

## 链栈操作为何是 O(1)

> 入栈（头插）：将新结点插入到链表头部 — $O(1)$
> 出栈：删除链表首元结点 — $O(1)$
>
> 无需遍历链表，所有操作都在链表头部完成。

---

## 考试辨析

- 链栈入栈/出栈都在链表头部进行，**不需要头结点**（栈顶指针即头指针）
- 链栈不会出现"栈满"——这是链栈相比顺序栈的主要优势`,
  },

  // --- 3.2 队列 ---
  {
    id: 'ds-3-2-1',
    chapterId: 'ds-3',
    title: '队列的定义与基本操作',
    keyConcepts: ['队列', 'FIFO', '先进先出', '队头', '队尾', 'EnQueue', 'DeQueue'],
    relatedPoints: ['ds-2-1-1', 'ds-3-2-2'],
    content: `## 队列的定义

> 队列 (Queue) 是只允许在**一端插入、另一端删除**的线性表。

| 术语 | 含义 |
|------|------|
| **队头 (Front)** | 允许删除的那一端 |
| **队尾 (Rear)** | 允许插入的那一端 |
| **空队列** | 不含任何元素的队列 |

---

## 队列的特点：FIFO

> **先进先出 (First In First Out)** — 先入队的元素先出队。

类比排队买票：新来的人排在队尾，队头的人先离开。

---

## 队列的基本操作

| 操作 | 函数原型 | 说明 |
|------|----------|------|
| **初始化** | InitQueue(&Q) | 构造一个空队列 |
| **判空** | QueueEmpty(Q) | 若队列为空返回 true |
| **入队** | EnQueue(&Q, x) | 将 $x$ 插入队尾 |
| **出队** | DeQueue(&Q, &x) | 删除队头元素，用 $x$ 返回 |
| **读队头** | GetHead(Q, &x) | 读取队头元素但不删除 |

---

## 栈 vs 队列

| 比较 | 栈 (Stack) | 队列 (Queue) |
|------|-----------|-------------|
| 操作端 | 一端（栈顶） | 两端（队头 + 队尾） |
| 逻辑 | LIFO | FIFO |
| 插入 | Push (入栈) | EnQueue (入队) |
| 删除 | Pop (出栈) | DeQueue (出队) |

---

## 考试辨析

- 栈和队列都是**操作受限的线性表**——本质上是线性表，只是限制了操作位置
- 常考"以下哪个是队列的应用"——答：层次遍历、BFS、缓冲区、排队系统`,
  },
  {
    id: 'ds-3-2-2',
    chapterId: 'ds-3',
    title: '顺序队列与循环队列',
    keyConcepts: ['循环队列', '假溢出', 'front', 'rear', '取模运算', '判空', '判满'],
    relatedPoints: ['ds-3-2-1', 'ds-3-2-3'],
    content: `## 顺序队列的问题：假溢出

顺序队列（数组实现）中，随着入队/出队操作，\`front\` 和 \`rear\` 不断后移。当 \`rear == MaxSize\` 时，即使队头之前有空位，也无法继续入队——称为**假溢出**。

---

## 循环队列的解决方案

> 将顺序队列视为一个**环状空间**，通过**取模运算**使指针循环使用。

\`\`\`
入队: Q.rear = (Q.rear + 1) % MaxSize
出队: Q.front = (Q.front + 1) % MaxSize
\`\`\`

---

## 数据结构定义

\`\`\`c
#define MaxSize 50
typedef struct {
    ElemType data[MaxSize];
    int front;   // 队头指针
    int rear;    // 队尾指针
} SqQueue;
\`\`\`

> **默认约定**：front 指向队头元素，rear 指向队尾元素的**下一个位置**（即下一个入队的位置）。

---

## 核心操作（默认方式）

| 操作 | 代码 |
|------|------|
| 初始化 | \`Q.front = Q.rear = 0\` |
| 判空 | \`Q.front == Q.rear\` |
| 入队 | \`Q.data[Q.rear] = x; Q.rear = (Q.rear + 1) % MaxSize\` |
| 出队 | \`x = Q.data[Q.front]; Q.front = (Q.front + 1) % MaxSize\` |
| 队头元素 | \`Q.data[Q.front]\` |

---

## 判空/判满 — 三种方法（高频必考）

### 方法① 牺牲一个存储单元（最常用）

| 条件 | 判断 |
|------|------|
| 队空 | \`Q.front == Q.rear\` |
| 队满 | \`(Q.rear + 1) % MaxSize == Q.front\` |
| 元素个数 | \`(Q.rear - Q.front + MaxSize) % MaxSize\` |

> 此时队列最大容量为 \`MaxSize - 1\`（牺牲了一个单元）。

### 方法② 增设 size/flag 成员

| 条件 | 判断 |
|------|------|
| 队空 | \`Q.front == Q.rear && Q.size == 0\` |
| 队满 | \`Q.front == Q.rear && Q.size == MaxSize\` |

> 此时 front == rear 时需要结合 size 判断是空还是满。

### 方法③ 增设 tag 标志

入队时 tag = 1，出队时 tag = 0：

| 条件 | 判断 |
|------|------|
| 队空 | \`Q.front == Q.rear && Q.tag == 0\` |
| 队满 | \`Q.front == Q.rear && Q.tag == 1\` |

---

## 元素个数公式（通用）

$$\\\\text{length} = (\\\\text{rear} - \\\\text{front} + \\\\text{MaxSize}) \\\\bmod \\\\text{MaxSize}$$

---

## 考试辨析

- 不同教材/题目中 rear 可能指向队尾元素本身（而非下一个位置），此时入队顺序变更为先移动再赋值
- 判断循环队列空/满时，先确认题目采用哪种方法——这是408选择题最常见陷阱
- 2011年真题：rear 指向队尾元素，第一个入队元素在 A[0]，初始 front=0, rear=n-1`,
  },
  {
    id: 'ds-3-2-3',
    chapterId: 'ds-3',
    title: '链式队列',
    keyConcepts: ['链队', '链式存储', '队头指针', '队尾指针'],
    relatedPoints: ['ds-3-2-1', 'ds-3-2-2', 'ds-2-3-1'],
    content: `## 链队的定义

> 采用**链式存储**的队列称为链队。

- 通常**带头结点**
- 需要两个指针：**队头指针** (front) 指向头结点，**队尾指针** (rear) 指向尾结点

---

## 链队的结点结构

\`\`\`c
// 结点
typedef struct LinkNode {
    ElemType data;
    struct LinkNode *next;
} LinkNode;

// 链队
typedef struct {
    LinkNode *front, *rear;  // 队头和队尾指针
} LinkQueue;
\`\`\`

---

## 核心操作

### 初始化（带头结点）

\`\`\`c
void InitQueue(LinkQueue *Q) {
    Q->front = Q->rear = (LinkNode*)malloc(sizeof(LinkNode));
    Q->front->next = NULL;
}
\`\`\`

### 判空

\`\`\`c
bool QueueEmpty(LinkQueue Q) {
    return Q.front == Q.rear;
}
\`\`\`

### 入队

\`\`\`c
void EnQueue(LinkQueue *Q, ElemType x) {
    LinkNode *s = (LinkNode*)malloc(sizeof(LinkNode));
    s->data = x;
    s->next = NULL;
    Q->rear->next = s;
    Q->rear = s;
}
\`\`\`

### 出队

\`\`\`c
bool DeQueue(LinkQueue *Q, ElemType *x) {
    if (Q->front == Q->rear) return false;
    LinkNode *p = Q->front->next;
    *x = p->data;
    Q->front->next = p->next;
    if (Q->rear == p)    // 若删除的是最后一个结点
        Q->rear = Q->front;
    free(p);
    return true;
}
\`\`\`

---

## 链队 vs 循环队列

| 特性 | 循环队列 | 链队 |
|------|---------|------|
| 存储空间 | 顺序、固定大小 | 链式、动态 |
| 判空/判满 | 需特殊处理（牺牲单元/size/tag） | 简单（front == rear） |
| 不会"满" | 否（会满） | 是（除非内存耗尽） |

---

## 考试辨析

- 链队出队时，若**删除的是最后一个结点**，需要将 \`rear\` 指回头结点（否则 rear 变成悬空指针）
- 链队适合元素个数不确定、空间需求动态变化的场景`,
  },
  {
    id: 'ds-3-2-4',
    chapterId: 'ds-3',
    title: '双端队列',
    keyConcepts: ['双端队列', '两端操作', '输入受限', '输出受限'],
    relatedPoints: ['ds-3-2-1', 'ds-3-2-2'],
    content: `## 双端队列的定义

> 允许**两端都可以进行入队和出队操作**的队列。

其元素的逻辑结构仍是线性结构（一对一关系）。

---

## 双端队列的分类

| 类型 | 特点 |
|------|------|
| **双端队列** | 两端都可以入队/出队 |
| **输入受限的双端队列** | 一端允许插入和删除，另一端只允许删除 |
| **输出受限的双端队列** | 一端允许插入和删除，另一端只允许插入 |

---

## 双端队列的输出序列问题（常考）

> 已知入队序列 1, 2, 3, 4，判断某序列是否可能是合法出队序列。

| 数据结构 | 合法序列示例 | 非法序列示例 |
|----------|-------------|-------------|
| 栈 | 3,2,1,4 | 3,1,2,4 |
| 队列 | 1,2,3,4 | 4,1,2,3 |
| 双端队列 | 根据限制条件判断 | — |

---

## 考试辨析

- 408 常考"输入/输出受限的双端队列"出队序列合法性判断
- 判定方法：模拟操作过程，检查每个出队元素在当前状态下是否可以从允许的一端取出
- 这类题通常在选择题中出现，分值 2 分`,
  },

  // --- 3.3 栈和队列的应用 ---
  {
    id: 'ds-3-3-1',
    chapterId: 'ds-3',
    title: '栈在括号匹配中的应用',
    keyConcepts: ['括号匹配', '栈', '扫描', '左括号入栈', '右括号匹配'],
    relatedPoints: ['ds-3-1-1', 'ds-3-3-2'],
    content: `## 括号匹配问题

判断一个表达式中的括号 \`()\`、\`[]\`、\`{}\` 是否成对出现且嵌套正确。

---

## 算法思想

1. 从左到右扫描表达式
2. 遇到**左括号** → 入栈
3. 遇到**右括号** → 出栈一个括号，检查是否匹配
- 若栈为空 → **右括号多余**（匹配失败）
- 若栈顶括号与当前右括号**类型不匹配** → 匹配失败
4. 扫描结束后，检查栈是否为空
- 栈为空 → 匹配成功
- 栈非空 → **左括号多余**（匹配失败）

---

## 算法实现

\`\`\`c
bool BracketCheck(char *str) {
    SqStack S;
    InitStack(&S);
    for (int i = 0; str[i] != '\\0'; i++) {
        if (str[i] == '(' || str[i] == '[' || str[i] == '{')
            Push(&S, str[i]);         // 左括号入栈
        else if (str[i] == ')' || str[i] == ']' || str[i] == '}') {
            if (StackEmpty(S)) return false;  // 右括号多余
            char top;
            Pop(&S, &top);
            if (!Match(top, str[i])) return false;  // 类型不匹配
        }
    }
    return StackEmpty(S);  // 栈空则匹配成功
}
\`\`\`

---

## 时间复杂度

- 每个字符扫描一次，每个括号最多入栈/出栈一次
- 时间复杂度 $O(n)$，空间复杂度 $O(n)$

---

## 考试辨析

- 栈是括号匹配的**标准解决方案**
- 408 算法题可能考三种括号 \`()\`、\`[]\`、\`{}\` 的匹配，注意类型匹配判断`,
  },
  {
    id: 'ds-3-3-2',
    chapterId: 'ds-3',
    title: '栈在表达式求值中的应用',
    keyConcepts: ['表达式求值', '中缀', '后缀', '前缀', '运算符优先级', '双栈法'],
    relatedPoints: ['ds-3-3-1', 'ds-3-3-3'],
    content: `## 三种表达式表示法

| 表示法 | 特点 | 示例（a + b * c） |
|--------|------|--------------------|
| **中缀 (Infix)** | 运算符在操作数中间 | \`a + b * c\` |
| **后缀 (Postfix / RPN)** | 运算符在操作数之后 | \`a b c * +\` |
| **前缀 (Prefix)** | 运算符在操作数之前 | \`+ a * b c\` |

> 中缀表达式需要括号和优先级规则，后缀和前缀表达式**不需要括号**即可唯一确定运算顺序。

---

## 中缀转后缀（核心算法）

1. 遇到**操作数** → 直接输出
2. 遇到 **\`(\`** → 入栈
3. 遇到 **\`)\`** → 依次弹出栈中运算符输出，直到 \`(\`（\`(\` 弹出但不输出）
4. 遇到**运算符**：
- 若优先级 **高于** 栈顶 或 栈顶为 \`(\` → 入栈
- 若优先级 ≤ 栈顶 → 依次弹出并输出，直到优先级更低或 \`(\` 或栈空，再入栈
5. 扫描结束 → 依次弹出栈中剩余运算符

---

## 运算符优先级（408统一沿用）

| 运算符 | 优先级 |
|--------|--------|
| \`*\`, \`/\` | 高 |
| \`+\`, \`-\` | 低 |
| \`(\` | 入栈时最高，栈中时最低 |

---

## 后缀表达式求值

1. 遇到**操作数** → 入栈
2. 遇到**运算符** → 弹出两个操作数（先弹出右操作数，后弹出左操作数），计算后将结果入栈

\`\`\`c
// 后缀求值示例
// 表达式: "23 15 + 4 *"  →  (23+15)*4 = 152
\`\`\`

---

## 中缀表达式直接求值（双栈法）

- 操作数栈 (OPND) — 存操作数
- 运算符栈 (OPTR) — 存运算符
- 扫描中缀表达式，操作数入 OPND，运算符按"中缀转后缀"规则处理
- 每弹出一个运算符，从 OPND 弹出两个数，计算后压回 OPND

---

## 考试辨析

- **2015年、2018年真题**考过中缀转后缀的手工模拟（选择题）
- 后缀表达式中的操作数顺序**与中缀中相同**（只是运算符位置变了）
- 先弹出的操作数是**右操作数**（对减法/除法至关重要）`,
  },
  {
    id: 'ds-3-3-3',
    chapterId: 'ds-3',
    title: '栈在递归中的应用',
    keyConcepts: ['递归', '调用栈', '递归工作栈', '递归深度', '递归转非递归'],
    relatedPoints: ['ds-3-1-1', 'ds-1-2-2'],
    content: `## 递归与栈的关系

> 递归调用的执行过程本质上是一个**栈**的入栈/出栈过程。

- 每次递归调用：**保存现场**（参数、局部变量、返回地址）→ 入栈
- 每次递归返回：**恢复现场** → 出栈

---

## 递归工作栈

系统为每一层递归创建一个**工作记录（栈帧）**，包含：
- 局部变量
- 参数值
- 返回地址

所有栈帧构成**递归工作栈**。递归深度 = 栈中栈帧的数量。

---

## 递归算法的时间空间分析

\`\`\`
int fact(int n) {
    if (n <= 1) return 1;
    return n * fact(n - 1);
}
\`\`\`

| 分析维度 | 值 | 说明 |
|----------|-----|------|
| 时间复杂度 | $O(n)$ | 递归 n 次，每次 O(1) |
| 空间复杂度 | $O(n)$ | 递归深度 = n |
| 递归工作栈深度 | n | 每次调用入栈一次 |

---

## 递归转非递归

> 任何递归算法都可以转换为**非递归算法**。核心手段：**用栈模拟递归调用过程**。

- 简单递归 → 可以转换为**循环**（如阶乘）
- 复杂递归 → 需要**显式栈**手动模拟（如二叉树遍历、汉诺塔）

---

## 递归的缺点

- 空间效率低（递归栈可能很深）
- 函数调用有额外时间开销
- 可能出现**栈溢出**（递归深度过大）

---

## 考试辨析

- 递归算法转换为非递归算法**必须使用栈**——这句话是错误的（简单递归可用循环，不需栈）
- 递归算法的空间复杂度主要由**递归深度**（而非总调用次数）决定`,
  },
  {
    id: 'ds-3-3-4',
    chapterId: 'ds-3',
    title: '队列的应用',
    keyConcepts: ['队列应用', '层次遍历', 'BFS', '缓冲区', '页面替换', 'FIFO'],
    relatedPoints: ['ds-3-2-1', 'ds-5-3-1', 'ds-6-3-1'],
    content: `## 队列的典型应用

---

### 1. 二叉树的层次遍历（广度优先遍历）

> 逐层访问二叉树结点，需要队列辅助。

\`\`\`
1. 根结点入队
2. 队列非空时：
- 出队一个结点，访问之
- 将其左、右子结点（若存在）依次入队
\`\`\`

---

### 2. 图的广度优先搜索 (BFS)

> 逐层扩展访问图中顶点，需要队列辅助记录待访问顶点。

详见第6章"图的遍历"。

---

### 3. 主机与打印机之间的缓冲区

> 主机向打印机发送数据的速度远大于打印机接收数据的速度。
> 用队列作为**缓冲**：先发送的数据先打印（FIFO）。

---

### 4. CPU 进程调度

> 操作系统中的**先来先服务 (FCFS)** 调度算法基于队列实现。
> 详见操作系统第2章。

---

### 5. 页面替换算法 — FIFO

> 最早进入内存的页面最先被换出，使用队列管理页面顺序。

---

## 栈 vs 队列的应用对比

| 数据结构 | 应用场景 |
|----------|---------|
| **栈** | 括号匹配、表达式求值、递归、DFS、函数调用、Undo 撤销 |
| **队列** | 层次遍历、BFS、缓冲区、排队系统、FCFS 调度 |

---

## 考试辨析

- 层次遍历 = BFS → 用**队列**；深度优先遍历 = DFS → 用**栈**（或递归）
- 408 选择题常见：给定一个场景，判断应该用栈还是队列`,
  },

  // --- 3.4 数组和特殊矩阵 ---
  {
    id: 'ds-3-4-1',
    chapterId: 'ds-3',
    title: '数组的存储结构',
    keyConcepts: ['数组', '多维数组', '行优先', '列优先', '地址计算'],
    relatedPoints: ['ds-2-2-1'],
    content: `## 数组的定义

> 数组是由 n 个**相同类型**的数据元素构成的有限序列，每个元素受 n 个线性关系的约束。

数组是线性表的推广：
- 一维数组 → 线性表
- 二维数组 → 每个元素是一维数组的线性表

---

## 多维数组的存储映射

由于内存是一维的，多维数组必须**映射**到一维地址空间。

### 行优先存储（C 语言默认）

**先行后列**：一行存完再存下一行。

$$LOC(a_{i,j}) = LOC(a_{0,0}) + [i \\times n + j] \\times L$$

其中 $n$ 为列数，$L$ 为每元素字节数。

### 列优先存储（Fortran 默认）

**先列后行**：一列存完再存下一列。

$$LOC(a_{i,j}) = LOC(a_{0,0}) + [j \\times m + i] \\times L$$

其中 $m$ 为行数。

---

## 地址计算公式对比

| 存储方式 | 二维地址公式 |
|----------|-------------|
| 行优先 | $LOC(a_{0,0}) + (i \\times n + j) \\times L$ |
| 列优先 | $LOC(a_{0,0}) + (j \\times m + i) \\times L$ |

---

## 考试辨析

- **矩阵下标范围**决定公式中的偏移量：若从 a[1][1] 开始，公式需调整为 $(i-1) \\times n + (j-1)$
- 408 常考给定下标范围，计算地址偏移
- 数组的**随机存取**特性：给定下标可 $O(1)$ 计算出地址`,
  },
  {
    id: 'ds-3-4-2',
    chapterId: 'ds-3',
    title: '特殊矩阵的压缩存储',
    keyConcepts: ['对称矩阵', '三角矩阵', '三对角矩阵', '压缩存储', '下标转换'],
    relatedPoints: ['ds-3-4-1'],
    content: `## 为什么要压缩存储

对具有特殊结构的矩阵（如对称矩阵），许多元素重复出现或为零，**压缩存储**可以节省空间。

> 压缩存储的核心思想：**只存储有效元素**，通过下标映射访问。

---

## 一、对称矩阵的压缩存储

$a_{i,j} = a_{j,i}$，只需存储上三角或下三角（含主对角线）。

存储元素个数：$\\frac{n(n+1)}{2}$

### 行优先存储下三角（含主对角线）

| 条件 | 在一维数组 B 中的下标 |
|------|----------------------|
| $i \\ge j$（下三角） | $\\frac{i(i-1)}{2} + j - 1$ |
| $i < j$（上三角） | $\\frac{j(j-1)}{2} + i - 1$（利用对称性） |

> 下标从 1 开始；若用 0 下标，公式需相应调整。

---

## 二、三角矩阵的压缩存储

| 类型 | 特点 | 元素数 |
|------|------|--------|
| **上三角矩阵** | 下三角为常数 C | $\\frac{n(n+1)}{2} + 1$ |
| **下三角矩阵** | 上三角为常数 C | $\\frac{n(n+1)}{2} + 1$ |

> 额外存储一个**常数项** C（存在一维数组最后一个位置）。

---

## 三、三对角矩阵的压缩存储

三条对角线外全为 0。每行最多 3 个非零元素。

| 对角线 | 位置关系 |
|--------|---------|
| 主对角线 | $i = j$ |
| 下次对角线 | $i = j + 1$ |
| 上次对角线 | $i = j - 1$ |

一维数组存储非零元素个数：$3n - 2$

行优先下标公式：
$$k = 2i + j - 3 \\quad (|i-j| \\le 1)$$

---

## 考试辨析

- 下标转换公式**不要求背诵**，但需要理解推导方法
- 关键考点：给出矩阵元素下标 $(i,j)$，求在一维压缩数组中的位置 $k$
- 注意**下标起始**（0 或 1）对公式的影响`,
  },
  {
    id: 'ds-3-4-3',
    chapterId: 'ds-3',
    title: '稀疏矩阵',
    keyConcepts: ['稀疏矩阵', '三元组', '十字链表', '稀疏因子'],
    relatedPoints: ['ds-3-4-2', 'ds-2-3-5'],
    content: `## 稀疏矩阵的定义

> 非零元素个数远少于零元素个数，且非零元素分布无规律的矩阵。

**稀疏因子** $\\delta = \\frac{\\text{非零元素个数}}{\\text{总元素个数}}$，通常 $\\delta \\le 0.05$。

---

## 压缩存储方式

### 1. 三元组顺序表

\`\`\`c
typedef struct {
    int row, col;     // 行号、列号
    ElemType value;   // 元素值
} Triple;

typedef struct {
    Triple data[MaxSize];
    int rows, cols, num;  // 行数、列数、非零元素个数
} TSMatrix;
\`\`\`

> 按行优先顺序存储，可用于：矩阵转置、矩阵加法等操作。

### 2. 十字链表

\`\`\`c
typedef struct OLNode {
    int row, col;
    ElemType value;
    struct OLNode *right, *down;  // 同行下一个、同列下一个
} OLNode, *OLink;
\`\`\`

> 每个非零元素是一个结点，通过 right 和 down 指针串联同行/同列的结点。

---

## 三元组 vs 十字链表

| 特性 | 三元组顺序表 | 十字链表 |
|------|------------|---------|
| 插入/删除 | 困难（需移动元素） | 方便（改指针） |
| 随机存取 | $O(num)$（需搜索） | 沿链表查找 |
| 存储开销 | 小 | 大（每个结点多两个指针） |
| 适用场景 | 矩阵非零元素个数固定的场景 | 非零元素个数频繁变化的场景 |

---

## 考试辨析

- 稀疏矩阵压缩存储后**失去了随机存取特性**（无法直接计算地址，必须搜索）
- 三元组存储的矩阵转置时间复杂度：朴素 $O(num \\times n)$，优化的快速转置 $O(num + n)$`,
  },

  // ============================================================
  // 第4章 串
  // ============================================================

  // --- 4.1 串的定义和基本操作 ---
  {
    id: 'ds-4-1-1',
    chapterId: 'ds-4',
    title: '串的定义与基本操作',
    keyConcepts: ['串', '字符串', '子串', '主串', '串长', '空串', '空白串', '串相等'],
    relatedPoints: ['ds-2-1-1'],
    content: `## 串的定义

> 串 (String) 是由**零个或多个字符**组成的有限序列。

记作：$S = 'a_1 a_2 \\ldots a_n'$（$n \\ge 0$）

| 术语 | 含义 |
|------|------|
| **串名** | $S$ |
| **串值** | 引号中的字符序列 |
| **串长** | $n$，字符的个数 |
| **空串** | $n = 0$ 的串，记作 $\\emptyset$ |

---

## 串的相关概念辨析（高频考点）

| 概念 | 定义 | 说明 |
|------|------|------|
| **子串** | 串中任意个**连续**字符组成的子序列 | "abc" 的子串有 "a","b","c","ab","bc","abc" 和空串 |
| **主串** | 包含子串的串 | — |
| **字符位置** | 字符在串中的序号 | 从 **1** 开始 |
| **子串位置** | 子串第一个字符在主串中的位置 | — |
| **串相等** | 长度相等且对应位置字符相同 | — |
| **空串 vs 空白串** | 空串 $n=0$，空白串由一个或多个空格组成 $n \\ge 1$ | 408 选择题常见干扰项 |

---

## 串的基本操作

| 操作 | 说明 |
|------|------|
| StrAssign(&T, chars) | 赋值 |
| StrCopy(&T, S) | 复制 |
| StrEmpty(S) | 判空 |
| StrCompare(S, T) | 比较（字典序） |
| StrLength(S) | 求串长 |
| Concat(&T, S1, S2) | 连接 |
| SubString(&Sub, S, pos, len) | 求子串 |
| Index(S, T) | 定位子串（模式匹配） |
| Replace(&S, T, V) | 替换 |

---

## 串与线性表的区别

| 比较维度 | 线性表 | 串 |
|----------|--------|-----|
| 数据元素 | 单个元素（任意类型） | **字符** |
| 操作对象 | 单个元素 | **子串**（连续若干字符） |
| 主要操作 | 插入、删除 | **查找**（模式匹配） |

> 串是一种**特殊的线性表**——数据元素限定为字符，且操作常以子串为单位。

---

## 考试辨析

- 空串 ($n=0$) \\u2260 空白串 ($n \\ge 1$，内容是空格)——此考点在408选择题中频繁出现
- 子串必须是**连续**的字符序列，子序列可以不连续（串没有"子序列"概念）`,
  },
  {
    id: 'ds-4-1-2',
    chapterId: 'ds-4',
    title: '串的存储结构',
    keyConcepts: ['顺序存储', '堆分配', '块链存储', '存储密度'],
    relatedPoints: ['ds-4-1-1', 'ds-2-2-1', 'ds-2-3-1'],
    content: `## 串的三种存储方式

---

### 一、定长顺序存储

\`\`\`c
#define MaxLen 255
typedef struct {
    char ch[MaxLen];
    int length;
} SString;
\`\`\`

> 超过 MaxLen 的部分被**截断**。

---

### 二、堆分配存储

\`\`\`c
typedef struct {
    char *ch;    // 动态分配
    int length;
} HString;
\`\`\`

> 用 malloc/free 管理空间，灵活但需要手动管理。

---

### 三、块链存储

\`\`\`c
#define ChunkSize 4
typedef struct Chunk {
    char ch[ChunkSize];
    struct Chunk *next;
} Chunk;
typedef struct {
    Chunk *head, *tail;
    int curlen;
} LString;
\`\`\`

| 块大小 | 存储密度 | 操作效率 |
|--------|---------|---------|
| 大 | 高 | 低（插入删除移动多） |
| 小 | 低 | 高（指针灵活） |

> **存储密度** $= \\frac{\\text{串值所占字节}}{\\text{结点结构所占字节}}$

---

## 三种存储方式对比

| 存储方式 | 优点 | 缺点 |
|----------|------|------|
| 定长顺序 | 简单 | 可能截断 |
| 堆分配 | 灵活 | 需手动管理 |
| 块链 | 插入删除方便 | 存储密度低 |

---

## 考试辨析

- 408 对串的存储结构要求不高，重点掌握**顺序存储**即可
- 块链存储的存储密度分析曾是选择题考点`,
  },

  // --- 4.2 串的模式匹配 ---
  {
    id: 'ds-4-2-1',
    chapterId: 'ds-4',
    title: '简单模式匹配算法 (BF算法)',
    keyConcepts: ['BF算法', '暴力匹配', '回溯', 'O(n*m)', '模式匹配'],
    relatedPoints: ['ds-4-2-2'],
    content: `## BF (Brute Force) 算法

> 从主串 $S$ 的第一个字符开始，依次与模式串 $T$ 比较。若失配，则 $i$ 回溯到 $i-j+2$，$j$ 回到 $1$，重新开始比较。

---

## 算法步骤

1. 设 $i$ 指向主串当前位置，$j$ 指向模式串当前位置
2. 若 $S[i] == T[j]$，则 $i++$，$j++$
3. 若 $S[i] \\neq T[j]$，则 $i$ **回溯**到本轮开始的下一个位置，$j$ 回到 $1$
4. 若 $j$ 超出模式串长度，匹配成功；若 $i$ 超出主串，失败

---

## 代码实现

\`\`\`c
int Index_BF(SString S, SString T) {
    int i = 1, j = 1;
    while (i <= S.length && j <= T.length) {
        if (S.ch[i] == T.ch[j]) {
            i++; j++;              // 继续比较后续字符
        } else {
            i = i - j + 2;         // i 回溯
            j = 1;                 // j 回到第1位
        }
    }
    if (j > T.length) return i - T.length;  // 匹配成功
    else return 0;                           // 匹配失败
}
\`\`\`

---

## 时间复杂度分析

| 情况 | 复杂度 | 说明 |
|------|--------|------|
| 最好情况 | $O(n+m)$ | 每次第1个字符就不匹配 |
| 平均情况 | $O(n+m)$ | 一般情况 |
| **最坏情况** | **$O(n \\times m)$** | 如 $S$="aaaaaaaaab"，$T$="aaab" |

> 最坏情况下，每次匹配到最后一位才发现不匹配，然后回溯重来。

---

## BF 算法的缺点

> **主串指针 $i$ 会回溯**——当失配时，已经比较过的主串字符需要重新比较。
> KMP算法正是针对这一缺点进行了改进。

---

## 考试辨析

- BF 算法的回溯公式：$i = i - j + 2$（i 回到本轮开始的下一个位置）
- BF 最坏时间复杂度 $O(n \\times m)$，虽然理论上比 KMP 差，但实际一般情况表现良好`,
  },
  {
    id: 'ds-4-2-2',
    chapterId: 'ds-4',
    title: 'KMP算法 — next数组',
    keyConcepts: ['KMP', 'next数组', '部分匹配值', '前缀', '后缀', 'i不回溯', 'O(n+m)'],
    relatedPoints: ['ds-4-2-1', 'ds-4-2-3'],
    content: `## KMP 算法的核心思想

> **主串指针 $i$ 永不回溯！** 当失配时，利用已经匹配的信息，让模式串指针 $j$ 跳到合适的位置（next[j]）继续比较。

---

## 前缀、后缀与部分匹配值 (PM)

| 术语 | 定义 | 示例（串 "ababa"） |
|------|------|---------------------|
| **前缀** | 除最后一个字符外，所有头部子串 | "a","ab","aba","abab" |
| **后缀** | 除第一个字符外，所有尾部子串 | "a","ba","aba","baba" |
| **部分匹配值** | 前缀与后缀的**最长相等长度** | 3（"aba"） |

---

## next 数组 — 核心公式

> **next[j]** 的含义：当模式串第 $j$ 个字符失配时，应将 $j$ 跳转到 **next[j]** 继续与主串 $i$ 位置比较。

### 手算规则（下标从 1 开始 — 408 标准）

| 规则 | 说明 |
|------|------|
| next[1] = **0** | 第一个字符失配，$i$ 右移，$j$ 回到 1 |
| next[2] = **1** | 第二个字符失配，$j$ 回到 1 |
| j \\u2265 3 | 求 $P[1 \\ldots j-1]$ 的**最长公共前后缀长度** $k$，则 next[j] = **$k + 1$** |

> **记忆口诀**：看前缀，找后缀，最长公共加个 1。

---

## 求 next 数组示例

模式串 T = **"abaabc"**

| j | 1 | 2 | 3 | 4 | 5 | 6 |
|---|---|---|---|---|---|---|
| T[j] | a | b | a | a | b | c |
| 前缀子串 | — | — | "ab" | "aba" | "abaa" | "abaab" |
| 最长公共前后缀 | — | — | 0 | 1 | 1 | 2 |
| **next[j]** | **0** | **1** | **1** | **2** | **2** | **3** |

---

## KMP 算法实现

\`\`\`c
int Index_KMP(SString S, SString T, int next[]) {
    int i = 1, j = 1;
    while (i <= S.length && j <= T.length) {
        if (j == 0 || S.ch[i] == T.ch[j]) {
            i++; j++;                       // 继续比较
        } else {
            j = next[j];                    // j 跳转，i 不动！
        }
    }
    if (j > T.length) return i - T.length;
    else return 0;
}
\`\`\`

> **关键**：i 只增不减（不回溯），j 根据 next 数组跳转。

---

## 时间复杂度

| 阶段 | 复杂度 |
|------|--------|
| 求 next 数组 | $O(m)$ |
| KMP 匹配 | $O(n)$ |
| **总计** | **$O(n + m)$** |

---

## 考试辨析（高频必考）

- next[1] **恒为 0**，next[2] **恒为 1**（408 标准下标从1开始）
- **next 值 \\u2260 最长公共前后缀长度**：next = 长度 + 1
- 若下标从 0 开始（如 LeetCode），则 next[0] = -1，且 next = 长度（不加1）
- 408 只考手算 next/nextval，**不考 KMP 算法大题**，全以选择题形式出现`,
  },
  {
    id: 'ds-4-2-3',
    chapterId: 'ds-4',
    title: 'KMP算法的优化 — nextval数组',
    keyConcepts: ['nextval', '优化', '无效回溯', '字符相等'],
    relatedPoints: ['ds-4-2-2'],
    content: `## 为什么要优化

> 当 $P[j] == P[next[j]]$ 时，next[j] 指向的字符与当前失配字符相同，必然再次失配，产生**无效回溯**。

nextval 通过**跳过相等的字符**来避免这种浪费。

---

## nextval 的计算规则

| 条件 | nextval[j] |
|------|-----------|
| $P[j] \\neq P[next[j]]$ | nextval[j] = next[j] |
| $P[j] == P[next[j]]$ | 令 $k = next[j]$，继续向前比较直到不等或到首位：nextval[j] = nextval[k] |

> nextval[1] = **0**（固定）

---

## 手算 nextval 示例

模式串 T = **"abaabc"**，已知 next 数组：

| j | 1 | 2 | 3 | 4 | 5 | 6 |
|---|---|---|---|---|---|---|
| T[j] | a | b | a | a | b | c |
| next[j] | 0 | 1 | 1 | 2 | 2 | 3 |

计算 nextval：

| j | 判断 | nextval |
|---|------|---------|
| 1 | 固定 | **0** |
| 2 | T[2]=b, T[1]=a, 不相等 | nextval[2] = next[2] = **1** |
| 3 | T[3]=a, T[1]=a, **相等** | nextval[3] = nextval[1] = **0** |
| 4 | T[4]=a, T[2]=b, 不相等 | nextval[4] = next[4] = **2** |
| 5 | T[5]=b, T[2]=b, **相等** | nextval[5] = nextval[2] = **1** |
| 6 | T[6]=c, T[3]=a, 不相等 | nextval[6] = next[6] = **3** |

最终 nextval = **[0, 1, 0, 2, 1, 3]**

---

## 再示例：T = "aaaaab"

| j | 1 | 2 | 3 | 4 | 5 | 6 |
|---|---|---|---|---|---|---|
| T[j] | a | a | a | a | a | b |
| next[j] | 0 | 1 | 2 | 3 | 4 | 5 |
| **nextval[j]** | **0** | **0** | **0** | **0** | **0** | **5** |

> 当模式串为 **"aaaaab"** 时，nextval 使前 5 个位置失配时 j 都跳到 0，大幅减少比较次数。

---

## next vs nextval 总结

| 数组 | 优化程度 | 计算复杂度 | 使用场景 |
|------|---------|-----------|---------|
| next | 基础 | 简单 | 一般情况 |
| nextval | 更优 | 稍复杂 | 模式串有重复字符时效果明显 |

---

## 考试辨析

- 408 考试 nextval 与 next **均可能考察**（通常在同一道选择题中）
- 手算时务必**逐位判断**：$P[j]$ 与 $P[next[j]]$ 是否相等
- nextval[1] = 0 是固定规则（下标从1开始时）`,
  },

  // ============================================================
  // 第5章 树与二叉树
  // ============================================================

  // --- 5.1 树的基本概念 ---
  {
    id: 'ds-5-1-1',
    chapterId: 'ds-5',
    title: '树的定义与基本术语',
    keyConcepts: ['树', '根', '结点', '度', '叶子', '分支结点', '孩子', '双亲', '深度', '高度', '有序树', '无序树', '森林'],
    relatedPoints: ['ds-5-1-2'],
    content: `## 树的定义
> 树 (Tree) 是 $n$（$n \\ge 0$）个结点的**有限集**。当 $n=0$ 时称为**空树**。

在任意一棵非空树中：
1. 有且仅有一个特定的称为**根 (Root)** 的结点
2. 其余结点可分为 $m$（$m > 0$）个互不相交的有限集 $T_1, T_2, \\ldots, T_m$，每个集合本身又是一棵树——称为根的**子树 (SubTree)**

> 树的定义是**递归**的——树由根和子树组成，子树本身也是树。

---

## 树的基本术语

| 术语 | 含义 |
|------|------|
| **结点的度** | 该结点的孩子个数 |
| **树的度** | 树中所有结点的度的最大值 |
| **叶子 (Leaf)** | 度为 0 的结点（终端结点） |
| **分支结点** | 度 > 0 的结点（非终端结点） |
| **孩子 (Child)** | 某结点的子树的根 |
| **双亲 (Parent)** | 若 B 是 A 的孩子，则 A 是 B 的双亲 |
| **兄弟 (Sibling)** | 同一双亲的孩子之间互称兄弟 |
| **路径** | 从根到某结点所经过的分支序列 |
| **路径长度** | 路径上所经过的"边"的个数 |
| **祖先** | 从根到某结点路径上的所有结点 |
| **子孙** | 某结点的子树中的所有结点 |
| **结点的深度** | 从根开始向下数（根深度为 1） |
| **结点的高度** | 从叶结点开始向上数（叶高度为 1） |
| **树的高度/深度** | 树中结点的最大层数 |

---

## 树的表示方法

| 方法 | 说明 |
|------|------|
| 树形图 | 直观的图形表示 |
| 嵌套集合 | 集合圈套圈 |
| 凹入表示法 | 缩进表示（类似目录结构） |
| 广义表 | $(A(B(D,E), C(F)))$ |

---

## 考试辨析

- 树不能为空（408教材一般约定）——这与二叉树不同（二叉树可以为空）
- 根结点没有前驱，其余每个结点有且仅有一个前驱
- 树中结点可以有多个后继——这是树与线性表的本质区别`,
  },
  {
    id: 'ds-5-1-2',
    chapterId: 'ds-5',
    title: '树的性质',
    keyConcepts: ['树的性质', '结点数=总度数+1', '度为m的树', 'm叉树'],
    relatedPoints: ['ds-5-1-1', 'ds-5-2-2'],
    content: `## 树的基本性质

### 性质 1：结点数 = 总度数 + 1

> 树中结点数 $n$ 等于所有结点的度数之和再加 1。
>
> $$n = \\sum_{i=1}^{n} d_i + 1 = n_0 + n_1 + n_2 + \\cdots + n_m$$

其中 $n_k$ 表示度为 $k$ 的结点个数。

**推导**：除根结点外，每个结点都有一条来自双亲的边——即每个结点恰好对应其双亲结点的一个度。

---

### 性质 2：度为 $m$ 的树中第 $i$ 层最多有 $m^{i-1}$ 个结点

根结点位于第 1 层，逐层最多有 $1, m, m^2, \\ldots$ 个结点。

---

### 性质 3：高度为 $h$ 的 $m$ 叉树最多结点数

$$\\frac{m^h - 1}{m - 1}$$

---

### 性质 4：$n$ 个结点的 $m$ 叉树最小高度

$$\\lceil \\log_m(n(m-1)+1) \\rceil$$

---

## 度为 m 的树 vs m 叉树（高频辨析）

| 概念 | 定义 | 举例 |
|------|------|------|
| **度为 m 的树** | 树中结点的最大度为 $m$ **且至少有一个结点度为 $m$** | 必须存在度为 m 的结点 |
| **m 叉树** | 每个结点最多有 $m$ 个孩子 | 可以所有结点度 $< m$ |
| **有序 m 叉树** | m 叉树中孩子按顺序排列 | 二叉树是最常用的有序树 |

> 度为 m 的树一定是 m 叉树，但 m 叉树不一定是度为 m 的树。

---

## 考试辨析

- **结点数 = 总度数 + 1** 是解决树中结点计数问题的核心公式
- 给定 $n_0, n_1, n_2, \\ldots$，可借助总度数公式求解任何未知量
- 区分"度为 m"和"m 叉"——前者要求至少有一个结点度恰好为 m`,
  },

  // --- 5.2 二叉树的概念 ---
  {
    id: 'ds-5-2-1',
    chapterId: 'ds-5',
    title: '二叉树的定义与基本形态',
    keyConcepts: ['二叉树', '左子树', '右子树', '满二叉树', '完全二叉树', '二叉排序树', '平衡二叉树'],
    relatedPoints: ['ds-5-1-1', 'ds-5-2-2'],
    content: `## 二叉树的定义

> 二叉树是 $n$（$n \\ge 0$）个结点的有限集，或为空集（空二叉树），或由一个根结点和两棵互不相交、分别称为**左子树**和**右子树**的二叉树组成。

---

## 二叉树 vs 树的关键区别

| 区别 | 树 | 二叉树 |
|------|-----|--------|
| 子树顺序 | 无序 | **严格区分左右** |
| 可以为空 | 408教材不可为空 | **可以为空** |
| 度为 2 的树 | 至少有一个结点度为 2 | 可以没有度为 2 的结点 |

> 二叉树**不是树的特例**，而是两个不同的概念。二叉树严格区分左右子树。

---

## 几种特殊的二叉树

| 类型 | 定义 | 特征 |
|------|------|------|
| **满二叉树** | 每层结点数都达到最大值 | 高度为 $h$ 时有 $2^h-1$ 个结点 |
| **完全二叉树** | 结点编号与满二叉树一一对应（从上到下、从左到右） | 度为 1 的结点只有 0 或 1 个 |
| **二叉排序树 (BST)** | 左子树 $<$ 根 $<$ 右子树 | 用于查找 |
| **平衡二叉树 (AVL)** | 任意结点的左右子树高度差 $\\le 1$ | 用于高效查找 |

---

## 完全二叉树的判定

> 将结点从上到下、从左到右依次编号（从 1 开始），若任何一个结点的编号都与等高度的满二叉树中相同位置的编号一致，则为完全二叉树。

---

## 考试辨析

- 二叉树**严格区分左右子树**——两棵不同的二叉树即使存储的值相同，但左右子树互换就是两棵不同的树
- 完全二叉树中度为 1 的结点只能是 0 或 1 个
- 满二叉树一定是完全二叉树，完全二叉树不一定是满二叉树`,
  },
  {
    id: 'ds-5-2-2',
    chapterId: 'ds-5',
    title: '二叉树的性质',
    keyConcepts: ['二叉树性质', 'n0=n2+1', '第k层最多结点', '高度公式', '完全二叉树'],
    relatedPoints: ['ds-5-2-1', 'ds-5-1-2'],
    content: `## 二叉树的五大性质（高频必考）

### 性质 1：叶子结点数 = 度为 2 的结点数 + 1

$$n_0 = n_2 + 1$$

**推导**：$n = n_0 + n_1 + n_2 = (n_1 + 2n_2) + 1$  →  $n_0 = n_2 + 1$

> 这是 408 最爱考的性质，必须会推导。

---

### 性质 2：第 $k$ 层最多有 $2^{k-1}$ 个结点

（根结点在第 1 层，$k \\\\ge 1$）

---

### 性质 3：高度为 $h$ 的二叉树最多有 $2^h - 1$ 个结点

（空树 $h = 0$，只有根结点 $h = 1$）

---

### 性质 4：$n$ 个结点的完全二叉树的高度

$$h = \\\\lfloor \\\\log_2 n \\\\rfloor + 1 \\\\quad \\\\text{或} \\\\quad h = \\\\lceil \\\\log_2 (n+1) \\\\rceil$$

---

### 性质 5：完全二叉树中度为 1 的结点数

> $n_1$ 只能是 **0 或 1**。

| $n$ 的奇偶 | $n_1$ |
|-----------|-------|
| $n$ 为奇数 | $n_1 = 0$ |
| $n$ 为偶数 | $n_1 = 1$ |

---

## 完全二叉树的编号关系

若对完全二叉树从 1 开始按层编号：

| 结点 $i$ | 关系 |
|----------|------|
| 双亲 | $\\\\lfloor i/2 \\\\rfloor$ |
| 左孩子 | $2i$（若 $2i \\\\le n$） |
| 右孩子 | $2i+1$（若 $2i+1 \\\\le n$） |
| 所在层次 | $\\\\lfloor \\\\log_2 i \\\\rfloor + 1$ |

---

## 考试辨析

- $n_0 = n_2 + 1$ 是选择+填空题的高频考点
- 已知结点总数 $n$，可通过性质联立方程求出 $n_0$、$n_1$、$n_2$
- 完全二叉树的编号关系常用于堆排序（第8章）`,
  },
  {
    id: 'ds-5-2-3',
    chapterId: 'ds-5',
    title: '二叉树的存储结构',
    keyConcepts: ['顺序存储', '链式存储', '二叉链表', '三叉链表'],
    relatedPoints: ['ds-5-2-1', 'ds-2-2-1', 'ds-2-3-1'],
    content: `## 顺序存储

> 将二叉树结点按**完全二叉树的编号**存入一维数组。空结点需占位（用 0 或特殊符号）。

| 特点 | 说明 |
|------|------|
| 适用 | **完全二叉树**和**满二叉树**（无空间浪费） |
| 不适合 | 一般二叉树（空间浪费严重，最坏右单支需 $2^n-1$ 空间） |
| 访问 | 通过编号公式 $O(1)$ 找到双亲/孩子 |

---

## 链式存储（最常用）

### 二叉链表

\`\`\`c
typedef struct BiTNode {
    ElemType data;
    struct BiTNode *lchild, *rchild;
} BiTNode, *BiTree;
\`\`\`

| 特点 | 说明 |
|------|------|
| 指针域 | 2 个（lchild + rchild） |
| 空指针数 | $n+1$ 个（共 $2n$ 个链域，$n-1$ 个非空） |

### 三叉链表

\`\`\`c
typedef struct TriTNode {
    ElemType data;
    struct TriTNode *lchild, *rchild, *parent;
} TriTNode, *TriTree;
\`\`\`

> 多一个 parent 指针，方便找双亲——但空间开销更大。

---

## 顺序 vs 链式存储对比

| 存储方式 | 优点 | 缺点 |
|----------|------|------|
| 顺序存储 | 随机访问，不需指针 | 仅适合完全二叉树 |
| 链式存储 | 灵活，适合各种形态 | 需额外指针空间 |

---

## 考试辨析

- $n$ 个结点的二叉链表中共有 **$n+1$ 个空指针域**——这也是线索二叉树的"可利用空间"
- 完全二叉树用顺序存储效率高，一般二叉树用链式存储`,
  },

  // --- 5.3 二叉树的遍历和线索二叉树 ---
  {
    id: 'ds-5-3-1',
    chapterId: 'ds-5',
    title: '二叉树的遍历',
    keyConcepts: ['先序', '中序', '后序', '层次遍历', '递归', '非递归', '栈', '队列'],
    relatedPoints: ['ds-5-2-1', 'ds-3-1-1', 'ds-3-2-1'],
    content: `## 四种遍历方式

| 遍历方式 | 顺序 | 递归实现 | 非递归辅助 |
|----------|------|----------|-----------|
| **先序 (NLR)** | 根 → 左 → 右 | 简单 | 栈 |
| **中序 (LNR)** | 左 → 根 → 右 | 简单 | 栈 |
| **后序 (LRN)** | 左 → 右 → 根 | 简单 | 栈（复杂，需记录访问状态） |
| **层次遍历** | 逐层从左到右 | — | **队列** |

---

## 递归遍历代码

\`\`\`c
// 先序
void PreOrder(BiTree T) {
    if (T) { visit(T); PreOrder(T->lchild); PreOrder(T->rchild); }
}
// 中序
void InOrder(BiTree T) {
    if (T) { InOrder(T->lchild); visit(T); InOrder(T->rchild); }
}
// 后序
void PostOrder(BiTree T) {
    if (T) { PostOrder(T->lchild); PostOrder(T->rchild); visit(T); }
}
\`\`\`

---

## 非递归中序遍历（栈）

\`\`\`c
void InOrder_NonRecur(BiTree T) {
    Stack S; InitStack(&S);
    BiTree p = T;
    while (p || !StackEmpty(S)) {
        if (p) {
            Push(&S, p); p = p->lchild;  // 一路向左
        } else {
            Pop(&S, &p); visit(p);
            p = p->rchild;                // 转向右子树
        }
    }
}
\`\`\`

---

## 层次遍历（队列）

\`\`\`c
void LevelOrder(BiTree T) {
    Queue Q; InitQueue(&Q);
    EnQueue(&Q, T);
    while (!QueueEmpty(Q)) {
        DeQueue(&Q, &p);
        visit(p);
        if (p->lchild) EnQueue(&Q, p->lchild);
        if (p->rchild) EnQueue(&Q, p->rchild);
    }
}
\`\`\`

---

## 时间复杂度

所有遍历算法的时间复杂度均为 $O(n)$（每个结点访问一次）。

递归遍历的空间复杂度为 $O(h)$（递归栈深度 = 树高），最坏 $O(n)$。

---

## 考试辨析

- 先序/中序/后序都属于**深度优先遍历(DFS)**
- 层次遍历属于**广度优先遍历(BFS)**
- 中序非递归算法是408常考代码，需要能手写`,
  },
  {
    id: 'ds-5-3-2',
    chapterId: 'ds-5',
    title: '遍历序列重建二叉树',
    keyConcepts: ['重建', '先序+中序', '后序+中序', '层序+中序', '唯一确定'],
    relatedPoints: ['ds-5-3-1'],
    content: `## 唯一确定二叉树的条件

> **需要中序序列 + 任意一种其他遍历序列** 才能唯一确定二叉树。

| 序列组合 | 能否唯一确定 |
|----------|------------|
| 先序 + 中序 | ✅ 能 |
| 后序 + 中序 | ✅ 能 |
| 层序 + 中序 | ✅ 能 |
| 先序 + 后序 | ❌ 不能（无法区分子树结构） |
| 单个序列 | ❌ 不能 |

---

## 重建方法（先序 + 中序）

1. **先序第一个** = 根结点
2. 在中序中找到根，**左边是左子树，右边是右子树**
3. 根据中序中左右子树的结点数，在先序中划分左右子树
4. 递归重建左右子树

---

## 例题

> 先序: A B D E C F G
> 中序: D B E A F C G

1. 根 = A（先序第一个）
2. 中序中 A 左边 DBE = 左子树（3结点），右边 FCG = 右子树
3. 先序中 A 后 3 个 BDE = 左子树先序，剩下 CFG = 右子树先序
4. 递归...

---

## 特殊遍历结论

| 条件 | 结论 |
|------|------|
| 先序 = 中序 | 所有非叶结点**只有右子树** |
| 后序 = 中序 | 所有非叶结点**只有左子树** |
| 先序 = 后序 | 只有根结点（或为单结点树） |

> **2017年真题**考查了"先序和中序相同"的结论。

---

## 考试辨析

- **中序是必须的**——没有中序序列就无法区分左右子树
- 408 常在选择题中考"以下哪组序列能唯一确定二叉树"
- 手工重建时注意区分左右子树的结点数对应关系`,
  },
  {
    id: 'ds-5-3-3',
    chapterId: 'ds-5',
    title: '线索二叉树的基本概念',
    keyConcepts: ['线索二叉树', 'ltag', 'rtag', '前驱', '后继', '空指针利用', 'n+1'],
    relatedPoints: ['ds-5-3-1', 'ds-5-3-4'],
    content: `## 为什么需要线索二叉树

> 传统二叉链表中，有 **$n+1$ 个空指针域**（共 $2n$ 个指针，$n-1$ 个非空）。
> 线索二叉树**利用这些空指针域**存放遍历序列中的前驱/后继信息。

---

## 线索二叉树的结点结构

\`\`\`c
typedef struct ThreadNode {
    ElemType data;
    struct ThreadNode *lchild, *rchild;
    int ltag, rtag;   // 标志位：0=指向孩子，1=线索
} ThreadNode, *ThreadTree;
\`\`\`

| 标志 | 值为 0 | 值为 1 |
|------|--------|--------|
| **ltag** | lchild 指向**左孩子** | lchild 指向**前驱**（线索） |
| **rtag** | rchild 指向**右孩子** | rchild 指向**后继**（线索） |

---

## 中序线索二叉树

> 中序线索二叉树是最常用的，因为其前驱和后继都能有效找到。

**中序线索树的遍历**：
- 若 rtag=1：直接通过 rchild 线索获取后继
- 若 rtag=0：后继 = **右子树中最左下的结点**

---

## 寻找前驱（中序线索）

- 若 ltag=1：直接通过 lchild 线索获取前驱
- 若 ltag=0：前驱 = **左子树中最右下的结点**

---

## 考试辨析

- $n$ 个结点的二叉链表有 **$n+1$** 个空指针域——线索二叉树的利用基础
- 标志位是线索二叉树与普通二叉树的唯一结构区别
- 先序线索树找前驱困难（可能需要遍历），后序线索树找后继困难`,
  },
  {
    id: 'ds-5-3-4',
    chapterId: 'ds-5',
    title: '线索二叉树的构造',
    keyConcepts: ['线索化', '中序线索化', 'pre指针', '递归'],
    relatedPoints: ['ds-5-3-3'],
    content: `## 中序线索化的构造

通过中序遍历，用一个全局 **pre 指针** 记录上一次访问的结点，建立 pre 与当前结点的线索关系。

\`\`\`c
// pre 为全局变量，初始为 NULL
void InThread(ThreadTree p) {
    if (p) {
        InThread(p->lchild);         // 递归左子树

        if (p->lchild == NULL) {     // 建立前驱线索
            p->lchild = pre;
            p->ltag = 1;
        }
        if (pre && pre->rchild == NULL) {  // 建立后继线索
            pre->rchild = p;
            pre->rtag = 1;
        }
        pre = p;                     // 更新 pre

        InThread(p->rchild);         // 递归右子树
    }
}
\`\`\`

---

## 通过线索二叉树遍历（中序）

\`\`\`c
// 找到中序线索树中第一个被访问的结点
ThreadNode *FirstNode(ThreadNode *p) {
    while (p->ltag == 0) p = p->lchild;
    return p;
}

// 找后继
ThreadNode *NextNode(ThreadNode *p) {
    if (p->rtag == 1) return p->rchild;
    return FirstNode(p->rchild);
}

// 中序遍历
void InOrder(ThreadNode *T) {
    for (ThreadNode *p = FirstNode(T); p; p = NextNode(p))
        visit(p);
}
\`\`\`

---

## 三种线索化对比

| 线索化类型 | 找前驱 | 找后继 |
|-----------|--------|--------|
| 先序线索 | 困难（需知道双亲） | 简单 |
| 中序线索 | 简单 | 简单 |
| 后序线索 | 简单 | 困难（需知道双亲） |

> 只有**中序线索二叉树**能方便地找到任意结点的前驱和后继。

---

## 考试辨析

- 线索化的本质：将中序遍历中的 pre（前一个访问结点）与当前结点相连
- **最后一个被访问结点**的 rchild 仍为 NULL（需要额外处理）
- 线索化不改变原遍历序列，只改变空指针域的内容`,
  },

  // --- 5.4 树、森林 ---
  {
    id: 'ds-5-4-1',
    chapterId: 'ds-5',
    title: '树的存储结构',
    keyConcepts: ['双亲表示法', '孩子表示法', '孩子兄弟表示法', '左孩子右兄弟'],
    relatedPoints: ['ds-5-1-1', 'ds-2-2-1', 'ds-2-3-1'],
    content: `## 三种存储结构

---

### 一、双亲表示法

用数组存储每个结点及其双亲的位置。

\`\`\`c
#define MaxSize 100
typedef struct {
    ElemType data;
    int parent;     // 双亲在数组中的下标（根为 -1）
} PTNode;
typedef struct {
    PTNode nodes[MaxSize];
    int n;
} PTree;
\`\`\`

| 特点 | 说明 |
|------|------|
| 找双亲 | $O(1)$ |
| 找孩子 | $O(n)$ — 需遍历整个数组 |
| 适用 | 并查集（只需向上查找） |

---

### 二、孩子表示法

每个结点用一个链表存储其所有孩子。

> 找孩子容易，找双亲困难。可结合双亲表示法 = 双亲孩子表示法。

---

### 三、孩子兄弟表示法（⭐ 最常用）

\`\`\`c
typedef struct CSNode {
    ElemType data;
    struct CSNode *firstchild, *nextsibling;
} CSNode, *CSTree;
\`\`\`

> **左孩子右兄弟**：firstchild 指向第一个孩子，nextsibling 指向下一个兄弟。

---

## 三种表示法对比

| 表示法 | 找双亲 | 找孩子 | 转二叉树 |
|--------|--------|--------|---------|
| 双亲表示法 | $O(1)$ | $O(n)$ | — |
| 孩子表示法 | $O(n)$ | 容易 | — |
| 孩子兄弟表示法 | 困难 | 容易 | ✅ 直接对应 |

---

## 考试辨析

- **孩子兄弟表示法（左孩子右兄弟）** 是树与二叉树转换的基础
- 并查集使用**双亲表示法**存储树`,
  },
  {
    id: 'ds-5-4-2',
    chapterId: 'ds-5',
    title: '树、森林与二叉树的转换',
    keyConcepts: ['树转二叉树', '森林转二叉树', '二叉树转树', '左孩子右兄弟'],
    relatedPoints: ['ds-5-4-1', 'ds-5-2-1'],
    content: `## 树 → 二叉树

> 规则：**左孩子右兄弟**。

1. 每个结点只保留其**第一个孩子**的连线（左孩子）
2. 兄弟之间加连线，从左到右串联（右孩子）
3. 以根为轴心，将树顺时针旋转 45°

> 转换后的二叉树**根结点一定没有右子树**。

---

## 森林 → 二叉树

1. 先将各棵树分别转为二叉树
2. 将各二叉树的根通过右指针串联起来（后一棵树的根作为前一棵树根的右孩子）

> 转换后第一棵树的根 = 二叉树的根。

---

## 二叉树 → 树

1. 从根开始，沿右链分离各结点（右孩子变兄弟）
2. 恢复兄弟间的原父子关系

---

## 转换的不变性

> 转换后的二叉树与原来的树/森林**具有相同的遍历序列对应关系**。

---

## 考试辨析

- 树转二叉树后**根结点无右子树**——利用此性质可判断某二叉树是否由树转换而来
- 森林转二叉树后根结点**可能有右子树**（连接后续树的根）`,
  },
  {
    id: 'ds-5-4-3',
    chapterId: 'ds-5',
    title: '树和森林的遍历',
    keyConcepts: ['先根遍历', '后根遍历', '先序森林', '中序森林', '遍历对应'],
    relatedPoints: ['ds-5-4-2', 'ds-5-3-1'],
    content: `## 树的遍历

| 遍历方式 | 顺序 | 对应二叉树遍历 |
|----------|------|--------------|
| **先根遍历** | 先访问根，再依次先根遍历各子树 | **先序**遍历 |
| **后根遍历** | 先依次后根遍历各子树，再访问根 | **中序**遍历 |

---

## 森林的遍历

| 遍历方式 | 顺序 | 对应二叉树遍历 |
|----------|------|--------------|
| **先序遍历森林** | 先访问第一棵树的根，再先序遍历第一棵树的子树森林，然后先序遍历剩余的树 | **先序**遍历 |
| **中序遍历森林** | 先中序遍历第一棵树的子树森林，再访问第一棵树的根，然后中序遍历剩余的树 | **中序**遍历 |

---

## 遍历对应关系总结

| 树/森林 | 对应的二叉树 |
|---------|------------|
| 树先根遍历 | 二叉树**先序**遍历 |
| 树后根遍历 | 二叉树**中序**遍历 |
| 森林先序遍历 | 二叉树**先序**遍历 |
| 森林中序遍历 | 二叉树**中序**遍历 |

> **树的遍历中没有"中根遍历"**——因为树的子树没有左右之分，谈论"中根"无意义。

---

## 考试辨析

- 树/森林的遍历与二叉树遍历的对应关系是高频考点
- 树的遍历只有先根和后根，没有中根
- 做题时可用转换后的二叉树来模拟树/森林的遍历结果`,
  },

  // --- 5.5 树与二叉树的应用 ---
  {
    id: 'ds-5-5-1',
    chapterId: 'ds-5',
    title: '哈夫曼树与哈夫曼编码',
    keyConcepts: ['哈夫曼树', 'WPL', '带权路径长度', '哈夫曼编码', '前缀编码', '2n-1'],
    relatedPoints: ['ds-5-2-1', 'ds-5-2-2'],
    content: `## 基本概念

| 术语 | 定义 |
|------|------|
| **权值 (Weight)** | 树结点赋予的有意义的数值 |
| **带权路径长度 (WPL)** | $\\\\sum_{i=1}^{n} w_i \\\\times l_i$（$l_i$ 为从根到第 $i$ 个叶子的路径长度） |
| **哈夫曼树** | 带权路径长度最小的二叉树（最优二叉树） |

---

## 哈夫曼树的构造算法

1. 将 $n$ 个结点作为 $n$ 棵单结点二叉树，构成森林 $F$
2. 从 $F$ 中选取**权值最小**的两棵树作为左右子树，合并成一棵新树（新根权值 = 两子树根权值之和）
3. 将新树加入 $F$，重复直到只剩一棵树

\`\`\`c
// 哈夫曼树的结点结构
typedef struct {
    int weight;
    int parent, lchild, rchild;
} HTNode, *HuffmanTree;
\`\`\`

---

## 哈夫曼树的特性

| 特性 | 说明 |
|------|------|
| 无度为 1 的结点 | 只有度为 0（叶子）和度为 2（内部） |
| 总结点数 | $2n - 1$（$n$ 个叶子结点） |
| 叶子数 | $n_0 = n$ |
| 分支结点数 | $n_2 = n - 1$ |
| WPL 不唯一 | 可能有多棵等价的哈夫曼树 |

---

## 哈夫曼编码

| 特性 | 说明 |
|------|------|
| **前缀编码** | 任何一个字符的编码都不是另一个字符编码的前缀 |
| 左 0 右 1 | 从根到叶子的路径：走左孩子得 0，走右孩子得 1 |
| 最优性 | 哈夫曼编码可使总编码长度最短 |

---

## 考试辨析

- **WPL 计算**：选择题常给一棵树要求计算或比较 WPL
- **$2n - 1$**：$n$ 个权值构造哈夫曼树，最终有 $2n-1$ 个结点
- 哈夫曼树**可能有多棵**（权值相等时有不同合并顺序），但最小 WPL 唯一
- **2012年真题**考了多路归并排序的最优合并顺序（本质是哈夫曼树）`,
  },
  {
    id: 'ds-5-5-2',
    chapterId: 'ds-5',
    title: '并查集',
    keyConcepts: ['并查集', 'Find', 'Union', '路径压缩', '按秩合并', '双亲表示法'],
    relatedPoints: ['ds-5-4-1', 'ds-6-4-1'],
    content: `## 并查集的定义

> 并查集 (Disjoint Set Union, DSU) 支持**查询 (Find)** 元素所属集合和**合并 (Union)** 两个集合。

---

## 存储结构 — 双亲表示法

\`\`\`c
#define SIZE 100
int father[SIZE];   // father[i] 表示 i 的双亲，根结点 father = -1
\`\`\`

---

## 核心操作

### Find — 查根

\`\`\`c
// 朴素查找
int Find(int x) {
    while (father[x] >= 0) x = father[x];
    return x;
}

// 带路径压缩的查找
int Find_Compress(int x) {
    if (father[x] < 0) return x;
    return father[x] = Find_Compress(father[x]);
}
\`\`\`

> **路径压缩**：查找过程中把路径上所有结点直接挂到根上，加速后续查找。

### Union — 合并

\`\`\`c
// 朴素合并：将 root2 挂到 root1
void Union(int root1, int root2) {
    father[root2] = root1;
}

// 按秩合并：将矮树挂到高树上
void Union_Rank(int root1, int root2) {
    if (father[root1] < father[root2])  // root1 树更高
        father[root2] = root1;
    else if (father[root1] > father[root2])
        father[root1] = root2;
    else {
        father[root2] = root1;
        father[root1]--;  // 高度 +1
    }
}
\`\`\`

---

## 优化效果

| 优化 | Find 复杂度 | Union 复杂度 |
|------|-----------|------------|
| 无优化 | $O(n)$ | $O(1)$ |
| 路径压缩 | 均摊 $O(\\\\alpha(n))$ | $O(1)$ |
| 按秩合并 + 路径压缩 | 均摊 $O(\\\\alpha(n))$ | 均摊 $O(\\\\alpha(n))$ |

> $\\\\alpha(n)$ 是 Ackermann 函数的反函数，增长极慢，实际上可视为常数。

---

## 考试辨析

- 并查集使用**双亲表示法**（数组）
- 路径压缩不影响集合合并的正确性
- 408 常在图中考并查集的应用（判断图的连通性、Kruskal 算法）`,
  },
  {
    id: 'ds-5-5-3',
    chapterId: 'ds-5',
    title: '堆及其应用',
    keyConcepts: ['堆', '小根堆', '大根堆', '完全二叉树', '堆排序', '优先队列'],
    relatedPoints: ['ds-5-2-1', 'ds-5-2-2', 'ds-8-4-1'],
    content: `## 堆的定义

> 堆 (Heap) 是一棵**完全二叉树**，且每个结点的值都不大于（或不小于）其孩子结点的值。

| 类型 | 条件 | 堆顶 |
|------|------|------|
| **小根堆** | 每个结点 $\\\\le$ 其孩子 | 最小值 |
| **大根堆** | 每个结点 $\\\\ge$ 其孩子 | 最大值 |

---

## 堆的存储

> 堆用**顺序存储**（数组），利用完全二叉树的编号规律。

\`\`\`c
// 数组下标从 1 开始
// 结点 i 的左孩子 = 2i，右孩子 = 2i+1，双亲 = i/2
\`\`\`

---

## 堆的核心操作

### 上滤 (Percolate Up / Shift Up)

插入新元素时，放在数组末尾，然后**向上调整**。

\`\`\`c
void ShiftUp(int heap[], int k) {
    while (k > 1 && heap[k] < heap[k/2]) {
        swap(heap[k], heap[k/2]);
        k = k / 2;
    }
}
\`\`\`

### 下滤 (Percolate Down / Shift Down)

删除堆顶时，将最后一个元素移到堆顶，然后**向下调整**。

\`\`\`c
void ShiftDown(int heap[], int n, int k) {
    while (2*k <= n) {
        int j = 2*k;                     // 左孩子
        if (j < n && heap[j] > heap[j+1]) j++;  // 取更小的孩子
        if (heap[k] <= heap[j]) break;
        swap(heap[k], heap[j]);
        k = j;
    }
}
\`\`\`

---

## 堆的建立

> 从最后一个非叶子结点开始（$n/2$），依次向下调整 → $O(n)$。

\`\`\`c
void BuildHeap(int heap[], int n) {
    for (int i = n/2; i >= 1; i--)
        ShiftDown(heap, n, i);
}
\`\`\`

---

## 堆的应用

| 应用 | 说明 |
|------|------|
| **堆排序** | $O(n\\\\log n)$，见第8章 |
| **优先队列** | 总是取出优先级最高（或最低）的元素 |
| **Top-K 问题** | 维护大小为 K 的堆求前 K 大/小的元素 |

---

## 考试辨析

- 堆一定是一棵**完全二叉树**（这是定义要求）
- 建堆时间复杂度是 **$O(n)$**（不是 $O(n\\\\log n)$！）——这是常考点
- 堆不保证结点值的全序关系，只保证父子间的大小关系
- 2025年考纲明确新增"堆及其应用`,
  },

  // ============================================================
  // 第6章 图
  // ============================================================

  // --- 6.1 图的基本概念 ---
  {
    id: 'ds-6-1-1',
    chapterId: 'ds-6',
    title: '图的基本概念与术语',
    keyConcepts: ['图', '顶点', '边', '有向图', '无向图', '完全图', '度', '入度', '出度', '连通', '强连通', '生成树'],
    relatedPoints: ['ds-5-1-1'],
    content: `## 图的定义
> 图 (Graph) 由顶点集 $V$ 和边集 $E$ 组成，记作 $G = (V, E)$。
$V$ 是非空有限集（至少一个顶点），$E$ 可以是空集。

---
## 图的基本分类
| 类型 | 定义 | 完全图边数 |
|------|------|-----------|
| **无向图** | 边无方向，$(u,v)$ 与 $(v,u)$ 同一条 | $n(n-1)/2$ |
| **有向图** | 边有方向，$<u,v>$ 不同于 $<v,u>$ | $n(n-1)$ |

---
## 核心术语
| 术语 | 含义 |
|------|------|
| **度** | 无向图中与该顶点关联的边数 |
| **入度 / 出度** | 有向图中指向/指出该顶点的边数 |
| **简单图** | 无重复边，无自环 |
| **多重图** | 有重复边或自环 |
| **路径** | 顶点序列 $v_1, v_2, \\ldots, v_k$，相邻顶点间有边 |
| **简单路径** | 路径中顶点不重复 |
| **回路/环** | 起点和终点相同的路径 |

---
## 度的公式（必考）
无向图：$\\sum \\deg(v) = 2|E|$
有向图：$\\sum \\text{入度} = \\sum \\text{出度} = |E|$

---
## 连通性
| 概念 | 说明 |
|------|------|
| **连通图**（无向） | 任意两顶点间有路径 |
| **连通分量** | 无向图的极大连通子图 |
| **强连通图**（有向） | 任意两顶点间双向均有路径 |
| **强连通分量** | 有向图的极大强连通子图 |
| **生成树** | 包含所有 $n$ 个顶点的极小连通子图，恰有 $n-1$ 条边 |

---
## 考试辨析
- $n$ 个顶点的连通图**至少**有 $n-1$ 条边（即生成树）
- $n$ 个顶点的强连通有向图**至少**有 $n$ 条边（成环）
- $\\sum \\deg = 2|E|$ 是图论最基础的恒等式，选择题高频出现`,
  },

  // --- 6.2 图的存储及基本操作 ---
  {
    id: 'ds-6-2-1',
    chapterId: 'ds-6',
    title: '图的存储 — 邻接矩阵',
    keyConcepts: ['邻接矩阵', '顺序存储', '对称矩阵', '稠密图', 'O(n^2)'],
    relatedPoints: ['ds-6-2-2', 'ds-2-2-1'],
    content: `## 邻接矩阵的定义
> 用一个 $n \\times n$ 的二维矩阵表示 $n$ 个顶点之间的邻接关系。

\\\`\\\`\\\`c
#define MaxVertexNum 100
typedef char VertexType;
typedef int EdgeType;
typedef struct {
    VertexType vex[MaxVertexNum];          // 顶点表
    EdgeType edge[MaxVertexNum][MaxVertexNum]; // 边矩阵
    int vexnum, arcnum;
} MGraph;
\\\`\\\`\\\`

---
## 特点
| 特性 | 说明 |
|------|------|
| 空间复杂度 | $O(n^2)$ |
| 适合图类型 | **稠密图** |
| 判断边 $(i,j)$ | $O(1)$（直接访问矩阵） |
| 求顶点度 | 需遍历整行/列 |
| 无向图矩阵 | **对称矩阵**（可压缩存储一半） |

---
## 考试辨析
- 邻接矩阵适合稠密图（$|E|$ 接近 $n^2$），不适合稀疏图
- 无向图的邻接矩阵一定是对称阵——可用下三角/上三角压缩存储
- 2011年真题考了邻接矩阵的压缩存储`,
  },
  {
    id: 'ds-6-2-2',
    chapterId: 'ds-6',
    title: '图的存储 — 邻接表',
    keyConcepts: ['邻接表', '链式存储', '边表', '顶点表', '稀疏图', 'O(n+e)'],
    relatedPoints: ['ds-6-2-1', 'ds-2-3-1'],
    content: `## 邻接表的定义
> 顶点用数组存储（顶点表），每个顶点的**邻接边**用链表存储（边表）。

\\\`\\\`\\\`c
// 边表结点
typedef struct ArcNode {
    int adjvex;              // 邻接点下标
    struct ArcNode *next;
    // InfoType info;        // 边权
} ArcNode;
// 顶点表
typedef struct VNode {
    VertexType data;
    ArcNode *first;          // 第一条边
} VNode, AdjList[MaxVertexNum];
// 邻接表
typedef struct {
    AdjList vertices;
    int vexnum, arcnum;
} ALGraph;
\\\`\\\`\\\`

---
## 特点
| 特性 | 说明 |
|------|------|
| 空间复杂度 | 无向图 $O(|V|+2|E|)$，有向图 $O(|V|+|E|)$ |
| 适合图类型 | **稀疏图** |
| 求顶点出度 | $O(1)$ ~ $O(n)$（有向图直接遍历边表） |
| 求顶点入度 | 需遍历全表（逆邻接表可解决） |
| 邻接表唯一吗 | **不唯一**（边表顺序可不同） |

---
## 邻接矩阵 vs 邻接表
| 比较维度 | 邻接矩阵 | 邻接表 |
|----------|---------|--------|
| 空间 | $O(n^2)$ | $O(n+e)$ |
| 适合 | 稠密图 | 稀疏图 |
| 判断边 | $O(1)$ | $O(n)$ |
| 遍历所有边 | $O(n^2)$ | $O(n+e)$ |
| 唯一性 | 唯一 | 不唯一 |

---
## 考试辨析
- 同一个图的邻接表表示**不唯一**（边表中边的顺序取决于建图顺序）
- 由此导致：基于邻接表的遍历（BFS/DFS）序列**不唯一**；
  基于邻接矩阵的遍历序列**唯一**`,
  },
  {
    id: 'ds-6-2-3',
    chapterId: 'ds-6',
    title: '十字链表与邻接多重表',
    keyConcepts: ['十字链表', '邻接多重表', '有向图', '无向图'],
    relatedPoints: ['ds-6-2-1', 'ds-6-2-2'],
    content: `## 十字链表（有向图）
> 结合邻接表（找出度容易）和逆邻接表（找入度容易）的优点。

每个顶点有一个结点，每条边有一个结点。

| 操作 | 复杂度 |
|------|--------|
| 求出度 | 沿 hlink 遍历 |
| 求入度 | 沿 tlink 遍历 |

---
## 邻接多重表（无向图）
> 解决邻接表中每条边存储两次的冗余问题。

每条边只存储**一个结点**（两个顶点共享），通过指针连接所有关联的边。

| 操作 | 复杂度 |
|------|--------|
| 删除边 | 方便（只需修改少量指针） |
| 标记边 | 方便 |

---
## 四种存储结构对比
| 结构 | 适用 | 空间 | 特点 |
|------|------|------|------|
| 邻接矩阵 | 稠密图 | $O(n^2)$ | 判断边快 |
| 邻接表 | 稀疏图 | $O(n+e)$ | 遍历快 |
| 十字链表 | 有向图 | $O(n+e)$ | 出入度都快 |
| 邻接多重表 | 无向图 | $O(n+e)$ | 删边快 |

---
## 考试辨析
- 408 对十字链表和邻接多重表只要求了解概念和适用场景
- 重点掌握邻接矩阵与邻接表的对比`,
  },

  // --- 6.3 图的遍历 ---
  {
    id: 'ds-6-3-1',
    chapterId: 'ds-6',
    title: '图的广度优先搜索 (BFS)',
    keyConcepts: ['BFS', '广度优先', '队列', '层次', '最短路径'],
    relatedPoints: ['ds-6-3-2', 'ds-3-2-1', 'ds-5-3-1'],
    content: `## BFS 算法思想
> 从某顶点出发，逐层访问所有邻接点——先访问的顶点的邻接点先被访问。

辅助数据结构：**队列**

\\\`\\\`\\\`c
bool visited[MAX_VERTEX_NUM];
void BFS(Graph G, int v) {
    Queue Q; InitQueue(&Q);
    visit(v); visited[v] = true;
    EnQueue(&Q, v);
    while (!QueueEmpty(Q)) {
        DeQueue(&Q, &v);
        for (w = FirstNeighbor(G, v); w >= 0; w = NextNeighbor(G, v, w))
            if (!visited[w]) {
                visit(w); visited[w] = true;
                EnQueue(&Q, w);
            }
    }
}
\\\`\\\`\\\`

---
## 复杂度分析
| 存储结构 | 时间复杂度 |
|----------|-----------|
| 邻接表 | $O(|V| + |E|)$ |
| 邻接矩阵 | $O(|V|^2)$ |
空间复杂度：$O(|V|)$（队列 + visited 数组）

---
## BFS 的应用
- 非带权图的**单源最短路径**
- 广度优先生成树
- 求连通分量

---
## 考试辨析
- BFS 用队列，DFS 用栈/递归——这是408反复考的知识点
- BFS 可求**无权图**单源最短路径（因为逐层扩展）
- 邻接表产生的 BFS 序列**不唯一**（边表顺序可变）`,
  },
  {
    id: 'ds-6-3-2',
    chapterId: 'ds-6',
    title: '图的深度优先搜索 (DFS)',
    keyConcepts: ['DFS', '深度优先', '递归', '栈', '回溯'],
    relatedPoints: ['ds-6-3-1', 'ds-3-1-1', 'ds-5-3-1'],
    content: `## DFS 算法思想
> 从某顶点出发，尽可能"深"地搜索——走到不能走为止，然后回溯。

辅助数据结构：**递归栈**（或显式栈）

\\\`\\\`\\\`c
bool visited[MAX_VERTEX_NUM];
void DFS(Graph G, int v) {
    visit(v); visited[v] = true;
    for (w = FirstNeighbor(G, v); w >= 0; w = NextNeighbor(G, v, w))
        if (!visited[w])
            DFS(G, w);          // 递归深入
}
\\\`\\\`\\\`

---
## 复杂度分析
| 存储结构 | 时间复杂度 |
|----------|-----------|
| 邻接表 | $O(|V| + |E|)$ |
| 邻接矩阵 | $O(|V|^2)$ |
空间复杂度：$O(|V|)$（递归栈深度）

---
## BFS vs DFS 对比
| 比较 | BFS | DFS |
|------|-----|-----|
| 辅助结构 | 队列 | 栈/递归 |
| 遍历方式 | 逐层扩展 | 深入到底再回溯 |
| 生成树 | 广度优先生成树 | 深度优先生成树 |
| 非带权最短路径 | ✅ 可以 | ❌ 不可以 |
| 判断连通性 | ✅ | ✅ |
| 判断环 | ✅ | ✅ |

---
## 考试辨析
- DFS 递归空间复杂度 $O(|V|)$（递归栈最深可达 $|V|$ 层）
- 2024年408考了拓扑排序代码——图算法代码题已连续3年出现
- 邻接矩阵的DFS/BFS序列**唯一**，邻接表**不唯一**`,
  },

  // --- 6.4 图的应用 ---
  {
    id: 'ds-6-4-1',
    chapterId: 'ds-6',
    title: '最小生成树',
    keyConcepts: ['最小生成树', 'MST', 'Prim', 'Kruskal', '贪心'],
    relatedPoints: ['ds-6-3-1', 'ds-6-3-2', 'ds-5-5-2'],
    content: `## 最小生成树定义
> 带权连通无向图中，权值之和最小的生成树。

---
## Prim 算法
| 属性 | 说明 |
|------|------|
| 策略 | 贪心——从顶点出发，每次选与当前树相连的最小权边 |
| 时间复杂度 | **$O(|V|^2)$** |
| 适合 | **稠密图** |
| 数据结构 | 数组(lowcost) |

核心操作：维护 lowcost[] 数组，每次选最小 lowcost 的顶点加入，更新 lowcost。

---
## Kruskal 算法
| 属性 | 说明 |
|------|------|
| 策略 | 贪心——从边出发，每次选最小权边，不构成回路则加入 |
| 时间复杂度 | **$O(|E| \\log |E|)$** |
| 适合 | **稀疏图** |
| 数据结构 | 堆（排序边）+ **并查集**（判回路） |

---
## Prim vs Kruskal
| | Prim | Kruskal |
|------|------|---------|
| 选边策略 | 选顶点（连入当前树） | 选边（全局最小） |
| 时间 | $O(n^2)$ | $O(e \\log e)$ |
| 密集图 | ✅ | ❌ |
| 稀疏图 | ❌ | ✅ |

---
## 考试辨析
- 若边权值**互不相等**，则最小生成树**唯一**
- Prim 和 Kruskal 都是贪心算法——局部最优导致全局最优
- Kruskal 常结合**并查集**考察`,
  },
  {
    id: 'ds-6-4-2',
    chapterId: 'ds-6',
    title: '最短路径 — Dijkstra算法',
    keyConcepts: ['Dijkstra', '单源最短路径', '贪心', 'dist[]', 'path[]', '非负权'],
    relatedPoints: ['ds-6-4-3'],
    content: `## Dijkstra 算法
> 求**单源最短路径**——从源点 $v_0$ 到其他所有顶点的最短路径。

策略：**贪心**，每次从未选顶点中选 dist 最小的加入。

---
## 核心数据结构
\\\`\\\`\\\`c
int dist[n];     // dist[i] = v0 到 i 的当前最短距离
int path[n];     // path[i] = 最短路径上 i 的前驱顶点
bool S[n];       // S[i] = true 表示 i 已确定最短路径
\\\`\\\`\\\`

---
## 时间复杂度
| 实现方式 | 时间复杂度 |
|----------|-----------|
| 朴素（遍历选最小） | $O(|V|^2)$ |
| 堆优化 | $O((|V|+|E|)\\log|V|)$ |

---
## 算法限制
> **不允许负权边！**
有负权边时 Dijkstra 可能得到错误结果。

---
## 考试辨析
- Dijkstra **不能**处理负权边——选择题常见错误选项
- 每加入一个新顶点，需要更新 dist[]（松弛操作）
- 区分 dist[i]（当前最短）和最终最短路径——加顶点后 dist 可能继续减小`,
  },
  {
    id: 'ds-6-4-3',
    chapterId: 'ds-6',
    title: '最短路径 — Floyd算法',
    keyConcepts: ['Floyd', '全源最短路径', '动态规划', '负权', 'O(n^3)'],
    relatedPoints: ['ds-6-4-2'],
    content: `## Floyd 算法
> 求**每一对顶点之间**的最短路径。

策略：**动态规划**

核心递推式：
$$A^{(k)}[i][j] = \\min\\{A^{(k-1)}[i][j],\\ A^{(k-1)}[i][k] + A^{(k-1)}[k][j]\\}$$

> 允许以 $k$ 为中间点松驰 $i \\to j$ 的路径。

\\\`\\\`\\\`c
for (int k = 0; k < n; k++)           // 中间点
    for (int i = 0; i < n; i++)       // 起点
        for (int j = 0; j < n; j++)   // 终点
            if (A[i][k] + A[k][j] < A[i][j])
                A[i][j] = A[i][k] + A[k][j];
\\\`\\\`\\\`

---
## Dijkstra vs Floyd
| | Dijkstra | Floyd |
|------|----------|-------|
| 类型 | 单源 | 全源 |
| 策略 | 贪心 | 动态规划 |
| 时间 | $O(n^2)$ | **$O(n^3)$** |
| 负权边 | ❌ | ✅（但不能有负权回路） |

---
## 考试辨析
- Floyd 的三重循环 $k$ 必须在外层——顺序不可改变
- Floyd 允许负权边但**不允许负权回路**（会无穷小）
- 408 通常以选择题形式考 Floyd 的手工模拟`,
  },
  {
    id: 'ds-6-4-4',
    chapterId: 'ds-6',
    title: '拓扑排序',
    keyConcepts: ['拓扑排序', 'AOV网', '入度', 'DAG', '有向无环图'],
    relatedPoints: ['ds-6-4-5', 'ds-3-1-1'],
    content: `## AOV 网
> Activity On Vertex：用顶点表示活动，用有向边表示先后依赖关系。

> **拓扑序列**：AOV 网中所有顶点的线性序列，满足若 $<v_i, v_j>$ 是边，则 $v_i$ 在 $v_j$ 之前。

---
## 拓扑排序算法
1. 计算所有顶点的入度
2. 将所有**入度为 0** 的顶点入栈
3. 栈非空时：出栈一个顶点输出，将其所有邻接点的入度减 1
- 若减后入度 = 0，入栈
4. 若输出顶点数 < 总顶点数 → 图中有回路

\\\`\\\`\\\`c
bool TopologicalSort(Graph G) {
    // 计算入度 indegree[]
    Stack S; InitStack(&S);
    for (int i = 0; i < G.vexnum; i++)
        if (indegree[i] == 0) Push(&S, i);
    int count = 0;
    while (!StackEmpty(S)) {
        Pop(&S, &i);
        print(i); count++;
        for (p = G.vertices[i].first; p; p = p->next) {
            int v = p->adjvex;
            if (--indegree[v] == 0)
                Push(&S, v);
        }
    }
    return count == G.vexnum;  // true=无回路
}
\\\`\\\`\\\`

---
## 复杂度
| 存储 | 时间 |
|------|------|
| 邻接表 | $O(|V| + |E|)$ |
| 邻接矩阵 | $O(|V|^2)$ |

---
## 考试辨析
- 拓扑序列通常**不唯一**
- 可以用拓扑排序判断有向图是否有环
- **2024年408考了拓扑排序的代码题**——图算法代码不再是"不太可能考"`,
  },
  {
    id: 'ds-6-4-5',
    chapterId: 'ds-6',
    title: '关键路径',
    keyConcepts: ['关键路径', 'AOE网', 've', 'vl', 'e', 'l', '时间余量', '关键活动'],
    relatedPoints: ['ds-6-4-4'],
    content: `## AOE 网
> Activity On Edge：用边表示活动，用顶点表示事件。通常用于工程工期估算。

---
## 四个关键量
| 量 | 含义 | 计算方向 | 计算方式 |
|-----|------|----------|----------|
| **ve(k)** | 事件最早发生时间 | 源点 → 汇点，**前→后** | 取 **max**（所有入边中最大） |
| **vl(k)** | 事件最迟发生时间 | 汇点 → 源点，**后→前** | 取 **min**（所有出边中最小） |
| **e(i)** | 活动最早开始时间 | = ve(弧尾) | — |
| **l(i)** | 活动最迟开始时间 | = vl(弧头) - 活动时间 | — |

> **时间余量** $d(i) = l(i) - e(i)$
> $d(i) = 0$ 的活动为**关键活动**，构成**关键路径**。

---
## 计算步骤
1. 按拓扑序计算 ve（前→后，取 max）
2. 初始化汇点 vl = ve，按逆拓扑序计算 vl（后→前，取 min）
3. 计算每条活动的 e 和 l：$e = ve(弧尾)$，$l = vl(弧头) - 权值$
4. 若 $l - e = 0$，则为关键活动

---
## 考试辨析
- 关键路径是 AOE 网中最长路径——**决定工程最短完成时间**
- 缩短关键活动可缩短工期——但**多条关键路径时缩短单一关键活动无效**
- AOE 网中**至少有一条**关键路径，可能有多条
- 408 通常考手工计算 ve/vl/e/l 表格`,
  },

  // ============================================================
  // 第7章 查找
  // ============================================================

  // --- 7.1 查找的基本概念 ---
  {
    id: 'ds-7-1-1',
    chapterId: 'ds-7',
    title: '查找的基本概念',
    keyConcepts: ['查找', '查找表', '关键字', '平均查找长度', 'ASL'],
    relatedPoints: [],
    content: `## 查找的定义

在数据集合中寻找满足某种条件的数据元素。

---

## 核心概念
| 术语 | 含义 |
|------|------|
| **查找表** | 同一类型数据元素的集合 |
| **关键字 (Key)** | 数据元素中唯一标识该元素的数据项 |
| **查找** | 根据给定值在查找表中确定关键字等于该值的记录 |
| **静态查找** | 仅查找，不修改表（顺序查找、折半查找） |
| **动态查找** | 查找 + 插入/删除（BST、散列表） |

---

## 平均查找长度 (ASL)
$$\\text{ASL} = \\sum_{i=1}^{n} P_i \\cdot C_i$$
$P_i$：查找第 $i$ 个记录的概率；$C_i$：找到第 $i$ 个记录所需的比较次数。

---

## 考试辨析
- ASL 是评价查找算法效率的核心指标
- 区分静态查找和动态查找——决定适用的数据结构`,
  },

  // --- 7.2 顺序查找和折半查找 ---
  {
    id: 'ds-7-2-1',
    chapterId: 'ds-7',
    title: '顺序查找与折半查找',
    keyConcepts: ['顺序查找', '折半查找', '二分查找', 'ASL', '有序表'],
    relatedPoints: ['ds-2-2-4'],
    content: `## 顺序查找

逐个扫描直到找到目标或遍历完毕。

| 属性 | 值 |
|------|-----|
| 适用 | 顺序表或链表，**无序/有序均可** |
| ASL(成功) | $\\frac{n+1}{2}$ |
| ASL(失败) | $n$（无序）；$n+1$（有序） |
| 时间复杂度 | $O(n)$ |

---

## 折半查找（二分查找）

**仅适用于有序的顺序表**（链表不能折半）。

| 属性 | 值 |
|------|-----|
| 适用 | **有序顺序表** |
| ASL | $\\approx \\log_2(n+1) - 1$ |
| 时间复杂度 | $O(\\log n)$ |
| 判定树 | 平衡二叉树 |

\`\`\`c
int BinarySearch(int a[], int n, int key) {
    int low = 0, high = n - 1;
    while (low <= high) {
        int mid = (low + high) / 2;
        if (a[mid] == key) return mid;
        else if (a[mid] < key) low = mid + 1;
        else high = mid - 1;
    }
    return -1;
}
\`\`\`

---

## 考试辨析
- 折半查找要求**顺序存储 + 有序**——两个条件缺一不可
- 折半查找的判定树是平衡二叉树，树高 $\\lceil \\log_2(n+1) \\rceil$
- 求 ASL 需画出判定树，计算每个结点的查找长度`,
  },
  {
    id: 'ds-7-2-2',
    chapterId: 'ds-7',
    title: '分块查找',
    keyConcepts: ['分块查找', '索引顺序查找', '块内无序', '块间有序'],
    relatedPoints: ['ds-7-2-1'],
    content: `## 分块查找的思想

将表分成若干块，**块间有序、块内无序**，建立索引表。

1. 在索引表中确定目标在哪一块（顺序或折半查索引）
2. 在块内顺序查找

---

## 复杂度
| 分块方式 | 最佳块大小 | ASL |
|----------|-----------|-----|
| $n$ 个记录分 $b$ 块 | $\\sqrt{n}$ | $\\approx \\sqrt{n} + 1$ |

$$\\text{ASL} = L_I + L_S = \\log_2(b+1) + \\frac{s+1}{2}$$

其中 $L_I$ = 索引查找，$L_S$ = 块内查找，$s$ = 每块记录数。

---

## 考试辨析
- 分块查找是顺序查找和折半查找的折中方案
- 408 对其考察较少，理解基本概念即可`,
  },

  // --- 7.3 树型查找 ---
  {
    id: 'ds-7-3-1',
    chapterId: 'ds-7',
    title: '二叉排序树 (BST)',
    keyConcepts: ['BST', '二叉排序树', '查找', '插入', '删除', '中序有序'],
    relatedPoints: ['ds-5-2-1', 'ds-5-3-1'],
    content: `## BST 定义

> 二叉排序树或为空树，或满足：
1. 若左子树非空，则左子树上所有结点的值 **<** 根结点的值
2. 若右子树非空，则右子树上所有结点的值 **>** 根结点的值
3. 左右子树本身也是 BST

**中序遍历 BST 得到递增序列。**

---

## 基本操作复杂度
| 操作 | 平均 | 最坏 |
|------|------|------|
| 查找 | $O(\\log n)$ | $O(n)$（退化为链表） |
| 插入 | $O(\\log n)$ | $O(n)$ |
| 删除 | $O(\\log n)$ | $O(n)$ |

---

## 删除结点的三种情况
| 被删结点 | 处理方式 |
|----------|----------|
| 叶子 | 直接删除 |
| 只有左/右子树 | 用孩子替代 |
| 有左右子树 | 用**直接后继**（右子树最左结点）或直接前驱替代 |

---

## 考试辨析
- BST 中序序列有序——利用此性质判断是否为 BST
- BST 可能退化为链表（插入有序序列时），因此引出 AVL（平衡二叉树）
- 408 算法题可能考 BST 的查找/插入代码`,
  },
  {
    id: 'ds-7-3-2',
    chapterId: 'ds-7',
    title: '平衡二叉树 (AVL)',
    keyConcepts: ['AVL', '平衡二叉树', '平衡因子', 'LL', 'RR', 'LR', 'RL', '旋转'],
    relatedPoints: ['ds-7-3-1', 'ds-5-2-2'],
    content: `## AVL 定义

> 任意结点的**左右子树高度差不超过 1** 的二叉排序树。

平衡因子 = 左子树高度 - 右子树高度，取值仅 { -1, 0, 1 }。

---

## 四种失衡调整（高频考点！）

| 类型 | 失衡点 | 调整 |
|------|--------|------|
| **LL** | 左子树左孩子插入 | **右旋** |
| **RR** | 右子树右孩子插入 | **左旋** |
| **LR** | 左子树右孩子插入 | 先左旋后右旋 |
| **RL** | 右子树左孩子插入 | 先右旋后左旋 |

---

## AVL 的性质

| 性质 | 值 |
|------|-----|
| 含有 $n$ 个结点的 AVL 最大高度 | $O(\\log n)$ |
| 高度为 $h$ 的 AVL 最少结点数 | $N_h = N_{h-1} + N_{h-2} + 1$，（$N_0=0, N_1=1$） |
| 查找复杂度 | $O(\\log n)$（始终平衡） |

---

## 考试辨析
- 408 考查 AVL 主要是**手工模拟旋转**（选择题）
- 给定插入序列，画出插入后的 AVL
- 了解 LL/RR/LR/RL 四种失衡型的手工调整方法即可`,
  },
  {
    id: 'ds-7-3-3',
    chapterId: 'ds-7',
    title: '红黑树',
    keyConcepts: ['红黑树', '红结点', '黑结点', '黑高', '近似平衡'],
    relatedPoints: ['ds-7-3-2'],
    content: `## 红黑树定义

> 红黑树是一种**近似平衡**的二叉查找树，通过颜色约束保证最长路径不超过最短路径的 2 倍。

---

## 五条性质
1. 每个结点是**红**色或**黑**色
2. 根结点是**黑色**
3. 叶结点（NIL）是**黑色**
4. 红色结点的两个孩子**必为黑色**（不存在连续红色）
5. 从任一结点到其每个叶结点的所有路径包含**相同数量的黑结点**

---

## 黑高 (Black Height)
> 从某结点到叶结点（不含自身）的黑色结点数。

根的黑高 $\\ge \\frac{h}{2}$（因为不能有连续红色）。

---

## vs AVL
| | AVL | 红黑树 |
|------|-----|--------|
| 平衡程度 | 严格平衡（-1/0/1） | 近似平衡 |
| 插入/删除 | 旋转次数多 | 旋转次数少 |
| 查找 | 更快 | 稍慢 |
| 适用 | 查找多 | **插入/删除多** |

---

## 考试辨析
- 408 对红黑树要求较浅——会判断是否符合红黑树定义即可
- 了解性质 4、5 用于推导路径长度关系`,
  },

  // --- 7.4 散列表 ---
  {
    id: 'ds-7-4-1',
    chapterId: 'ds-7',
    title: 'B树与B+树',
    keyConcepts: ['B树', 'B+树', '多路平衡查找树', 'm阶', '关键字', '子树'],
    relatedPoints: ['ds-7-3-1'],
    content: `## B 树 (B-Tree)

> **m 阶 B 树**是一棵**多路平衡查找树**，每个结点最多 $m$ 棵子树。

### 性质（m 阶 B 树）
- 每个结点最多 $m$ 个子树，最多 $m-1$ 个关键字
- 根结点至少 2 个子树（非叶）
- 非根非叶结点至少 $\\lceil m/2 \\rceil$ 个子树
- 所有叶结点在同一层（平衡）
- 结点内关键字**有序**

---

## B+ 树

### 与 B 树的区别
| | B 树 | B+ 树 |
|------|------|-------|
| 关键字数量 | 每个结点最多 $m-1$ 个 | 最多 $m$ 个 |
| 非叶结点存数据吗 | **存** | **不存**（只存索引） |
| 叶结点 | 都在同一层 | 包含全部关键字，**链表串联** |
| 查找 | 可能在非叶层结束 | **必须到叶层** |

---

## 考试辨析
- B+ 树支持**顺序查找**（叶结点链表），B 树不行
- B+ 树更适合**范围查询**（数据库索引常用）
- 408 主要考 B/B+ 树性质的选择题`,
  },
  {
    id: 'ds-7-4-2',
    chapterId: 'ds-7',
    title: '散列表的概念与构造',
    keyConcepts: ['散列表', '哈希表', '散列函数', '除留余数法', '装填因子'],
    relatedPoints: ['ds-7-4-3'],
    content: `## 散列表的定义

> 通过**散列函数**将关键字映射到存储地址，实现快速查找。

散列表是**空间换时间**的经典应用：理论上可达到 $O(1)$ 的查找时间。

---

## 散列函数设计

| 方法 | 说明 |
|------|------|
| **除留余数法** | $H(key) = key \\bmod p$，$p$ 是不大于表长的最大素数 |
| 直接定址法 | $H(key) = a \\times key + b$ |
| 平方取中法 | 取 $key^2$ 的中间几位 |
| 数字分析法 | 适合关键字各位分布不均匀 |

---

## 装填因子
$$\\alpha = \\frac{\\text{表中记录数}}{\\text{表长}}$$

$\\alpha$ 越大，空间越满，冲突概率越高。

---

## 考试辨析
- 散列表查找效率取决于：散列函数、冲突处理方法、装填因子
- 选择合适的 $p$（素数）能减少冲突`,
  },
  {
    id: 'ds-7-4-3',
    chapterId: 'ds-7',
    title: '散列表的冲突处理',
    keyConcepts: ['冲突', '开放定址法', '链地址法', '线性探测', '二次探测', '再散列'],
    relatedPoints: ['ds-7-4-2'],
    content: `## 开放定址法

当冲突发生时，按某种探测序列找下一个空闲地址：

$$H_i = (H(key) + d_i) \\bmod m$$

| 方法 | $d_i$ | 特点 |
|------|-------|------|
| **线性探测** | $1,2,3,\\ldots$ | 简单，但容易**聚集** |
| **平方探测** | $1,-1,4,-4,\\ldots$ | 减少聚集，但可能探测不到全部位置 |
| **再散列** | $i \\times H_2(key)$ | 双散列，效果好 |

---

## 链地址法（拉链法）

> 同义词用链表链接起来。

| 特点 | 说明 |
|------|------|
| 不会"堆满" | 可存储超过表长的记录 |
| 查找 | 散列后沿链表查找 |
| ASL | 冲突越多，链表越长 |

---

## 查找长度计算（常考）

**ASL(成功)** = 各关键字查找次数之和 / 关键字个数
**ASL(失败)** = 各地址查找失败次数之和 / 散列地址数

**平均查找长度取决于装填因子 $\\alpha$**，与表长无关！

---

## 考试辨析
- 线性探测法插入、查找、删除都需**依次探测**
- 删除不能用"直接置空"（会切断探测链），需用**懒惰删除**标记
- 散列表的 ASL 计算是408常考题型`,
  },

  // --- 7.5 串的匹配 ---
  // --- 补充 ---
  {
    id: 'ds-7-4-4', chapterId: 'ds-7', title: 'B树与B+树的详细对比',
    keyConcepts: ['B树', 'B+树', 'm阶', '子树', '关键字', '分裂', '数据库索引'], relatedPoints: ['ds-7-4-1'],
    content: `## m阶B树核心性质
- 每个结点至多m棵子树，至多m-1个关键字
- 根结点至少2棵子树；非根非叶至少ceil(m/2)棵子树
- **n个关键字对应n+1棵子树**——关键字间有指针到子树
- 所有叶结点（失败结点）在同一层

## B树插入——分裂
插入到终端结点，若关键字>m-1则从ceil(m/2)位置分裂：
中间关键字上移到父结点，左右分成两个结点。父结点溢出则继续向上分裂。

## B树删除
- 非终端结点：用前驱或后继替换后，转化为删终端结点
- 终端结点够借：父子换位法（父亲下来，兄弟上去）
- 终端结点不够借：与兄弟+父关键字合并，可能连锁合并

---
## B树 vs B+树 核心对比
| | B树 | B+树 |
|------|-----|------|
| n个关键字子树数 | **n+1** | **n** |
| 根关键字范围 | [1, m-1] | [1, m] |
| 非根关键字范围 | [ceil(m/2)-1, m-1] | [ceil(m/2), m] |
| 关键字重复 | 不重复 | 叶结点含全部(重复) |
| 数据存储 | 所有结点均可 | **仅叶结点存数据** |
| 非叶结点 | 存数据+索引 | **仅索引** |
| 范围查询 | 低(需回溯) | **高(叶结点链表)** |
| 查找路径 | 可能在中间命中 | 必须走到底 |

---
## B树高度公式
log_m(n+1) <= h <= log_{ceil(m/2)}((n+1)/2) + 1

## 考试辨析
- B+树更适合数据库索引——范围查询快(叶子链表)
- B+树非叶结点不存数据——同样高度能索引更多记录
- 5阶B树=每个结点最多5子树4关键字（对应2-3-4树）`,
  },

  // --- 补充 ---
  {
    id: 'ds-8-6-2', chapterId: 'ds-8', title: '排序算法完整对比与选择指南',
    keyConcepts: ['排序', '时间复杂度', '空间复杂度', '稳定性', '选择依据'], relatedPoints: ['ds-8-6-1'],
    content: `## 内部排序算法总表
| 算法 | 平均 | 最坏 | 空间 | 稳定 | 适用存储 |
|------|------|------|------|:--:|----------|
| 直接插入 | O(n²) | O(n²) | O(1) | ✅ | 顺序+链式 |
| 折半插入 | O(n²) | O(n²) | O(1) | ✅ | 仅顺序 |
| 希尔 | ~O(n^1.3) | O(n²) | O(1) | ❌ | 仅顺序 |
| 冒泡 | O(n²) | O(n²) | O(1) | ✅ | 顺序+链式 |
| **快速** | **O(nlogn)** | O(n²) | O(logn) | ❌ | 仅顺序 |
| 简单选择 | O(n²) | O(n²) | O(1) | ❌ | 顺序+链式 |
| **堆** | **O(nlogn)** | **O(nlogn)** | **O(1)** | ❌ | 仅顺序 |
| **归并** | **O(nlogn)** | **O(nlogn)** | **O(n)** | ✅ | 顺序+链式 |
| 基数 | O(d(n+r)) | O(d(n+r)) | O(r) | ✅ | 顺序+链式 |

---
## 稳定性口诀：插冒归基
稳定排序 = **插**入 + **冒**泡 + **归**并 + **基**数（余下均不稳定）

## 选择指南
- n小 → 直接插入/冒泡（简单）
- n大+随机分布 → **快速排序**（平均最优）
- n大+内存少 → **堆排序**（O(1)额外空间） |
- n大+要稳定 → **归并排序**（唯一稳定O(nlogn)排序） |
- 基本有序 → 直接插入/冒泡（可到O(n)） |

## 各算法的关键特征
- 快排：越有序越慢（最坏O(n²)）；一趟确定一个枢轴最终位置
- 堆排：建堆O(n)（不是O(nlogn)！）；一趟确定全局最值
- 归并：比较次数与初态无关；空间O(n)
- 希尔：不稳定（分组导致同值元素跳跃交换）
- 基数：不基于比较——突破O(nlogn)下界

---
## 考试辨析
- 堆排序是O(nlogn)中唯一空间O(1)的——适合嵌入式
- 快排空间O(logn)来自递归栈（不是O(1)！）
- 简单选择排序不稳定——例：5 8 5 2 -> 2 8 5 5（两个5换位了）`,
  },

  {
    id: 'ds-7-5-1',
    chapterId: 'ds-7',
    title: '串的模式匹配（查找视角）',
    keyConcepts: ['串匹配', 'BF', 'KMP', 'next数组'],
    relatedPoints: ['ds-4-2-1', 'ds-4-2-2', 'ds-4-2-3'],
    content: `## 串的模式匹配与查找

串的模式匹配本质上是**特殊查找**——在文本串中查找模式串的位置。

详见第4章「串」中的详细讲解。

| 算法 | 时间 | 核心思想 |
|------|------|----------|
| BF | $O(n \\times m)$ | 暴力回溯 |
| KMP | $O(n + m)$ | next数组免回溯 |

---

## 考试辨析
- 408 将"串的模式匹配"列为查找的一部分，但考试重点在第4章`,
  },

  // ============================================================
  // 第8章 排序
  // ============================================================

  // --- 8.1 排序的基本概念 ---
  {
    id: 'ds-8-1-1',
    chapterId: 'ds-8',
    title: '排序的基本概念',
    keyConcepts: ['排序', '稳定性', '内部排序', '外部排序', '比较'],
    relatedPoints: [],
    content: `## 排序的定义

> 将一组"无序"的记录序列调整为"有序"的记录序列。

---

## 核心概念
| 术语 | 含义 |
|------|------|
| **稳定性** | 若 $a=b$ 且 $a$ 在 $b$ 前，排序后 $a$ 仍在 $b$ 前，则算法**稳定** |
| **内部排序** | 全部记录可同时放入内存 |
| **外部排序** | 记录太多，需要内外存交换 |
| **比较排序** | 基于关键字的比较（$\\Omega(n\\log n)$ 下界） |

---

## 考试辨析
- **稳定性**是排序选择+大题的高频考点
- 记住哪些排序稳定、哪些不稳定——这是408选择题的永恒主题`,
  },

  // --- 8.2 插入排序 ---
  {
    id: 'ds-8-2-1',
    chapterId: 'ds-8',
    title: '插入排序（直接插入与折半插入）',
    keyConcepts: ['直接插入排序', '折半插入排序', '稳定', 'O(n^2)'],
    relatedPoints: ['ds-8-1-1'],
    content: `## 直接插入排序

> 将待排序记录逐个插入已排好的序列中。像打扑克牌理牌。

\\\`\\\`\\\`c
void InsertSort(int a[], int n) {
    for (int i = 1; i < n; i++) {
        int tmp = a[i], j;
        for (j = i - 1; j >= 0 && a[j] > tmp; j--)
            a[j + 1] = a[j];
        a[j + 1] = tmp;
    }
}
\\\`\\\`\\\`

| 属性 | 值 |
|------|-----|
| 时间(最好) | $O(n)$（已有序） |
| 时间(最坏) | $O(n^2)$（逆序） |
| 空间 | $O(1)$ |
| **稳定** | ✅ |

---

## 折半插入排序

用折半查找确定插入位置，减少比较次数（$O(n\\log n)$），但移动次数不变（$O(n^2)$）。

---

## 考试辨析
- 直接插入排序的最好情况 $O(n)$——当序列基本有序时
- 插入排序是**稳定**的
- 适用于 $n$ 较小时`,
  },
  {
    id: 'ds-8-2-2',
    chapterId: 'ds-8',
    title: '希尔排序',
    keyConcepts: ['希尔排序', '缩小增量', '不稳定', 'O(n^1.3)'],
    relatedPoints: ['ds-8-2-1'],
    content: `## 希尔排序思想

> 将记录按增量分组，每组进行直接插入排序；逐步缩小增量，直到增量为 1。

---

## 时间复杂度
| 增量序列 | 最坏 |
|----------|------|
| 希尔原始（倍数减半） | $O(n^2)$ |
| Hibbard（$2^k-1$） | $O(n^{1.5})$ |

---

## 特性
| 属性 | 值 |
|------|-----|
| 空间 | $O(1)$ |
| **稳定** | ❌ **不稳定**（分组导致同值元素可能交换） |

---

## 考试辨析
- 希尔排序是**不稳定**的——分组跨越可能交换同值元素
- 希尔排序是插入排序的改进——利用"基本有序 + 直接插入"的优势`,
  },

  // --- 8.3 交换排序 ---
  {
    id: 'ds-8-3-1',
    chapterId: 'ds-8',
    title: '冒泡排序',
    keyConcepts: ['冒泡排序', '交换', '稳定', 'O(n^2)', '最好O(n)'],
    relatedPoints: ['ds-8-1-1'],
    content: `## 冒泡排序

> 相邻元素两两比较，逆序则交换；每趟确定一个最大/最小元素的位置。

\\\`\\\`\\\`c
void BubbleSort(int a[], int n) {
    for (int i = 0; i < n - 1; i++) {
        bool flag = false;
        for (int j = 0; j < n - 1 - i; j++)
            if (a[j] > a[j + 1]) {
                swap(a[j], a[j + 1]);
                flag = true;
            }
        if (!flag) break;  // 已有序，提前结束
    }
}
\\\`\\\`\\\`

| 属性 | 值 |
|------|-----|
| 时间(最好) | $O(n)$（已有序，一趟结束） |
| 时间(最坏) | $O(n^2)$ |
| 空间 | $O(1)$ |
| **稳定** | ✅ |

---

## 考试辨析
- 带 flag 优化后最好情况 $O(n)$
- 冒泡排序是**稳定**的（相邻交换，不会跨过相等元素）`,
  },
  {
    id: 'ds-8-3-2',
    chapterId: 'ds-8',
    title: '快速排序',
    keyConcepts: ['快速排序', '分治', '枢轴', '划分', '不稳定', 'O(n log n)'],
    relatedPoints: ['ds-8-3-1', 'ds-5-3-1'],
    content: `## 快速排序思想

> **分治**：选一个枢轴 (pivot)，划分成左右两部分——左边 $\\le$ 枢轴 $\\le$ 右边，再递归排序左右。

\\\`\\\`\\\`c
int Partition(int a[], int low, int high) {
    int pivot = a[low];
    while (low < high) {
        while (low < high && a[high] >= pivot) high--;
        a[low] = a[high];
        while (low < high && a[low] <= pivot) low++;
        a[high] = a[low];
    }
    a[low] = pivot;
    return low;
}

void QuickSort(int a[], int low, int high) {
    if (low < high) {
        int pos = Partition(a, low, high);
        QuickSort(a, low, pos - 1);
        QuickSort(a, pos + 1, high);
    }
}
\\\`\\\`\\\`

| 属性 | 值 |
|------|-----|
| 时间(最好) | $O(n\\log n)$（均匀划分） |
| 时间(最坏) | $O(n^2)$（有序或逆序 —— 退化为冒泡） |
| 空间 | $O(\\log n)$（递归栈） |
| **稳定** | ❌ **不稳定** |

---

## 考试辨析
- 快排是408大题的常客（2011、2013、2016年均考过）
- **快排不稳定**——划分过程中的元素互换可能打乱等值顺序
- 快排空间复杂度 $O(\\log n)$（而非 $O(1)$），因为递归栈深度
- 不稳定性+最坏情况 $O(n^2)$ 是快排选择题的常见干扰项`,
  },

  // --- 8.4 选择排序 ---
  {
    id: 'ds-8-4-1',
    chapterId: 'ds-8',
    title: '简单选择排序与堆排序',
    keyConcepts: ['选择排序', '堆排序', '不稳定', 'O(n^2)', 'O(n log n)', '大根堆'],
    relatedPoints: ['ds-8-1-1', 'ds-5-5-3'],
    content: `## 简单选择排序

每趟从待排序部分选最小的，放到已排序末尾。

| 属性 | 值 |
|------|-----|
| 时间(最好/最坏) | $O(n^2)$ |
| 空间 | $O(1)$ |
| **稳定** | ❌ **不稳定** |

---

## 堆排序

利用大根堆/小根堆进行排序。

| 属性 | 值 |
|------|-----|
| 建堆时间 | $O(n)$ |
| 总体时间 | $O(n\\log n)$ |
| 空间 | $O(1)$ |
| **稳定** | ❌ **不稳定** |

---

## 考试辨析
- 堆排序**空间 $O(1)$** —— 它是**原地**的（区别于快排需要 $O(\\log n)$ 递归栈）
- 建堆复杂度 $O(n)$ 不是 $O(n\\log n)$ —— 利用"多数结点在底层"的性质
- 堆排序为 **$O(1)$ 空间 + $O(n\\log n)$ 时间**，但不稳定`,
  },

  // --- 8.5 归并排序和基数排序 ---
  {
    id: 'ds-8-5-1',
    chapterId: 'ds-8',
    title: '归并排序',
    keyConcepts: ['归并排序', '分治', '二路归并', '稳定', 'O(n log n)', 'O(n)空间'],
    relatedPoints: ['ds-8-1-1'],
    content: `## 归并排序思想

> **分治**：将序列递归分成两半，排序后合并（归并）。

\\\`\\\`\\\`c
void MergeSort(int a[], int low, int high) {
    if (low < high) {
        int mid = (low + high) / 2;
        MergeSort(a, low, mid);
        MergeSort(a, mid + 1, high);
        Merge(a, low, mid, high);  // O(n) 把两个有序段合并
    }
}
\\\`\\\`\\\`

| 属性 | 值 |
|------|-----|
| 时间(任何情况) | $O(n\\log n)$ |
| 空间 | **$O(n)$** |
| **稳定** | ✅ |

---

## 归并排序特性
- 适合**外部排序**（可逐段读入合并）
- 时间和空间都是 $O(n\\log n)$
- $O(n)$ 额外空间是归并排序的主要缺点

---

## 考试辨析
- 归并排序是**稳定**的 $O(n\\log n)$ 算法——唯一的稳定+高效的内部排序
- 归并排序的比较次数与初始顺序无关
- 算法题常考**两个有序数组的合并**（Merge操作）`,
  },
  {
    id: 'ds-8-5-2',
    chapterId: 'ds-8',
    title: '基数排序',
    keyConcepts: ['基数排序', 'LSD', 'MSD', '分配', '收集', '稳定', 'O(d(n+r))'],
    relatedPoints: ['ds-8-5-1'],
    content: `## 基数排序思想

> 不基于比较，而是通过"分配"和"收集"按关键字各位进行排序。

| 方式 | 说明 |
|------|------|
| **LSD** | 从低位到高位 |
| **MSD** | 从高位到低位 |

---

## 复杂度
| 属性 | 值 |
|------|-----|
| 时间 | $O(d(n+r))$，$d$=位数，$r$=基数 |
| 空间 | $O(r)$（队列数组） |
| **稳定** | ✅（LSD方式） |

---

## 考试辨析
- 基数排序**不基于比较**——突破了 $\\Omega(n\\log n)$ 下界
- 不适合关键字范围太大的情况
- 408 考得较少，了解基本原理即可`,
  },

  // --- 8.6 各种内部排序算法的比较 ---
  {
    id: 'ds-8-6-1',
    chapterId: 'ds-8',
    title: '各种内部排序算法的比较',
    keyConcepts: ['排序对比', '时间', '空间', '稳定性', '选择依据'],
    relatedPoints: ['ds-8-1-1', 'ds-8-2-1', 'ds-8-3-1', 'ds-8-4-1', 'ds-8-5-1'],
    content: `## 综合对比总表（408必背）

| 算法 | 最好 | 最坏 | 平均 | 空间 | 稳定 |
|------|------|------|------|------|------|
| 直接插入 | $O(n)$ | $O(n^2)$ | $O(n^2)$ | $O(1)$ | ✅ |
| 希尔 | — | $O(n^2)$ | ~$O(n^{1.3})$ | $O(1)$ | ❌ |
| 冒泡 | $O(n)$ | $O(n^2)$ | $O(n^2)$ | $O(1)$ | ✅ |
| 快速 | $O(n\\log n)$ | $O(n^2)$ | $O(n\\log n)$ | $O(\\log n)$ | ❌ |
| 简单选择 | $O(n^2)$ | $O(n^2)$ | $O(n^2)$ | $O(1)$ | ❌ |
| 堆 | $O(n\\log n)$ | $O(n\\log n)$ | $O(n\\log n)$ | $O(1)$ | ❌ |
| 归并 | $O(n\\log n)$ | $O(n\\log n)$ | $O(n\\log n)$ | $O(n)$ | ✅ |
| 基数 | $O(d(n+r))$ | $O(d(n+r))$ | $O(d(n+r))$ | $O(r)$ | ✅ |

---

## 选择建议
| 场景 | 推荐算法 |
|------|----------|
| $n$ 很小 | 直接插入排序 |
| $n$ 较大，要求稳定 | 归并排序 |
| $n$ 较大，内存少 | **堆排序**（$O(1)$ 额外空间） |
| $n$ 较大，一般情况 | **快速排序** |
| 基本有序 | 直接插入、冒泡 |

---

## 考试辨析
- 这个对比表必须能默写——408 选择题和大题的"第一问"经常从此出发
- 稳定性排序：**插冒归基**（插入、冒泡、归并、基数）即"**插冒归基**"
- $O(n\\log n)$ 中只有归并**稳定**，只有堆**原地**+$O(n\\log n)$`,
  },

  // --- 8.7 外部排序 ---
  {
    id: 'ds-8-7-1',
    chapterId: 'ds-8',
    title: '外部排序',
    keyConcepts: ['外部排序', '归并段', '败者树', '最佳归并树', '置换选择排序'],
    relatedPoints: ['ds-8-5-1', 'ds-5-5-1'],
    content: `## 外部排序的基本思想

> 当记录太多无法全部装入内存时，需要利用外存进行排序。

核心：**归并排序**——先产生初始归并段，再 $k$ 路归并。

---

## 时间复杂度
$$\\text{总时间} = \\text{初始归并段产生} + \\text{归并趟数} \\times \\text{每趟 IO}$$

影响总时间的因素：归并趟数 + 每次归并的 IO 量。

---

## 优化方法
| 方法 | 效果 |
|------|------|
| **多路归并**（增大 $k$） | 减少归并趟数 |
| **败者树** | 减少 $k$ 路归并中的比较次数 |
| **置换选择排序** | 产生更长的初始归并段 |
| **最佳归并树**（哈夫曼树） | 最小化总归并代价 |

---

## 考试辨析
- 外部排序采用**归并排序**——因为归并可逐段读写
- 最佳归并树 = 哈夫曼树的应用（2012年真题考察）
- $m$ 个初始归并段做 $k$ 路归并，趟数 = $\\lceil \\log_k m \\rceil$`,
  },
];
